<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://baba9811.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://baba9811.github.io/" rel="alternate" type="text/html" hreflang="ko"/><updated>2026-05-29T00:35:44+00:00</updated><id>https://baba9811.github.io/feed.xml</id><title type="html">Research Notes</title><subtitle>Paper reviews and research notes on machine learning and AI. </subtitle><entry xml:lang="ko"><title type="html">[논문 리뷰] Ferret-UI Lite: Lessons from Building Small On-Device GUI Agents</title><link href="https://baba9811.github.io/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/" rel="alternate" type="text/html" title="[논문 리뷰] Ferret-UI Lite: Lessons from Building Small On-Device GUI Agents"/><published>2026-05-29T06:30:00+00:00</published><updated>2026-05-29T06:30:00+00:00</updated><id>https://baba9811.github.io/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age</id><content type="html" xml:base="https://baba9811.github.io/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/"><![CDATA[<div class="lang-toggle" style="margin-bottom: 1.5rem;"> <a href="/en/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/" class="btn btn-sm btn-outline-secondary">🇺🇸 Read in English</a> </div> <h4 id="메타정보">메타정보</h4> <table> <thead> <tr> <th>항목</th> <th>내용</th> </tr> </thead> <tbody> <tr> <td>저자</td> <td>Zhen Yang et al. (저자 16명, Apple)</td> </tr> <tr> <td>학회</td> <td>arXiv preprint · 2025</td> </tr> <tr> <td>arXiv</td> <td><a href="https://arxiv.org/abs/2509.26539">2509.26539</a></td> </tr> <tr> <td>데이터</td> <td>GUI 그라운딩·내비게이션 공개 데이터셋 (OS-Atlas, UGround, Aria-UI, Aguvis, Jedi, ShowUI 등) + 모바일·데스크톱 합성 데이터</td> </tr> <tr> <td><span style="white-space: nowrap">리뷰 일자</span></td> <td>2026-05-29</td> </tr> </tbody> </table> <h4 id="tldr">TL;DR</h4> <ul> <li>Apple이 모바일·웹·데스크톱을 하나로 다루는 <strong>3B 크기의 end-to-end 온디바이스 GUI 에이전트 Ferret-UI Lite</strong>를 공개했다. 그라운딩에서 ScreenSpot-V2 91.6, ScreenSpot-Pro 53.3, OSWorld-G 55.3을 기록해 비슷한 3B 모델을 큰 폭으로 앞서고 7B급과의 격차를 좁혔다. 내비게이션은 AndroidWorld 28.0%, OSWorld 19.8%로 같은 크기에선 강하지만 큰 모델엔 여전히 못 미친다.</li> <li>레시피는 세 갈래다. (1) 실데이터와 합성 데이터를 <strong>통합 행동 공간 (unified action space)</strong> 아래 대규모로 큐레이션하고, (2) 추론 시점에 <strong>zoom-in 시각 도구 사용 (visual tool-use)</strong>으로 고해상도 화면을 정밀하게 보고, (3) <strong>SFT → RLVR</strong> 2단계 학습에 검증 가능한 보상 (verifiable reward)을 설계해 붙인다.</li> <li>제목 그대로 “교훈 보고서”다. <strong>작은 모델은 RL 보상 설계에 매우 민감하고</strong>, 그라운딩·내비게이션 데이터는 서로를 보완하며, 고해상도 합성 데이터가 정밀 그라운딩에 특히 효과적이라는 점을 ablation으로 보여준다. 코드·모델 가중치는 공개되지 않았다.</li> </ul> <h4 id="소개-introduction">소개 (Introduction)</h4> <p>GUI 에이전트 (graphical user interface agent)는 사람이 마우스와 키보드로 하던 화면 조작을 모델이 대신하는 기술이다. 화면을 보고 “이 버튼을 누르고, 저 칸에 텍스트를 입력하고, 다음 화면으로 넘어가라”를 스스로 판단해 실행한다. 운전 중에 손을 쓰지 않고 리마인더를 적거나, 요리하다 젖은 손으로 레시피를 띄우는 상황을 떠올리면, 이런 보조 기능은 <strong>낮은 지연 시간, 강한 프라이버시 보장, 연결이 끊긴 환경에서의 견고함</strong>을 요구한다. 세 조건 모두 “서버로 화면을 보내 큰 모델이 처리”하는 방식과 충돌한다. 그래서 기기 위에서 직접 도는 작은 (on-device) GUI 에이전트가 필요하다.</p> <p>문제는 기존 연구의 무게중심이 정반대에 있다는 점이다. 대다수 방법은 거대 파운데이션 모델에 기댄다. 지각·계획·행동을 분리한 멀티 에이전트 시스템을 GPT나 Gemini 같은 대형 LLM 위에 올려 인상적인 내비게이션 성능을 내지만, 모델링 복잡도·컴퓨팅 예산·추론 시간이 모두 커진다. end-to-end GUI 에이전트는 원시 스크린샷을 행동으로 곧장 매핑해 이 워크플로우를 단순화하는 매력적인 대안이지만, 여기서도 큰 모델이 선호된다. 하나의 모델 안에 저수준 그라운딩, 화면 이해, 멀티스텝 계획, 자기 반성 (self-reflection)이라는 이질적 능력을 모두 욱여넣어야 하기 때문이다.</p> <p>Ferret-UI Lite는 이 불균형을 정면으로 겨냥한다. “작은 모델로도 강한 GUI 에이전트를 만들 수 있는가, 만들 수 있다면 어떤 기법이 효과적이고 어디서 한계에 부딪히는가”를 3B 규모에서 체계적으로 실험하고, 그 과정에서 얻은 교훈을 정직하게 보고하는 것이 이 논문의 본질이다. Apple의 Ferret-UI 계보 (Ferret-UI, Ferret-UI 2)를 잇는 작업으로, 이번엔 “이해”보다 “에이전트로서의 행동”과 “온디바이스 효율”에 초점을 맞췄다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig1-capability-vs-size-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig1-capability-vs-size-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig1-capability-vs-size-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig1-capability-vs-size.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1. 모델 크기 대비 성능. (a) 그라운딩에서 Ferret-UI Lite(빨간 별, 3B)는 같은 크기 모델은 물론 여러 큰 모델까지 앞선다. (b) 내비게이션에서는 3B 중 최상위지만 72B급과는 격차가 분명하다 — 작은 모델에서 멀티스텝 추론이 본질적으로 어렵다는 신호.</figcaption> </figure> <h4 id="핵심-기여-key-contributions">핵심 기여 (Key Contributions)</h4> <ul> <li><strong>3B 온디바이스 end-to-end GUI 에이전트</strong>: 모바일·웹·데스크톱을 단일 모델로 다루며, 그라운딩과 내비게이션을 한 번에 학습한다. 같은 3B급에서 그라운딩 SOTA를 큰 폭으로 갱신한다.</li> <li><strong>실+합성 데이터의 대규모 큐레이션 + 통합 행동 공간</strong>: 공개 데이터셋들을 점 기반 (point-based) 좌표와 함수 호출 (function-call) 형식으로 통일하고, 부족한 부분을 합성 데이터로 메운다. 특히 멀티 에이전트 시스템으로 온라인 롤아웃을 생성하는 파이프라인이 핵심이다.</li> <li><strong>추론 시점 zoom-in 시각 도구 사용</strong>: 한 번 예측한 위치 주변을 잘라 다시 보는 “이미지로 생각하기” 메커니즘으로, 작은 모델이 큰 해상도의 복잡한 화면에서도 좁은 영역만 정밀하게 판단하도록 만든다.</li> <li><strong>검증 가능한 보상 기반 2단계 학습 (SFT → RLVR)</strong>: 그라운딩에는 박스 포함 (containment) 보상, 내비게이션에는 행동 타입 + 파라미터 보상을 설계한다. 작은 모델에서 이 보상 설계가 성능을 좌우한다는 점을 ablation으로 입증한다.</li> <li><strong>스케일다운의 교훈 정리</strong>: 데이터 혼합 비율, 고해상도 데이터, CoT 길이, RL 보상 구성에 대한 풍부한 ablation으로 “무엇이 작은 모델에 통하고 무엇이 안 통하는가”를 기록한다. 이 점이 단일 SOTA 수치보다 더 오래 남을 기여다.</li> </ul> <h4 id="관련-연구--배경-지식">관련 연구 / 배경 지식</h4> <p><strong>그라운딩과 내비게이션.</strong> GUI 태스크는 크게 두 가지로 나뉜다. <strong>그라운딩 (grounding)</strong>은 “Refresh 버튼을 눌러” 같은 자연어 지시에서 클릭할 화면 좌표를 찍는 단일 스텝 문제다. <strong>내비게이션 (navigation)</strong>은 “이 포스터를 엄마에게 메일로 보내”처럼 여러 스텝에 걸쳐 화면을 바꿔가며 목표를 달성하는 문제로, 누적된 화면·행동 히스토리 위에서 계획을 세워야 한다. 그라운딩은 정밀한 위치, 내비게이션은 장기 계획이 생명이라 보통 따로 다뤄진다. Ferret-UI Lite는 둘을 한 모델에서 함께 학습한다.</p> <p><strong>end-to-end vs 멀티 에이전트.</strong> 멀티 에이전트 방식은 계획 모듈과 그라운딩 모듈을 분리하고, GPT·Gemini 같은 대형 모델을 계획자로 쓴 뒤 전용 모듈로 좌표를 찍는다. 강력하지만 무겁다. end-to-end 방식은 OS-Atlas, UI-TARS, ShowUI처럼 멀티모달 입력에서 행동 시퀀스를 곧장 예측하는 단일 vision-language-action 모델로, Ferret-UI Lite도 여기에 속한다. 흥미롭게도 Ferret-UI Lite는 <strong>학습 데이터 생성에는 멀티 에이전트 시스템을 쓰되, 배포 모델은 단일 end-to-end</strong>로 만든다 — 멀티 에이전트의 풍부함을 작은 단일 모델로 증류하는 셈이다.</p> <p><strong>RLVR과 GRPO.</strong> 검증 가능한 보상 기반 강화학습 (Reinforcement Learning with Verifiable Rewards, RLVR)은 사람 라벨 대신 규칙으로 자동 계산되는 보상을 쓴다. GUI 그라운딩은 “예측 좌표가 정답 박스 안에 들어갔는가”처럼 보상을 자동 검증할 수 있어 RLVR과 궁합이 좋다. 최적화에는 DeepSeekMath가 제안한 Group Relative Policy Optimization (GRPO)을 쓴다. 한 입력에 대해 여러 후보를 샘플링하고, 그룹 내 보상의 평균·표준편차로 정규화한 어드밴티지로 정책을 업데이트하는 방식이라 가치 함수 (value network) 없이도 안정적이다.</p> <p><strong>이미지로 생각하기 (thinking with images).</strong> 최근 비전 추론 흐름은 모델이 이미지를 한 번만 보고 끝내는 대신, 관심 영역을 잘라 확대해 다시 보는 능동적 시각 행동을 학습시킨다. Ferret-UI Lite의 zoom-in은 이 아이디어를 그라운딩에 적용한 것이다.</p> <h4 id="방법--아키텍처-상세">방법 / 아키텍처 상세</h4> <h5 id="모델-백본과-입출력">모델 백본과 입출력</h5> <p>Ferret-UI Lite는 텍스트 전용 데이터와 비전-언어 이해 데이터를 섞어 사전학습한 <strong>내부 3B dense 모델</strong>을 출발점으로 한다. 이미지 인코더는 객체 탐지용 plain ViT 백본인 VitDet 구조를 쓰고, 입력 스크린샷을 셀 격자로 동적으로 쪼개는 <strong>AnyRes</strong> 전략을 채택한다. 고해상도 UI 스크린샷을 토큰 폭발 없이 처리하기 위한 선택이다. 인코더가 뽑은 시각 토큰은 <strong>decoder-only LLM</strong>으로 들어가고, 모델은 GUI 화면과 사용자 지시를 입력받아 chain-of-thought 추론 트레이스 (<code class="language-plaintext highlighter-rouge">&lt;think&gt;...</code>)와 저수준 행동 정책 (<code class="language-plaintext highlighter-rouge">&lt;function&gt;...</code>)을 순차적으로 출력한다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig3-architecture-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig3-architecture-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig3-architecture-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig3-architecture.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 3. 모델 아키텍처와 학습 레시피. 사용자 지시와 스크린샷이 이미지 인코더를 거쳐 decoder-only LLM으로 들어가고, 모델은 think 트레이스와 function 호출(행동)을 생성한다. 학습은 SFT → RLVR 2단계로 진행된다.</figcaption> </figure> <h5 id="통합-행동-공간-unified-action-space">통합 행동 공간 (Unified Action Space)</h5> <p>이질적인 공개 데이터셋을 한 모델로 학습하려면 행동 표기부터 통일해야 한다. Ferret-UI Lite는 두 축에서 형식을 통일한다.</p> <ul> <li><strong>그라운딩 좌표</strong>: 어떤 데이터셋은 bounding box를, 어떤 데이터셋은 단일 점을 준다. 모든 박스를 기하 중심점으로 변환해 점 기반 표현으로 정규화한다.</li> </ul> \[(x_{\text{center}}, y_{\text{center}}) = \left( \frac{x_{\min}+x_{\max}}{2},\; \frac{y_{\min}+y_{\max}}{2} \right)\] <p>여기서 $(x_{\min}, y_{\min})$과 $(x_{\max}, y_{\max})$는 박스의 모서리다. 이미 점으로 주어진 주석은 그대로 둔다.</p> <ul> <li><strong>행동 표기</strong>: UI-TARS (Qin et al., 2025)의 분류를 따라 행동을 함수 호출 형식으로 표준화한다. 본문은 이를 “11개 대표 행동”으로 기술하지만, 실제 Table 5에는 플랫폼별 변형을 포함해 15개 행동이 정리돼 있다. 모든 플랫폼 공통 행동 (tap, swipe, textentry, terminate 등)에 더해, 데스크톱·웹 전용 (right_click, double_click, press_hotkey)과 모바일 전용 (long_press, navigate_home, open_app, navigate_back)이 분리돼 있다. 함수 호출 표기는 출력이 구조적이라 파싱이 쉽고, 최신 LLM의 코딩·도구 사용 능력과 자연스럽게 정렬된다.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab5-action-space-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab5-action-space-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab5-action-space-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab5-action-space.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 5. 통합 행동 공간. 모든 플랫폼 공통 행동과 데스크톱·웹·모바일 전용 행동이 함수 호출 형식으로 정의돼 있다.</figcaption> </figure> <h5 id="sft-데이터-실데이터--합성-데이터">SFT 데이터: 실데이터 + 합성 데이터</h5> <p>SFT 단계의 데이터는 두 축으로 모은다. <strong>공개 데이터셋</strong>은 GroundUI, OS-Atlas, UGround, Aria-UI, Aguvis, WaveUI, ShowUI, Jedi, AgentNet 등 그라운딩·내비게이션 양쪽을 폭넓게 끌어온다. 그 위에 모바일·OS 플랫폼용 <strong>합성 데이터</strong>를 그라운딩·내비게이션 모두에 대해 직접 생성한다. 작은 모델일수록 많은 학습 토큰이 필요하다는 관찰 (Kaplan et al., 2020) 때문에, 규모·범위·다양성 확보가 특히 중요하다고 본다.</p> <h5 id="합성-데이터-생성-파이프라인">합성 데이터 생성 파이프라인</h5> <p>합성 데이터는 네 종류로 나뉜다.</p> <ul> <li><strong>고해상도 그라운딩 데이터</strong>: 여러 GUI 스크린샷을 이어붙여 더 큰 합성 이미지를 만든다 (예: OS-Atlas 기반). 더 빽빽한 레이아웃과 풍부한 공간 맥락에 모델을 노출시켜, 현실의 다중 요소 화면에서 정밀 위치 잡기를 학습시킨다.</li> <li><strong>CoT 내비게이션 데이터</strong>: 세 종류의 추론 트레이스를 만든다 — (i) plan (다음 행동의 간결한 서술), (ii) action think (GUI 요소·히스토리·후보 행동에 대한 추론), (iii) reflect (목표 대비 자기 평가). 각 컴포넌트는 GPT-4o가 set-of-marks (SoM) 시각 프롬프팅으로, 현재 화면의 사람-주석 행동과 에피소드 히스토리를 조건으로 생성한다. 이를 조합해 컴퓨팅 프로파일이 다른 두 에이전트를 만든다: <strong>short-CoT</strong>는 행동 출력 앞에 plan만 붙이고, <strong>long-CoT</strong>는 action think와 reflection까지 확장한다.</li> <li><strong>합성 QA 데이터</strong>: “장바구니에 뭐가 남았지?” 같은 보조 질의에 답하도록, 기존 에피소드의 목표를 자연어 질문으로 바꾸고 마지막 화면에 근거한 답을 단다. 또한 깨끗한 궤적에 일부러 오류 프레임을 섞어 (정상 terminate를 잘못된 swipe로 교체 등) 막힌 상태를 만들고, 그에 대한 교정 시퀀스를 생성해 <strong>재계획 (replanning)</strong> 능력을 학습시킨다.</li> <li><strong>온라인 내비게이션 데이터</strong>: 멀티 에이전트 시스템 (Figure 4)으로 실제 GUI 플랫폼과 상호작용해 대규모 롤아웃을 만든다. 네 컴포넌트로 구성된다 — 난이도를 점증시키는 <strong>커리큘럼 task generator</strong>, 목표를 스텝 단위 지시로 분해하는 <strong>planning agent</strong>, 행동을 실행하는 <strong>grounding agent</strong>, 궤적을 평가해 텍스트 보상을 주는 <strong>critic model</strong>. 이렇게 모은 온라인 궤적은 사람 주석 데이터엔 없는 행동 오류·환경 확률성·재계획 전략을 담으며, CoT 트레이스로 보강한 뒤 VLM-as-a-judge로 저품질 샘플을 걸러낸다.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig4-synthetic-pipeline-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig4-synthetic-pipeline-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig4-synthetic-pipeline-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig4-synthetic-pipeline.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 4. 합성 내비게이션 데이터 생성 파이프라인. 위쪽은 사람-주석 궤적에서 SoM 프롬프팅으로 오프라인 합성 데이터(CoT·QA·고해상도)를 만드는 경로, 아래쪽은 planning·grounding·critic·task generator로 이뤄진 멀티 에이전트 시스템에서 온라인 롤아웃을 모아 VLM 심사로 거르는 경로다.</figcaption> </figure> <h5 id="강화학습-zoom-in-시각-도구-사용">강화학습: zoom-in 시각 도구 사용</h5> <p>SFT는 주석 출력을 그대로 모방하도록 강제하기 때문에, GUI 상호작용의 유연성을 충분히 살리지 못한다. 같은 목표를 여러 형태의 행동으로 달성할 수 있는데도 표면적 라벨 일치만 학습하는 것이다. RLVR은 task 성공 자체를 보상으로 삼아 이 한계를 푼다.</p> <p><strong>그라운딩 RL</strong>은 OS-Atlas 위에서 수행한다. SFT가 주석 중심점을 정확히 재현하도록 강제하는 것과 달리, RL에서는 <strong>예측 위치가 정답 박스 안에 들어가기만 하면</strong> 양의 보상을 주는 포함 기반 (containment) 보상을 쓴다. “정답의 진짜 목표 = 박스 안 어디든”이라는 그라운딩의 본질에 더 맞는 설계다.</p> <p>여기에 <strong>zoom-in 메커니즘</strong>을 더한다. 모델이 초기 예측을 내면, 그 위치 주변을 잘라낸 뒤 그 크롭 영역에서 다시 정밀 예측을 한다. 사람이 작은 글씨를 볼 때 확대하는 행동을 모사한 것으로, 복잡하고 고해상도인 UI에서 특히 유리하다. 작은 모델이 화면 전체에 걸친 미묘한 이해를 동원할 필요 없이 <strong>좁은 영역만 보고 최종 판단</strong>하게 만들어주기 때문이다. 초기 예측과 정밀 예측을 모두 학습 풀에 남겨 다중 스케일 그라운딩 감독을 제공한다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig5-zoom-in-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig5-zoom-in-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig5-zoom-in-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig5-zoom-in.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 5. zoom-in 연산. 'Refresh the file explorer' 지시에 대해 Step 1에서 초기 위치를 예측하고, 그 주변을 크롭한 Step 2에서 다시 예측해 정밀도를 높인다.</figcaption> </figure> <p><strong>내비게이션 RL</strong>은 모바일·데스크톱 합성 데이터와 AgentNet을 쓴다. 현재 스크린샷, 고수준 지시, 과거 행동 히스토리를 입력으로 모델이 $M$개의 후보 출력 $z = [z_1, \ldots, z_M]$를 샘플링한다. 각 $z_i = [c_i; a_i]$는 chain-of-thought 텍스트 $c_i$와 그 뒤의 예측 행동 $a_i = [\tau_i; \theta_i]$로 이뤄지고, $\tau_i$는 행동 타입, $\theta_i$는 파라미터 (예: 탭 위치)다. 보상은 생성 행동을 정답 $a^{\text{gt}} = [\tau^{\text{gt}}; \theta^{\text{gt}}]$과 비교해 계산한다.</p> <h4 id="학습-목표--손실-함수">학습 목표 / 손실 함수</h4> <p>내비게이션 보상은 두 성분의 합이다. 하나는 행동 타입이 맞는지, 다른 하나는 파라미터가 정확한지를 본다.</p> \[r_i = f_{\text{type}}(\tau_i, \tau^{\text{gt}}, \theta^{\text{gt}}) + f_{\text{param}}(\theta_i, \theta^{\text{gt}})\] <p><strong>행동 타입 보상</strong> $f_{\text{type}}$는 타입 일치를 검사하되, 파라미터가 필요 없는 행동 (예: navigate_home처럼 $\theta^{\text{gt}} = \emptyset$)은 더 강하게 보상한다. 파라미터가 필요한 행동은 타입만 맞으면 부분 점수를 준다.</p> \[f_{\text{type}}(\tau_i, \tau^{\text{gt}}, \theta^{\text{gt}}) = \begin{cases} 2, &amp; \text{if } \tau_i = \tau^{\text{gt}} \text{ and } \theta^{\text{gt}} = \emptyset, \\ 1, &amp; \text{if } \tau_i = \tau^{\text{gt}} \text{ and } \theta^{\text{gt}} \neq \emptyset, \\ 0, &amp; \text{otherwise.} \end{cases}\] <p><strong>파라미터 보상</strong> $f_{\text{param}}$은 예측 파라미터의 충실도를 본다. 텍스트 입력·방향 같은 문자열 파라미터는 정확히 일치할 때 1, 아니면 0인 exact-match를 쓴다. tap처럼 위치 기반 행동은 두 가지 보상을 실험한다. 하나는 그라운딩 보상과 똑같이 예측 좌표가 정답 박스 안에 들면 1, 아니면 0인 <strong>희소 보상 (sparse reward)</strong>이고, 다른 하나는 정답 중심과의 정규화 거리로 점진적 점수를 주는 <strong>밀집 보상 (dense reward)</strong>이다.</p> \[f_{\text{param}}^{\text{dense}}(\theta_i, \theta^{\text{gt}}) = \max\!\left( 1 - \lambda \left( \frac{|x_i - x^{\text{gt}}|}{w} + \frac{|y_i - y^{\text{gt}}|}{h} \right),\; 0 \right)\] <p>여기서 $(x_i, y_i)$와 $(x^{\text{gt}}, y^{\text{gt}})$는 예측·정답 중심, $w$와 $h$는 정답 요소의 너비·높이, 감쇠 인자 $\lambda$는 민감도 조절용으로 0.5로 둔다. 즉 정답에 가까울수록 1에 가까운 연속 보상을 받고, 박스 폭만큼 벗어나면 0으로 떨어진다.</p> <p><strong>최적화 (GRPO).</strong> 그라운딩·내비게이션 RL 모두 GRPO로 최적화한다. 학습 예제마다 여러 예측을 샘플링하는데, 그라운딩은 <strong>원본 이미지 8개 + zoom-in 크롭 4개</strong>, 내비게이션은 <strong>32개</strong> 후보를 뽑는다. 각 샘플이 보상 $r_i$를 받고, 그룹 안에서 정규화한 어드밴티지로 정책을 업데이트한다.</p> \[\begin{aligned} A_i &amp;= \frac{r_i - \text{mean}(r)}{\text{std}(r)}, \\ r &amp;= [r_1, r_2, \ldots, r_M] \end{aligned}\] <p>학습 효율을 위해 <strong>온라인 필터링 (online filtering)</strong>을 적용한다. 샘플된 보상이 모두 같은 (전부 0 또는 전부 1) 프롬프트는 학습 신호가 없으므로 버린다. 가장 유익한, 즉 결정 경계를 다듬는 예제에 집중하기 위함이다.</p> <h4 id="학습-데이터와-파이프라인">학습 데이터와 파이프라인</h4> <table> <thead> <tr> <th>항목</th> <th>내용</th> </tr> </thead> <tbody> <tr> <td>백본</td> <td>내부 3B dense 멀티모달 LLM (텍스트 + 비전-언어 이해 데이터로 사전학습)</td> </tr> <tr> <td>이미지 인코더</td> <td>VitDet + AnyRes (스크린샷을 셀 격자로 동적 분할)</td> </tr> <tr> <td>SFT 데이터</td> <td>공개 그라운딩·내비게이션 데이터셋 + 모바일·데스크톱 합성 데이터 (고해상도 그라운딩, short/long-CoT, QA, 온라인 멀티 에이전트 롤아웃)</td> </tr> <tr> <td>RL 데이터</td> <td>그라운딩: OS-Atlas / 내비게이션: 모바일·데스크톱 합성 + AgentNet</td> </tr> <tr> <td>학습 스텝</td> <td>SFT 10K 스텝, RL 1,500 스텝</td> </tr> <tr> <td>RL 알고리즘</td> <td>GRPO + 온라인 필터링 (그라운딩 8+4 샘플, 내비게이션 32 샘플)</td> </tr> <tr> <td>보상</td> <td>그라운딩: 박스 포함 / 내비게이션: 행동 타입($f_{\text{type}}$) + 파라미터($f_{\text{param}}$, sparse 또는 dense, $\lambda=0.5$)</td> </tr> </tbody> </table> <h4 id="실험-결과">실험 결과</h4> <p>내부 3B dense 모델을 백본으로, SFT 10K 스텝 + RL 1,500 스텝으로 학습한다. 그라운딩과 내비게이션을 차례로 평가한다.</p> <h5 id="gui-그라운딩">GUI 그라운딩</h5> <p>ScreenSpot-V2, ScreenSpot-Pro, OSWorld-G 세 벤치마크로 평가한다. 모바일·데스크톱·웹과 다양한 해상도를 아우르며, 특히 ScreenSpot-Pro는 고해상도 데스크톱 화면이라 어렵다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab1-grounding-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab1-grounding-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab1-grounding-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab1-grounding.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 1. 그라운딩 성능. Ferret-UI Lite-3B는 ScreenSpot-V2 91.6, ScreenSpot-Pro 53.3, OSWorld-G 55.3으로 3B 그룹을 크게 앞서고 7B급과 어깨를 나란히 한다.</figcaption> </figure> <ul> <li><strong>ScreenSpot-V2 91.6</strong>: 다른 3B 베이스라인 (UI-R1-3B 89.2, Jedi-3B 88.8)을 앞서고, 90.3~92.8인 7B급에 바짝 붙는다.</li> <li><strong>ScreenSpot-Pro 53.3</strong>: 다른 3B 모델이 대개 30대 중반인데 비해 압도적으로 높고 (Jedi-3B 37.1, GUI-G1-3B 38.1), GUI-Owl-7B (54.9) 바로 아래다.</li> <li><strong>OSWorld-G 55.3</strong>: 3B 중 최고이며 (Jedi-3B 50.9), GUI-Owl-7B (55.9)·GUI-Owl-32B (58.0)과 경쟁한다. 7B 중 최강인 GTA1-7B (67.7)와는 격차가 있지만 파라미터 규모를 감안하면 작은 차이다.</li> </ul> <p>여기서 한 가지 짚을 점. <strong>논문 초록은 OSWorld-G를 61.2로 적지만, Table 1·Figure 6a·Table 8은 모두 일관되게 55.3을 가리킨다.</strong> 세 표가 일치하므로 본 리뷰는 검증된 값인 55.3을 따른다 (초록의 61.2는 오기로 보인다). 작은 차이가 아니라 6점 차이라, 초록만 보고 인용하면 오류가 전파되기 쉬운 지점이다.</p> <h5 id="gui-내비게이션">GUI 내비게이션</h5> <p><strong>오프라인 (Android Control).</strong> 저수준 (단일 스텝 지시)과 고수준 (전역 목표) 태스크로 나뉜다. Ferret-UI Lite-3B는 저수준 86.6%, 고수준 68.9%로, 비슷한 규모 모델 (OS-Atlas-4B 80.6/67.5, InternVL-2-4B 80.1/66.7)은 물론 7B·72B 일부 (Qwen2-VL-7B 82.6/69.7, Aguvis-72B 84.4/66.4)와도 견줄 만하다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab3-navigation-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab3-navigation-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab3-navigation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab3-navigation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 3. 온라인 내비게이션. (a) AndroidWorld에서 3B 모델 중 최고(28.0%), 7B급과 경쟁. (b) OSWorld-Verified(15스텝)에서 모든 3B를 앞서지만(17.3%) 큰 모델·상용 모델과는 격차.</figcaption> </figure> <ul> <li><strong>AndroidWorld 28.0%</strong>: 116개 태스크·20개 앱으로 구성된 동적 환경에서 5회 평균. 3B 중 최고 (ScaleCUA-3B 23.7)이고, UI-TARS-1.5-7B 같은 7B급과도 경쟁한다.</li> <li><strong>OSWorld-Verified 17.3%</strong> (최대 15스텝): 모든 3B 모델 (ScaleCUA-3B 9.6, OpenCUA-A3B 16.9)을 앞서고 7B와도 경쟁한다. 최대 스텝을 50으로 늘리면 <strong>19.8%</strong>로 올라 test-time scaling 여지를 보인다.</li> </ul> <p>다만 논문도 분명히 인정한다. 내비게이션 성능은 모델 규모에 제약돼, OSWorld 리더보드의 SOTA (예: Claude-4-Sonnet 43.9%)에는 못 미친다. 참고로 같은 15스텝 자체 평가에서 Claude-4-Sonnet은 31.2%, Doubao-1.5-Thinking은 31.9%를 기록했다.</p> <h4 id="결과-분석--ablation">결과 분석 / Ablation</h4> <p>이 논문의 진짜 가치는 단일 수치가 아니라 ablation에서 나온다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig6-grounding-ablation-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig6-grounding-ablation-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig6-grounding-ablation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig6-grounding-ablation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 6. 그라운딩 ablation. (a) RL이 SFT 대비 일관되게 향상되고 zoom-in이 추가 이득을 준다. (b) 그라운딩:내비게이션 50:50 균형이 최선. (c) 고해상도 합성 데이터가 특히 ScreenSpot-Pro에서 효과적.</figcaption> </figure> <ul> <li><strong>RL과 zoom-in의 효과 (Figure 6a)</strong>: SFT만 했을 때 대비 RL이 일관되게 올린다. ScreenSpot-Pro 52.3 → 52.7 (RL, zoom-in 없음) → 53.3 (RL + zoom-in), OSWorld-G 54.1 → 54.1 → 55.3. zoom-in이 마지막 한 끗을 더해주는데, 이는 모델이 RL 최적화뿐 아니라 작고 빽빽한 요소를 다룰 때 능동적으로 확대를 활용하도록 학습했음을 뜻한다.</li> <li><strong>데이터 혼합 비율 (Figure 6b)</strong>: 그라운딩:내비게이션 비율을 0:100, 30:70, 50:50, 70:30으로 바꿔본다. 두 데이터는 서로를 보완한다. 그라운딩 데이터가 전혀 없는 0:100에서도 ScreenSpot-Pro가 41.2를 찍는 건 내비게이션 데이터에 그라운딩 신호가 섞여 있다는 뜻이고, 반대로 그라운딩 데이터를 더해도 내비게이션은 나빠지지 않는다. 특히 OSWorld-Chrome은 그라운딩이 0%인 0:100에서 0.0으로 무너졌다가 50:50에서 22.7로 회복하는데, 이는 내비게이션조차 그라운딩 능력을 전제로 함을 보여준다. <strong>50:50 균형이 전반적으로 최선</strong>이다 (ScreenSpot-Pro 50.1, 그라운딩-편중 70:30의 49.5보다 높다).</li> <li><strong>고해상도 합성 데이터 (Figure 6c)</strong>: 고해상도 합성 데이터를 넣으면 ScreenSpot-V2는 소폭 (89.5 → 90.0), 어려운 ScreenSpot-Pro에서 더 뚜렷이 (48.3 → 48.9) 오른다. 정밀 위치 잡기에 고해상도 데이터가 특히 유효하다는 신호다.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig7-rl-ablation-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig7-rl-ablation-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig7-rl-ablation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig7-rl-ablation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 7. AndroidWorld RL ablation. (a) SFT 스텝이 적을수록 RL의 상대 이득이 크다. (b) 행동 타입(AT)만으로는 부족하고, AT + 밀집 그라운딩(DG)이 최선이다.</figcaption> </figure> <p>내비게이션 쪽 ablation은 작은 모델 학습의 핵심 교훈을 담는다.</p> <ul> <li><strong>CoT·합성 데이터의 효과 (Table 4)</strong>: AndroidWorld에서 baseline 13.7 → short-CoT 15.8 → long-CoT 19.6으로, 추론 트레이스가 길수록 멀티스텝 성능이 오른다. long-CoT 위에 온라인·오프라인 합성 데이터를 5K → 13K → 17K로 늘리면 20.3 → 22.4 → 25.2로 추가 상승한다 (약 +6점). 다양성을 갖춘 합성 데이터 스케일업이 통한다는 증거다.</li> </ul> <table> <thead> <tr> <th>Model Variants</th> <th>AndroidWorld 성공률 (%)</th> </tr> </thead> <tbody> <tr> <td>Baseline (CoT·합성 데이터 없음)</td> <td>13.7</td> </tr> <tr> <td>+ Short CoT</td> <td>15.8</td> </tr> <tr> <td>+ Long CoT</td> <td>19.6</td> </tr> <tr> <td>+ Syn. data (5K)</td> <td>20.3</td> </tr> <tr> <td>+ Syn. data (13K)</td> <td>22.4</td> </tr> <tr> <td>+ Syn. data (17K)</td> <td>25.2</td> </tr> </tbody> </table> <ul> <li><strong>RL은 SFT가 적을 때 더 빛난다 (Figure 7a)</strong>: SFT 2K·6K·10K 스텝 각각에 RLVR을 붙이면 모든 설정에서 향상되는데, <strong>SFT 스텝이 적을수록 이득이 크다</strong>. RLVR이 부족한 SFT를 보완하는 역할을 한다는 뜻이다.</li> <li><strong>보상 설계가 전부 (Figure 7b)</strong>: 네 가지 보상 구성을 비교한다 — 행동 타입만 (AT), 희소 그라운딩만 (SG), AT + SG, AT + 밀집 그라운딩 (DG). <strong>행동 타입 보상만 쓰면 SFT-only보다도 떨어진다</strong> (정답 위치가 강화되지 않으므로). 그라운딩 보상만으로는 탭 정확도는 오르지만 SFT 베이스라인을 못 넘는다. 둘을 합쳐야 일관된 향상이 나오고, <strong>밀집 그라운딩이 희소 그라운딩보다 낫다</strong>. 작은 GUI 에이전트에서 RLVR 보상 구조를 세심히 설계하는 일이 얼마나 중요한지를 정면으로 보여주는 결과다.</li> </ul> <p>추가로, 그라운딩 데이터셋 ablation (Table 6)에서는 OS-Atlas를 빼면 ScreenSpot-Pro가 가장 크게 떨어져 (−6.80), 이 데이터셋이 고해상도 데스크톱 그라운딩에 가장 결정적임을 확인한다.</p> <h4 id="한계와-비판적-평가">한계와 비판적 평가</h4> <ul> <li><strong>내비게이션의 본질적 천장.</strong> 저자도 인정하듯, 멀티스텝 내비게이션은 모델 규모에 강하게 묶인다. OSWorld 19.8%는 3B 중에선 좋지만 상용 SOTA (40%대)와는 격차가 크다. 작은 모델로 장기 계획·자기 반성을 충분히 담기 어렵다는 점은 이 방향의 근본 난제로 남는다.</li> <li><strong>RL 보상 민감성 = 재현성 부담.</strong> “보상 설계가 성능을 좌우한다”는 발견은 양날의 검이다. 밀집 보상의 $\lambda$, 샘플 수 (8+4 / 32), 온라인 필터링 임계 등 하이퍼파라미터에 민감하다는 뜻이고, 코드·가중치가 공개되지 않아 외부 재현이 어렵다.</li> <li><strong>OSWorld-G 수치의 내부 불일치.</strong> 초록 (61.2)과 본문 표 (55.3)가 6점이나 어긋난다. 검증 가능한 정량 보고를 강조하는 논문에서 핵심 수치가 어긋난 것은 신뢰도에 흠집을 낸다.</li> <li><strong>지연 시간·메모리의 부재.</strong> “온디바이스”를 표방하지만 실제 기기에서의 추론 지연, 메모리 사용량, 배터리 영향 같은 배포 지표가 보고되지 않는다. zoom-in은 한 입력을 두 번 보는 셈이라 추론 비용을 늘리는데, 그 비용-정확도 트레이드오프가 정량화되지 않았다.</li> <li><strong>합성 데이터의 GPT-4o 의존.</strong> CoT·QA 데이터가 GPT-4o의 SoM 프롬프팅으로 생성된다. 큰 모델의 추론을 작은 모델로 증류하는 구조라, 교사 모델 품질에 상한이 묶이고 라이선스·재현 측면의 제약도 따른다.</li> </ul> <h4 id="시사점--takeaways">시사점 / Takeaways</h4> <ul> <li><strong>작은 GUI 에이전트는 “데이터 + 추론 시점 도구 + 보상 설계”의 합으로 만들어진다.</strong> 단일 묘책이 아니라, 통합 행동 공간·합성 데이터·zoom-in·RLVR이 각자 몇 점씩 더해 3B를 7B급 그라운딩까지 끌어올린다.</li> <li><strong>그라운딩과 내비게이션은 한 모델에서 서로를 돕는다.</strong> 50:50 혼합이 최선이고, 그라운딩 능력은 내비게이션의 전제 조건이다 (그라운딩 0% → OSWorld-Chrome 0%). 두 능력을 분리해 학습할 이유가 없다.</li> <li><strong>RL 보상은 “무엇을 강화하는가”가 전부다.</strong> 작은 모델에서 행동 타입만 보상하면 오히려 퇴보하고, 밀집 그라운딩 보상을 더해야 안정적으로 오른다. 보상의 형태가 모델 크기보다 결과를 좌우하는 구간이 분명히 존재한다.</li> <li><strong>그라운딩은 거의 풀렸고, 내비게이션은 열려 있다.</strong> 3B가 ScreenSpot-V2 91.6을 찍는 시대에 단일 화면 그라운딩의 한계 효용은 작아졌다. 다음 전선은 멀티스텝 계획·오류 복구·장기 안정성이며, 여기서 작은 모델은 아직 갈 길이 멀다.</li> <li><strong>“교훈”을 공유하는 논문의 가치.</strong> SOTA 한 줄보다, 무엇이 통하고 안 통했는지를 ablation으로 남긴 점이 오래 간다. 온디바이스 에이전트를 만들려는 후속 연구에 그대로 출발점이 된다.</li> </ul> <h4 id="참고-자료">참고 자료</h4> <ul> <li>논문: <a href="https://arxiv.org/abs/2509.26539">Ferret-UI Lite: Lessons from Building Small On-Device GUI Agents (arXiv:2509.26539)</a></li> <li>코드/가중치: 공개되지 않음</li> <li>계보: <a href="https://arxiv.org/abs/2404.05719">Ferret-UI (arXiv:2404.05719)</a>, Ferret-UI 2 등 Apple GUI 이해 연구</li> </ul> <h4 id="더-읽어보기">더 읽어보기</h4> <ul> <li><strong><a href="https://arxiv.org/abs/2404.05719">Ferret-UI: Grounded Mobile UI Understanding with Multimodal LLMs</a></strong> (You et al., 2024) — Ferret-UI Lite의 직계 조상. 모바일 UI 화면을 referring·grounding·reasoning으로 이해하는 첫 MLLM이다.</li> <li><strong><a href="https://arxiv.org/abs/2501.12326">UI-TARS: Pioneering Automated GUI Interaction with Native Agents</a></strong> (Qin et al., 2025) — Ferret-UI Lite가 행동 분류 (action taxonomy)를 차용하고 여러 벤치마크에서 비교 대상으로 삼는 핵심 baseline.</li> <li><strong><a href="https://arxiv.org/abs/2410.23218">OS-ATLAS: A Foundation Action Model for Generalist GUI Agents</a></strong> (Wu et al., ICLR 2025) — RL 그라운딩의 학습 데이터이자 Android Control 평가 세팅의 출처. 그라운딩 데이터셋 ablation에서 가장 중요한 것으로 꼽힌다.</li> <li><strong><a href="https://arxiv.org/abs/2404.07972">OSWorld: Benchmarking Multimodal Agents for Open-Ended Tasks in Real Computer Environments</a></strong> (Xie et al., NeurIPS 2024) — 실제 OS 환경에서 컴퓨터 사용 에이전트를 평가하는 벤치마크. Ferret-UI Lite의 내비게이션 한계가 가장 선명히 드러나는 무대다.</li> <li><strong><a href="https://arxiv.org/abs/2402.03300">DeepSeekMath: Pushing the Limits of Mathematical Reasoning in Open Language Models</a></strong> (Shao et al., 2024) — Ferret-UI Lite의 RLVR이 쓰는 최적화 알고리즘 GRPO를 제안한 논문.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="gui-agent"/><category term="on-device"/><category term="multimodal-llm"/><category term="reinforcement-learning"/><category term="grounding"/><category term="vision-language-action"/><summary type="html"><![CDATA[Apple이 만든 3B 온디바이스 GUI 에이전트. 실+합성 데이터 큐레이션, zoom-in 시각 도구 사용, SFT→RLVR 2단계 학습으로 작은 모델을 어디까지 끌어올릴 수 있는지, 그리고 어디서 막히는지를 정직하게 기록한 '교훈 보고서'를 깊게 읽는다.]]></summary></entry><entry xml:lang="en"><title type="html">[Paper Review] Ferret-UI Lite: Lessons from Building Small On-Device GUI Agents</title><link href="https://baba9811.github.io/en/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/" rel="alternate" type="text/html" title="[Paper Review] Ferret-UI Lite: Lessons from Building Small On-Device GUI Agents"/><published>2026-05-29T06:30:00+00:00</published><updated>2026-05-29T06:30:00+00:00</updated><id>https://baba9811.github.io/en/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age</id><content type="html" xml:base="https://baba9811.github.io/en/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/"><![CDATA[<div class="lang-toggle" style="margin-bottom: 1.5rem;"> <a href="/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/" class="btn btn-sm btn-outline-secondary">🇰🇷 한국어로 읽기</a> </div> <h4 id="metadata">Metadata</h4> <table> <thead> <tr> <th>Field</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Authors</td> <td>Zhen Yang et al. (16 co-authors, Apple)</td> </tr> <tr> <td>Venue</td> <td>arXiv preprint · 2025</td> </tr> <tr> <td>arXiv</td> <td><a href="https://arxiv.org/abs/2509.26539">2509.26539</a></td> </tr> <tr> <td>Data</td> <td>Public GUI grounding/navigation datasets (OS-Atlas, UGround, Aria-UI, Aguvis, Jedi, ShowUI, etc.) + mobile/desktop synthetic data</td> </tr> <tr> <td><span style="white-space: nowrap">Review date</span></td> <td>2026-05-29</td> </tr> </tbody> </table> <h4 id="tldr">TL;DR</h4> <ul> <li>Apple introduces <strong>Ferret-UI Lite, a 3B end-to-end on-device GUI agent</strong> that handles mobile, web, and desktop in a single model. On grounding it scores 91.6 on ScreenSpot-V2, 53.3 on ScreenSpot-Pro, and 55.3 on OSWorld-G — well ahead of comparable 3B models and closing in on the 7B tier. On navigation it reaches 28.0% on AndroidWorld and 19.8% on OSWorld: strong for its size, but still short of larger models.</li> <li>The recipe has three threads: (1) curate real and synthetic data at scale under a <strong>unified action space</strong>, (2) use <strong>zoom-in visual tool-use</strong> at inference time to read high-resolution screens precisely, and (3) train in two stages, <strong>SFT → RLVR</strong>, with carefully designed verifiable rewards.</li> <li>True to its title, this is a “lessons” report. Ablations show that <strong>small models are highly sensitive to RL reward design</strong>, that grounding and navigation data mutually reinforce each other, and that synthetic high-resolution data is especially effective for precise grounding. Code and weights are not released.</li> </ul> <h4 id="introduction">Introduction</h4> <p>A GUI (graphical user interface) agent takes over the screen manipulation a human would do with a mouse and keyboard: it looks at the screen and decides, on its own, “click this button, type into that field, move to the next screen,” then executes. Think of jotting a reminder hands-free while driving, or pulling up a recipe with wet hands. These assistive scenarios demand <strong>low latency, strong privacy guarantees, and robustness under limited connectivity</strong> — all three of which conflict with the “ship the screen to a big server model” approach. Hence the need for small, on-device GUI agents.</p> <p>The catch is that the field’s center of gravity sits at the opposite end. Most methods lean on giant foundation models. Multi-agent systems that separate perception, planning, and action — built on top of large LLMs like GPT or Gemini — achieve impressive navigation, but at the cost of modeling complexity, compute budget, and inference time. End-to-end GUI agents that map raw screenshots directly to actions are an appealing alternative, yet here too larger models are preferred, because a single model has to absorb a heterogeneous mix of low-level grounding, screen understanding, multi-step planning, and self-reflection.</p> <p>Ferret-UI Lite takes aim squarely at this imbalance. The real point of the paper is to ask, at the 3B scale, “can we build a strong GUI agent from a small model — and if so, which techniques work, and where does it break?” — then report the lessons honestly. It continues Apple’s Ferret-UI lineage (Ferret-UI, Ferret-UI 2), but this time the focus shifts from “understanding” toward “acting as an agent” and “on-device efficiency.”</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig1-capability-vs-size-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig1-capability-vs-size-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig1-capability-vs-size-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig1-capability-vs-size.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1. Performance vs. model size. (a) On grounding, Ferret-UI Lite (red star, 3B) surpasses same-size models and even several larger ones. (b) On navigation it tops the 3B group but trails the 72B tier by a clear margin — a sign that multi-step reasoning is inherently hard for small models.</figcaption> </figure> <h4 id="key-contributions">Key Contributions</h4> <ul> <li><strong>A 3B on-device end-to-end GUI agent</strong> that handles mobile, web, and desktop in one model and learns grounding and navigation jointly. It sets a substantially higher grounding bar within the 3B tier.</li> <li><strong>Large-scale curation of real + synthetic data under a unified action space</strong>: public datasets are unified into point-based coordinates and a function-call action format, and gaps are filled with synthetic data. A multi-agent system that generates online rollouts is the centerpiece.</li> <li><strong>Inference-time zoom-in visual tool-use</strong>: a “thinking with images” mechanism that crops around the model’s initial prediction and re-predicts, letting a small model make a final decision over a narrow region of a large, cluttered screen.</li> <li><strong>Two-stage training with verifiable rewards (SFT → RLVR)</strong>: a containment reward for grounding, and an action-type + parameter reward for navigation. Ablations show that, for small models, this reward design is decisive.</li> <li><strong>A consolidated set of scale-down lessons</strong>: rich ablations on data-mixture ratios, high-resolution data, CoT length, and RL reward composition map out what does and does not work for small models. This is the contribution that will outlast any single SOTA number.</li> </ul> <h4 id="background">Background</h4> <p><strong>Grounding vs. navigation.</strong> GUI tasks split into two kinds. <strong>Grounding</strong> is a single-step problem: given an instruction like “click the Refresh button,” predict the screen coordinate to click. <strong>Navigation</strong> spans many steps — “send this poster to my mom by email” — changing the screen as it goes, planning over an accumulated history of screens and actions. Grounding lives or dies on precise location; navigation on long-horizon planning. They are usually handled separately; Ferret-UI Lite trains both in one model.</p> <p><strong>End-to-end vs. multi-agent.</strong> Multi-agent approaches separate a planner from a grounding module, using a large model (GPT, Gemini) as the planner and a specialized module to ground coordinates — powerful but heavy. End-to-end approaches, like OS-Atlas, UI-TARS, and ShowUI, predict action sequences directly from multimodal inputs with a single vision-language-action model; Ferret-UI Lite belongs here. Notably, it <strong>uses a multi-agent system to generate training data but deploys a single end-to-end model</strong> — distilling the richness of a multi-agent pipeline into a small unified model.</p> <p><strong>RLVR and GRPO.</strong> Reinforcement Learning with Verifiable Rewards (RLVR) replaces human labels with rule-based, automatically computable rewards. GUI grounding is a natural fit, since “did the predicted coordinate land inside the ground-truth box?” can be checked automatically. Optimization uses Group Relative Policy Optimization (GRPO), introduced in DeepSeekMath: sample multiple candidates per input and update the policy with an advantage normalized by the group’s reward mean and standard deviation — stable without a value network.</p> <p><strong>Thinking with images.</strong> A recent line of visual-reasoning work teaches models to actively crop and zoom into regions of interest rather than look once and commit. Ferret-UI Lite’s zoom-in applies this idea to grounding.</p> <h4 id="method--architecture">Method / Architecture</h4> <h5 id="backbone-and-io">Backbone and I/O</h5> <p>Ferret-UI Lite starts from an <strong>internal 3B dense model</strong> pretrained on a mixture of text-only and vision-language understanding data. The image encoder uses the VitDet plain-ViT detection backbone with the <strong>AnyRes</strong> strategy, which dynamically partitions each input screenshot into a grid of cells — a choice that lets it process high-resolution UI screenshots without a token explosion. The resulting visual tokens feed a <strong>decoder-only LLM</strong>, and given a GUI screen plus a user instruction the model emits chain-of-thought traces (<code class="language-plaintext highlighter-rouge">&lt;think&gt;...</code>) and a low-level action policy (<code class="language-plaintext highlighter-rouge">&lt;function&gt;...</code>) in sequence.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig3-architecture-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig3-architecture-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig3-architecture-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig3-architecture.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 3. Model architecture and training recipes. The user instruction and screenshot pass through the image encoder into a decoder-only LLM, which generates think traces and function calls (actions). Training proceeds in two stages: SFT → RLVR.</figcaption> </figure> <h5 id="unified-action-space">Unified Action Space</h5> <p>To train one model on heterogeneous public datasets, the action representation has to be unified first. Ferret-UI Lite unifies it along two axes.</p> <ul> <li><strong>Grounding coordinates</strong>: some datasets give bounding boxes, others single points. Every box is mapped to its geometric center, normalizing everything to a point-based representation.</li> </ul> \[(x_{\text{center}}, y_{\text{center}}) = \left( \frac{x_{\min}+x_{\max}}{2},\; \frac{y_{\min}+y_{\max}}{2} \right)\] <p>where $(x_{\min}, y_{\min})$ and $(x_{\max}, y_{\max})$ are the box corners. Point annotations are left unchanged.</p> <ul> <li><strong>Action format</strong>: following the taxonomy of UI-TARS (Qin et al., 2025), actions are standardized into a function-call format. The text calls this “eleven representative actions,” though Table 5 actually enumerates fifteen once platform-specific variants are counted. On top of cross-platform actions (tap, swipe, textentry, terminate, etc.) sit desktop/web-only ones (right_click, double_click, press_hotkey) and mobile-only ones (long_press, navigate_home, open_app, navigate_back). The function-call form yields structured outputs that are easy to parse and align naturally with the coding and tool-use abilities of modern LLMs.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab5-action-space-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab5-action-space-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab5-action-space-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab5-action-space.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 5. The unified action space. Cross-platform actions plus desktop/web- and mobile-specific actions, all defined as function calls.</figcaption> </figure> <h5 id="sft-data-real--synthetic">SFT Data: Real + Synthetic</h5> <p>SFT data is gathered along two axes. The <strong>public datasets</strong> span both grounding and navigation: GroundUI, OS-Atlas, UGround, Aria-UI, Aguvis, WaveUI, ShowUI, Jedi, AgentNet. On top of those, the authors generate <strong>synthetic data</strong> for mobile and OS platforms, for both grounding and navigation. Because smaller models need many training tokens (Kaplan et al., 2020), scale, coverage, and diversity matter especially here.</p> <h5 id="synthetic-data-generation">Synthetic Data Generation</h5> <p>The synthetic data comes in four kinds.</p> <ul> <li><strong>High-resolution grounding data</strong>: multiple GUI screenshots are concatenated into larger composite images (e.g., from OS-Atlas). This exposes the model to denser layouts and richer spatial context, teaching precise localization in realistic multi-element screens.</li> <li><strong>CoT navigation data</strong>: three reasoning traces are produced — (i) plan (a concise description of the next action), (ii) action think (reasoning over GUI elements, history, and candidate actions), and (iii) reflect (self-assessment relative to the goal). Each component is generated by GPT-4o via set-of-marks (SoM) visual prompting, conditioned on the human-annotated action for the current screen and the episode history. Two agents with different compute profiles result: <strong>short-CoT</strong> adds only a plan trace before the action, and <strong>long-CoT</strong> extends to action-think and reflection.</li> <li><strong>Synthetic QA data</strong>: to support assistive queries (“what’s left in my cart?”), episode goals are rewritten into natural-language questions with answers grounded in the final screen. The authors also inject error-prone frames into clean trajectories (e.g., replacing a correct terminate with an erroneous swipe) to simulate a stuck state, then generate the correction sequence — teaching <strong>replanning</strong>.</li> <li><strong>Online navigation data</strong>: a multi-agent system (Figure 4) interacts with real GUI platforms to produce rollouts at scale, with four components — a curriculum <strong>task generator</strong> of increasing difficulty, a <strong>planning agent</strong> that decomposes goals into step-level instructions, a <strong>grounding agent</strong> that executes actions, and a <strong>critic model</strong> that scores trajectories with textual rewards. These online trajectories carry the action errors, environmental stochasticity, and replanning strategies absent from human-annotated data; they are enriched with CoT traces and filtered by a VLM-as-a-judge.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig4-synthetic-pipeline-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig4-synthetic-pipeline-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig4-synthetic-pipeline-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig4-synthetic-pipeline.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 4. Synthetic navigation data pipeline. The top path builds offline synthetic data (CoT, QA, high-res) from human-annotated trajectories via SoM prompting; the bottom path collects online rollouts from a multi-agent system (planning, grounding, critic, task generator) filtered by a VLM judge.</figcaption> </figure> <h5 id="reinforcement-learning-zoom-in-visual-tool-use">Reinforcement Learning: Zoom-in Visual Tool-Use</h5> <p>SFT forces strict imitation of annotated outputs and so under-uses the flexibility of GUI tasks: the same goal can be reached by differently-shaped actions, yet SFT only learns surface-level label matching. RLVR addresses this by rewarding task success itself.</p> <p><strong>Grounding RL</strong> runs on OS-Atlas. Unlike SFT, which forces the model to reproduce the annotated center, RL grants positive reward <strong>whenever the predicted location falls inside the ground-truth box</strong> — a containment reward that better matches what grounding actually wants (“anywhere inside the box is correct”).</p> <p>On top of this comes the <strong>zoom-in mechanism</strong>. After an initial prediction, the image is cropped around that location and the model re-predicts on the crop. It mimics how a person zooms in to read fine print, and helps especially on complex, high-resolution UIs: it lets a small model make its final decision over a <strong>narrow region</strong> rather than mobilizing subtle understanding across the whole screen. Both the initial and refined predictions are kept in the training pool, providing multi-scale grounding supervision.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig5-zoom-in-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig5-zoom-in-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig5-zoom-in-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig5-zoom-in.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 5. The zoom-in operation. For 'Refresh the file explorer,' Step 1 predicts an initial location, and Step 2 re-predicts on a crop around it for higher precision.</figcaption> </figure> <p><strong>Navigation RL</strong> uses the mobile/desktop synthetic data plus AgentNet. Given the current screenshot, the high-level instruction, and the history of past actions, the model samples $M$ candidate outputs $z = [z_1, \ldots, z_M]$. Each $z_i = [c_i; a_i]$ is a chain-of-thought text $c_i$ followed by a predicted action $a_i = [\tau_i; \theta_i]$, with action type $\tau_i$ and parameters $\theta_i$ (e.g., a tap location). The reward compares the generated action against the ground truth $a^{\text{gt}} = [\tau^{\text{gt}}; \theta^{\text{gt}}]$.</p> <h4 id="training-objective--loss">Training Objective / Loss</h4> <p>The navigation reward is a sum of two components — one checks whether the action type is right, the other whether the parameters are precise.</p> \[r_i = f_{\text{type}}(\tau_i, \tau^{\text{gt}}, \theta^{\text{gt}}) + f_{\text{param}}(\theta_i, \theta^{\text{gt}})\] <p>The <strong>action-type reward</strong> $f_{\text{type}}$ checks type match but rewards parameter-free actions (e.g., navigate_home, where $\theta^{\text{gt}} = \emptyset$) more strongly; actions that require parameters get partial credit for matching the type.</p> \[f_{\text{type}}(\tau_i, \tau^{\text{gt}}, \theta^{\text{gt}}) = \begin{cases} 2, &amp; \text{if } \tau_i = \tau^{\text{gt}} \text{ and } \theta^{\text{gt}} = \emptyset, \\ 1, &amp; \text{if } \tau_i = \tau^{\text{gt}} \text{ and } \theta^{\text{gt}} \neq \emptyset, \\ 0, &amp; \text{otherwise.} \end{cases}\] <p>The <strong>parameter reward</strong> $f_{\text{param}}$ evaluates parameter fidelity. For string parameters (text entry, direction) it uses exact match — 1 if matched, 0 otherwise. For location-based actions like tap, the authors experiment with two rewards: a <strong>sparse reward</strong> (1 if the predicted coordinate is inside the box, 0 otherwise — same as the grounding reward) and a <strong>dense reward</strong> that gives a graded score by normalized distance to the ground-truth center.</p> \[f_{\text{param}}^{\text{dense}}(\theta_i, \theta^{\text{gt}}) = \max\!\left( 1 - \lambda \left( \frac{|x_i - x^{\text{gt}}|}{w} + \frac{|y_i - y^{\text{gt}}|}{h} \right),\; 0 \right)\] <p>Here $(x_i, y_i)$ and $(x^{\text{gt}}, y^{\text{gt}})$ are the predicted and ground-truth centers, $w$ and $h$ the ground-truth element’s width and height, and the decay factor $\lambda$ — set to 0.5 — controls sensitivity. The closer to the target, the closer the reward to 1; a full box-width off drives it to 0.</p> <p><strong>Optimization (GRPO).</strong> Both grounding and navigation RL use GRPO. Per training example, multiple predictions are sampled — <strong>8 from the original image + 4 from the zoom-in crop</strong> for grounding, and <strong>32 candidates</strong> for navigation. Each sample receives a reward $r_i$, and the policy is updated with a group-normalized advantage.</p> \[\begin{aligned} A_i &amp;= \frac{r_i - \text{mean}(r)}{\text{std}(r)}, \\ r &amp;= [r_1, r_2, \ldots, r_M] \end{aligned}\] <p>For efficiency, <strong>online filtering</strong> discards prompts whose sampled rewards are all identical (uniformly 0 or 1), since they carry no learning signal — focusing on the most informative examples that sharpen the decision boundary.</p> <h4 id="training-data-and-pipeline">Training Data and Pipeline</h4> <table> <thead> <tr> <th>Item</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Backbone</td> <td>Internal 3B dense multimodal LLM (pretrained on text + vision-language understanding data)</td> </tr> <tr> <td>Image encoder</td> <td>VitDet + AnyRes (dynamically partitions screenshots into a grid of cells)</td> </tr> <tr> <td>SFT data</td> <td>Public grounding/navigation datasets + mobile/desktop synthetic data (high-res grounding, short/long-CoT, QA, online multi-agent rollouts)</td> </tr> <tr> <td>RL data</td> <td>Grounding: OS-Atlas / Navigation: mobile+desktop synthetic + AgentNet</td> </tr> <tr> <td>Training steps</td> <td>SFT 10K steps, RL 1,500 steps</td> </tr> <tr> <td>RL algorithm</td> <td>GRPO + online filtering (grounding 8+4 samples, navigation 32 samples)</td> </tr> <tr> <td>Rewards</td> <td>Grounding: box containment / Navigation: action type ($f_{\text{type}}$) + parameters ($f_{\text{param}}$, sparse or dense, $\lambda=0.5$)</td> </tr> </tbody> </table> <h4 id="experiments">Experiments</h4> <p>The backbone is an internal 3B dense model, trained for 10K SFT steps + 1,500 RL steps. Grounding and navigation are evaluated in turn.</p> <h5 id="gui-grounding">GUI Grounding</h5> <p>Evaluation uses ScreenSpot-V2, ScreenSpot-Pro, and OSWorld-G, spanning mobile/desktop/web and a range of resolutions. ScreenSpot-Pro is the hard one, featuring high-resolution desktop screens.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab1-grounding-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab1-grounding-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab1-grounding-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab1-grounding.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 1. Grounding performance. Ferret-UI Lite-3B reaches 91.6 on ScreenSpot-V2, 53.3 on ScreenSpot-Pro, and 55.3 on OSWorld-G — well ahead of the 3B group and shoulder-to-shoulder with the 7B tier.</figcaption> </figure> <ul> <li><strong>ScreenSpot-V2 91.6</strong>: ahead of other 3B baselines (UI-R1-3B 89.2, Jedi-3B 88.8) and close to the 7B range of 90.3–92.8.</li> <li><strong>ScreenSpot-Pro 53.3</strong>: dramatically higher than other 3B models, which mostly sit in the mid-30s (Jedi-3B 37.1, GUI-G1-3B 38.1), and just below GUI-Owl-7B (54.9).</li> <li><strong>OSWorld-G 55.3</strong>: best among 3B (Jedi-3B 50.9), competitive with GUI-Owl-7B (55.9) and GUI-Owl-32B (58.0). The strongest 7B, GTA1-7B (67.7), still leads, but the gap is modest given the parameter scale.</li> </ul> <p>One thing worth flagging: <strong>the abstract reports OSWorld-G as 61.2, while Table 1, Figure 6a, and Table 8 all consistently say 55.3.</strong> Since three tables agree, this review uses the verified 55.3 (the abstract’s 61.2 appears to be a typo). It’s a 6-point discrepancy, not a rounding issue — easy to propagate if you cite from the abstract alone.</p> <h5 id="gui-navigation">GUI Navigation</h5> <p><strong>Offline (Android Control).</strong> Split into low-level (single-step instruction) and high-level (global goal) tasks. Ferret-UI Lite-3B scores 86.6% low-level and 68.9% high-level, beating similar-size models (OS-Atlas-4B 80.6/67.5, InternVL-2-4B 80.1/66.7) and even some 7B/72B models (Qwen2-VL-7B 82.6/69.7, Aguvis-72B 84.4/66.4).</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab3-navigation-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab3-navigation-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab3-navigation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/tab3-navigation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 3. Online navigation. (a) AndroidWorld: best among 3B models (28.0%), competitive with the 7B tier. (b) OSWorld-Verified (15-step): ahead of all 3B models (17.3%) but behind larger and commercial models.</figcaption> </figure> <ul> <li><strong>AndroidWorld 28.0%</strong>: averaged over five runs in a dynamic environment of 116 tasks across 20 apps. Best among 3B (ScaleCUA-3B 23.7) and competitive with the 7B tier such as UI-TARS-1.5-7B.</li> <li><strong>OSWorld-Verified 17.3%</strong> (max 15 steps): ahead of all 3B models (ScaleCUA-3B 9.6, OpenCUA-A3B 16.9) and competitive with 7B. Raising the step cap to 50 lifts it to <strong>19.8%</strong>, showing some test-time scaling headroom.</li> </ul> <p>The paper is candid about the ceiling, though: navigation is constrained by model scale and falls short of the OSWorld leaderboard SOTA (e.g., 43.9% for Claude-4-Sonnet). For reference, in the same 15-step internal evaluation, Claude-4-Sonnet scores 31.2% and Doubao-1.5-Thinking 31.9%.</p> <h4 id="analysis--ablations">Analysis / Ablations</h4> <p>The real value here is in the ablations, not the headline numbers.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig6-grounding-ablation-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig6-grounding-ablation-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig6-grounding-ablation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig6-grounding-ablation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 6. Grounding ablations. (a) RL consistently improves over SFT, and zoom-in adds a further gain. (b) A balanced 50:50 grounding:navigation mix is best. (c) Synthetic high-resolution data helps, especially on ScreenSpot-Pro.</figcaption> </figure> <ul> <li><strong>RL and zoom-in (Figure 6a)</strong>: RL consistently beats SFT-only. ScreenSpot-Pro goes 52.3 → 52.7 (RL, no zoom-in) → 53.3 (RL + zoom-in); OSWorld-G 54.1 → 54.1 → 55.3. The zoom-in adds the final increment, indicating the model learns not only from RL but also to actively use zoom-in for small or cluttered elements.</li> <li><strong>Data-mixture ratio (Figure 6b)</strong>: varying grounding:navigation across 0:100, 30:70, 50:50, 70:30 shows the two data types complement each other. Even with zero grounding data (0:100), ScreenSpot-Pro still reaches 41.2 — navigation data carries grounding signal — while adding grounding data doesn’t hurt navigation. Tellingly, OSWorld-Chrome collapses to 0.0 at 0:100 but recovers to 22.7 at 50:50, showing that even navigation presupposes grounding ability. <strong>The balanced 50:50 mix is best overall</strong> (ScreenSpot-Pro 50.1, above the grounding-heavy 70:30’s 49.5).</li> <li><strong>Synthetic high-resolution data (Figure 6c)</strong>: adding it nudges ScreenSpot-V2 (89.5 → 90.0) and helps more clearly on the hard ScreenSpot-Pro (48.3 → 48.9) — high-resolution data is particularly useful for precise localization.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig7-rl-ablation-480.webp 480w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig7-rl-ablation-800.webp 800w,/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig7-rl-ablation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0018-ferret-ui-lite-lessons-from-building-small-on-device-gui-age/fig7-rl-ablation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 7. AndroidWorld RL ablations. (a) The fewer the SFT steps, the larger RL's relative gain. (b) Action type (AT) alone is insufficient; AT + dense grounding (DG) is best.</figcaption> </figure> <p>The navigation-side ablations carry the core lessons of training small models.</p> <ul> <li><strong>CoT and synthetic data (Table 4)</strong>: on AndroidWorld, baseline 13.7 → short-CoT 15.8 → long-CoT 19.6 — longer reasoning traces lift multi-step performance. Scaling synthetic data on top of long-CoT from 5K → 13K → 17K pushes it further, 20.3 → 22.4 → 25.2 (about +6 points), evidence that scaling diverse synthetic data works.</li> </ul> <table> <thead> <tr> <th>Model Variants</th> <th>AndroidWorld success (%)</th> </tr> </thead> <tbody> <tr> <td>Baseline (no CoT / synthetic data)</td> <td>13.7</td> </tr> <tr> <td>+ Short CoT</td> <td>15.8</td> </tr> <tr> <td>+ Long CoT</td> <td>19.6</td> </tr> <tr> <td>+ Syn. data (5K)</td> <td>20.3</td> </tr> <tr> <td>+ Syn. data (13K)</td> <td>22.4</td> </tr> <tr> <td>+ Syn. data (17K)</td> <td>25.2</td> </tr> </tbody> </table> <ul> <li><strong>RL shines most when SFT is scarce (Figure 7a)</strong>: adding RLVR on top of 2K, 6K, and 10K SFT checkpoints improves all of them, but <strong>the gain is largest when SFT steps are fewer</strong> — RLVR compensates for limited SFT.</li> <li><strong>Reward design is everything (Figure 7b)</strong>: comparing four configurations — action type only (AT), sparse grounding only (SG), AT + SG, and AT + dense grounding (DG). <strong>Using only the action-type reward drops below even SFT-only</strong> (correct positions are never reinforced). Grounding reward alone improves tapping but doesn’t beat the SFT baseline. Combining the two yields consistent gains, and <strong>dense grounding beats sparse grounding</strong>. It is a direct demonstration of how much careful RLVR reward structure matters for small GUI agents.</li> </ul> <p>Additionally, a grounding-dataset ablation (Table 6) shows that removing OS-Atlas causes the largest drop on ScreenSpot-Pro (−6.80), confirming it is the most critical source for high-resolution desktop grounding.</p> <h4 id="limitations-and-critical-assessment">Limitations and Critical Assessment</h4> <ul> <li><strong>An inherent navigation ceiling.</strong> As the authors concede, multi-step navigation is tightly coupled to model scale. OSWorld 19.8% is good among 3B but far from commercial SOTA (40s). Packing enough long-horizon planning and self-reflection into a small model remains the fundamental hard problem here.</li> <li><strong>RL reward sensitivity = a reproducibility burden.</strong> The finding that “reward design decides performance” cuts both ways. It means sensitivity to hyperparameters — the dense reward’s $\lambda$, the sample counts (8+4 / 32), the online-filtering threshold — and with no code or weights released, external reproduction is hard.</li> <li><strong>The internal OSWorld-G inconsistency.</strong> The abstract (61.2) and the body tables (55.3) disagree by 6 points. In a paper that emphasizes verifiable, quantitative reporting, a mismatched headline number dents credibility.</li> <li><strong>No latency or memory numbers.</strong> Despite the “on-device” framing, no real-device inference latency, memory footprint, or battery impact is reported. Zoom-in effectively looks at one input twice, raising inference cost, yet that cost-accuracy trade-off is not quantified.</li> <li><strong>GPT-4o dependence in synthetic data.</strong> The CoT and QA data are generated via GPT-4o SoM prompting. This is distillation of a larger model’s reasoning into a smaller one, which caps quality at the teacher’s level and carries licensing and reproducibility constraints.</li> </ul> <h4 id="takeaways">Takeaways</h4> <ul> <li><strong>A small GUI agent is the sum of “data + inference-time tools + reward design.”</strong> There’s no single trick; the unified action space, synthetic data, zoom-in, and RLVR each add a few points to push a 3B model up to 7B-tier grounding.</li> <li><strong>Grounding and navigation help each other in one model.</strong> A 50:50 mix is best, and grounding ability is a prerequisite for navigation (0% grounding → 0% on OSWorld-Chrome). There’s little reason to train the two abilities separately.</li> <li><strong>RL rewards are all about “what you reinforce.”</strong> In a small model, rewarding action type alone actually regresses, and adding a dense grounding reward is what makes it climb steadily. There’s a clear regime where the shape of the reward matters more than model size.</li> <li><strong>Grounding is nearly solved; navigation is wide open.</strong> When a 3B hits 91.6 on ScreenSpot-V2, the marginal value of single-screen grounding shrinks. The next frontier is multi-step planning, error recovery, and long-horizon stability — and small models still have a long way to go there.</li> <li><strong>The value of a “lessons” paper.</strong> What endures isn’t one SOTA line but the ablations recording what worked and what didn’t. They become a ready starting point for anyone building on-device agents next.</li> </ul> <h4 id="references">References</h4> <ul> <li>Paper: <a href="https://arxiv.org/abs/2509.26539">Ferret-UI Lite: Lessons from Building Small On-Device GUI Agents (arXiv:2509.26539)</a></li> <li>Code/weights: not released</li> <li>Lineage: <a href="https://arxiv.org/abs/2404.05719">Ferret-UI (arXiv:2404.05719)</a>, Ferret-UI 2, and other Apple GUI-understanding work</li> </ul> <h4 id="further-reading">Further Reading</h4> <ul> <li><strong><a href="https://arxiv.org/abs/2404.05719">Ferret-UI: Grounded Mobile UI Understanding with Multimodal LLMs</a></strong> (You et al., 2024) — the direct ancestor of Ferret-UI Lite; the first MLLM to understand mobile UI screens with referring, grounding, and reasoning.</li> <li><strong><a href="https://arxiv.org/abs/2501.12326">UI-TARS: Pioneering Automated GUI Interaction with Native Agents</a></strong> (Qin et al., 2025) — the key baseline whose action taxonomy Ferret-UI Lite borrows and against which it compares on many benchmarks.</li> <li><strong><a href="https://arxiv.org/abs/2410.23218">OS-ATLAS: A Foundation Action Model for Generalist GUI Agents</a></strong> (Wu et al., ICLR 2025) — the RL grounding data and the source of the Android Control evaluation setting; the most important grounding dataset in the ablation.</li> <li><strong><a href="https://arxiv.org/abs/2404.07972">OSWorld: Benchmarking Multimodal Agents for Open-Ended Tasks in Real Computer Environments</a></strong> (Xie et al., NeurIPS 2024) — the benchmark for computer-use agents in real OS environments, where Ferret-UI Lite’s navigation ceiling is most visible.</li> <li><strong><a href="https://arxiv.org/abs/2402.03300">DeepSeekMath: Pushing the Limits of Mathematical Reasoning in Open Language Models</a></strong> (Shao et al., 2024) — introduces GRPO, the optimization algorithm Ferret-UI Lite’s RLVR uses.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="gui-agent"/><category term="on-device"/><category term="multimodal-llm"/><category term="reinforcement-learning"/><category term="grounding"/><category term="vision-language-action"/><summary type="html"><![CDATA[Apple's 3B on-device GUI agent. A close read of how real+synthetic data curation, zoom-in visual tool-use, and a two-stage SFT→RLVR recipe push a small model as far as it can go on GUI grounding and navigation — and where it honestly hits a wall.]]></summary></entry><entry xml:lang="ko"><title type="html">[논문 리뷰] ShowUI: One Vision-Language-Action Model for GUI Visual Agent</title><link href="https://baba9811.github.io/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/" rel="alternate" type="text/html" title="[논문 리뷰] ShowUI: One Vision-Language-Action Model for GUI Visual Agent"/><published>2026-05-29T05:00:00+00:00</published><updated>2026-05-29T05:00:00+00:00</updated><id>https://baba9811.github.io/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent</id><content type="html" xml:base="https://baba9811.github.io/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/"><![CDATA[<div class="lang-toggle" style="margin-bottom: 1.5rem;"> <a href="/en/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/" class="btn btn-sm btn-outline-secondary">🇺🇸 Read in English</a> </div> <h4 id="메타정보">메타정보</h4> <table> <thead> <tr> <th>항목</th> <th>내용</th> </tr> </thead> <tbody> <tr> <td>저자</td> <td>Kevin Qinghong Lin et al. (저자 9명, National University of Singapore · Microsoft)</td> </tr> <tr> <td>학회</td> <td>CVPR · 2025</td> </tr> <tr> <td>arXiv</td> <td><a href="https://arxiv.org/abs/2411.17465">2411.17465</a></td> </tr> <tr> <td>Code</td> <td><a href="https://github.com/showlab/ShowUI">showlab/ShowUI</a></td> </tr> <tr> <td>데이터</td> <td>자체 수집 웹 그라운딩 22K + AMEX 모바일 97K + OmniAct 데스크톱 + GUIAct 내비게이션 — 총 256K 샘플</td> </tr> <tr> <td><span style="white-space: nowrap">리뷰 일자</span></td> <td>2026-05-29</td> </tr> </tbody> </table> <h4 id="tldr">TL;DR</h4> <ul> <li>스크린샷을 HTML이나 accessibility tree 없이 <strong>사람처럼 픽셀로만 보고</strong> 클릭·입력하는 GUI 비주얼 에이전트 ShowUI를 제안한다. Qwen2-VL-2B 위에 세 가지 설계를 얹어, <strong>2B 모델이 256K 데이터만으로 zero-shot Screenspot 그라운딩 75.1%</strong>를 찍어 7B·18B 모델을 모두 앞선다.</li> <li>핵심은 <strong>UI-Guided Visual Token Selection</strong> — 스크린샷의 인접 패치를 RGB로 묶어 UI connected graph를 만들고, 한 컴포넌트 안의 중복 토큰을 학습 중에 무작위로 건너뛴다. 위치 정보를 보존하기 때문에 토큰을 평균으로 합치는 token merging과 달리 그라운딩 성능이 거의 떨어지지 않으면서 visual token을 ~33% 줄이고 학습을 1.4배 가속한다.</li> <li><strong>Interleaved Vision-Language-Action Streaming</strong>으로 내비게이션의 시각-행동 히스토리와 그라운딩의 다중 질의를 하나의 스트림으로 통합하고, <strong>데이터 품질·균형 샘플링</strong>으로 작지만 강한 학습 코퍼스를 만든다. 모델은 <a href="https://github.com/showlab/ShowUI">showlab/ShowUI</a>에 전부 공개돼 있다.</li> </ul> <h4 id="소개-introduction">소개 (Introduction)</h4> <p>Graphical User Interface(GUI)는 사람이 디지털 세계와 상호작용하는 기본 통로다. 그래서 “사용자의 자연어 지시를 받아 화면을 조작하는 에이전트”는 워크플로우 자동화의 오랜 목표였다. 초기 접근의 대부분은 <strong>language agent</strong> — 화면 뒤의 HTML이나 accessibility tree 같은 텍스트 메타정보를 GPT-4 류의 closed-source LLM에 먹여 다음 행동을 고르는 방식이었다. 텍스트 구조가 깔끔하게 주어지는 환경에서는 강력하지만, 현실의 사용자는 그런 “구조적 오라클”에 접근하지 못한 채 <strong>눈으로 화면을 보고</strong> 마우스를 움직인다. 모바일 앱, 캔버스 기반 디자인 툴, 게임처럼 DOM이 의미를 거의 담지 못하는 화면에서는 텍스트 메타정보 자체가 없거나 신뢰할 수 없다.</p> <p>그래서 최근 흐름은 <strong>GUI visual agent</strong> — 스크린샷을 직접 입력으로 받아 사람처럼 시각적으로 UI를 인지하고 조작하는 모델로 옮겨가고 있다. 그런데 이 전환에는 세 가지 고유한 난관이 따른다. (a) <strong>비싼 시각 모델링</strong>: UI 스크린샷은 보통 2K 해상도라 패치로 쪼개면 토큰 수가 폭발하고, self-attention의 long-context 비용이 커진다. (b) <strong>시각-언어-행동의 얽힘 관리</strong>: 행동은 기기마다 다르고(모바일의 <code class="language-plaintext highlighter-rouge">PRESS HOME</code>은 웹에 없고, <code class="language-plaintext highlighter-rouge">SCROLL</code>은 웹에선 상하 2방향·모바일에선 4방향), 내비게이션은 여러 스텝의 스크린샷·행동 히스토리가 누적된다. (c) <strong>다양한 학습 데이터</strong>: 웹·모바일·데스크톱에 걸친 GUI 데이터는 그라운딩·내비게이션 등 목적이 제각각이라 어떻게 골라 섞을지가 불명확하다.</p> <p>ShowUI는 이 세 난관을 각각 정조준하는 세 가지 설계로 답한다. 그리고 그 답이 단지 “큰 모델로 밀어붙이기”가 아니라 <strong>GUI라는 도메인의 구조를 이용한 효율화</strong>라는 점에서 읽을 가치가 있다. 자연 이미지 연구의 토큰 압축 기법(token pruning, token merging)을 그대로 가져오면 GUI에서 망가지는데, 왜 망가지고 무엇으로 고쳐야 하는지를 이 논문이 깔끔하게 보여준다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig2-comparison-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig2-comparison-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig2-comparison-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig2-comparison.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 2. (왼쪽) Screenspot zero-shot 그라운딩에서 모델 크기·학습 데이터 대비 정확도. ShowUI-2B는 가장 작고 가장 적은 데이터로 최고 정확도를 낸다. (오른쪽) UI-guided 토큰 선택으로 visual token을 33% 줄이고 학습을 1.4배 가속한다.</figcaption> </figure> <h4 id="핵심-기여-key-contributions">핵심 기여 (Key Contributions)</h4> <ul> <li><strong>UI-Guided Visual Token Selection</strong>: RGB 공간에서 스크린샷의 중복 패치를 묶어 UI connected graph를 구성하고, 이를 self-attention의 토큰 선택 기준으로 삼는다. 추가 학습 파라미터 없이 visual token 중복을 줄여 계산량을 낮춘다.</li> <li><strong>Interleaved Vision-Language-Action Streaming</strong>: 행동을 JSON으로 표준화하고, 내비게이션의 시각-행동 히스토리(action-visual)와 그라운딩의 다중 질의(action-query)를 하나의 interleaved 스트림으로 통합해 다양한 GUI 시나리오를 한 모델로 처리한다.</li> <li><strong>소규모 고품질 instruction-following 데이터셋</strong>: 데이터 타입별 가치를 분석(웹에서 static text는 40%지만 정보량이 낮다 등)하고, 시각적으로 풍부한 요소만 남긴 뒤 균형 샘플링으로 불균형을 보정한다. 총 256K 샘플로 학습.</li> <li><strong>결과</strong>: 2B 모델이 zero-shot Screenspot 그라운딩 75.1%로 7B·18B 모델을 앞서고, AITW(모바일)·Mind2Web(웹)·MiniWob(온라인) 내비게이션에서 경쟁력 있는 성능을 보인다. 모델·코드 전면 공개.</li> </ul> <h4 id="관련-연구--배경-지식">관련 연구 / 배경 지식</h4> <p><strong>GUI 에이전트의 두 갈래.</strong> 하나는 <strong>training-free</strong> 방식으로, GUI를 HTML로 변환하거나 accessibility tree를 읽거나 OCR·Set-of-Marks로 화면을 텍스트화한 뒤 closed-source LLM에 넘긴다. 강력하지만 비싼 API에 의존하고, 텍스트 오라클이 없는 화면에선 무력하다. 다른 하나는 <strong>training-based</strong> 방식으로, 대규모 vision-text 코퍼스로 사전학습한 모델에 element grounding이나 navigation을 직접 학습시킨다. SeeClick 류가 웹 스크린샷으로 그라운딩 능력을 키운 대표 사례이고, ShowUI도 이 두 번째 갈래에 속한다.</p> <p><strong>그라운딩 vs 내비게이션.</strong> GUI 태스크는 크게 두 가지다. <strong>그라운딩</strong>은 “이 버튼을 눌러”라는 지시에서 클릭할 좌표 $[x, y]$를 찍는 것이고(한 스크린샷 + 한 질의), <strong>내비게이션</strong>은 여러 스텝에 걸쳐 화면을 바꿔가며 목표를 달성하는 것(누적 히스토리)이다. 두 태스크는 입력 구조가 달라 보통 따로 다뤄지는데, ShowUI는 이를 하나의 streaming 형식으로 합친다.</p> <p><strong>왜 자연 이미지의 토큰 압축이 GUI에서 안 통하는가.</strong> Vision Transformer의 계산 병목은 긴 토큰 시퀀스를 처리하는 cascaded self-attention이다. 자연 이미지에서는 token pruning이나 token merging(ToMe)으로 비슷한 패치를 합쳐 토큰을 줄인다. 그러나 GUI 그라운딩은 <strong>정확한 위치</strong>가 생명이다. 토큰을 평균으로 합쳐버리면(merging) 그 토큰이 화면의 어디였는지 위치 정보가 사라져 좌표를 찍을 수 없다. ShowUI는 여기서 영감을 Mixture-of-Depth(MoD)에서 가져온다 — 토큰을 합치는 대신 <strong>일부 토큰을 라우팅으로 건너뛰되, 살아남은 토큰은 원래 위치 임베딩을 그대로 유지</strong>한다. 문제는 “어떤 토큰을 건너뛸지”의 기준인데, ShowUI는 그 기준을 학습하는 대신 <strong>UI connected graph라는 공짜 사전 지식</strong>으로 대체한다.</p> <h4 id="방법--아키텍처-상세">방법 / 아키텍처 상세</h4> <p>ShowUI는 Qwen2-VL-2B를 백본으로 삼아 세 컴포넌트를 얹는다. 전체 동작은 다음과 같다: 사용자 태스크 쿼리, 사전 정의된 action space(시스템 프롬프트의 README로 제공), 초기 스크린샷을 받아 → 다음 행동을 예측 → 화면 갱신 → 반복. 시각 토큰과 텍스트(행동) 토큰이 하나의 시퀀스에 interleaved되어 self-attention을 통과한다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig3-architecture-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig3-architecture-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig3-architecture-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig3-architecture.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 3. ShowUI의 동작 개요. 태스크 쿼리·action space·초기 스크린샷에서 시작해, 시각 관찰(녹색)과 행동 히스토리(주황)를 interleaved 스트림으로 쌓으며 다음 행동을 순차 생성한다.</figcaption> </figure> <h3 id="ui-connected-graph-gui-중복을-rgb로-잡아내기">UI Connected Graph: GUI 중복을 RGB로 잡아내기</h3> <p>핵심 통찰은 “UI 스크린샷은 자연 이미지와 근본적으로 다르다”는 것이다. 자연 이미지는 텍스처·패턴이 풍부해 거의 모든 패치가 정보를 담지만, UI는 빈 공간·단색 배경·반복되는 영역이 많아 <strong>상당수 패치가 중복</strong>이다. 게다가 UI는 가독성을 위해 일관된 색 구성을 쓰기 때문에, 시각적으로 같은 영역은 <strong>RGB 값이 거의 정확히 같다</strong>. 이 성질이 공짜 redundancy 신호가 된다.</p> <p>ShowUI는 스크린샷을 패치 격자로 나눠 각 패치를 그래프의 노드로 보고, 인접한 두 패치의 RGB 차이가 임계값 $\delta$보다 작으면 두 노드를 연결한다. 그 결과 화면은 $K$개의 connected component로 묶이는데, $K$는 원래 패치 수 $G_h \times G_w$보다 훨씬 작다. 연결 판정은 다음 조건으로 이뤄진다.</p> \[\| \mathrm{RGB}(i, j) - \mathrm{RGB}(i', j') \| &lt; \delta\] <p>여기서 $(i’, j’)$는 $(i, j)$의 오른쪽·아래 이웃이다. 컴포넌트를 효율적으로 찾기 위해 Union-Find를 쓴다.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Algorithm 1: Find Connected Components on UI-Graph
입력:  크기 H × W 스크린샷, 패치 크기 c, 임계값 δ
출력:  패치 → connected component 할당 맵
1: 이미지를 G_h × G_w 패치로 분할 (G_h = H/c, G_w = W/c), 각 패치는 노드
2: 모든 노드에 대해 Union-Find 구조 UF 초기화
3: for 모든 노드 (i, j) do
4:     for (i,j)의 오른쪽·아래 이웃 (i', j') do
5:         if ‖RGB(i,j) − RGB(i',j')‖ &lt; δ then
6:             UF.union((i, j), (i', j'))
7: return UF의 할당 맵
</code></pre></div></div> <p>이 그래프가 화면의 정보량에 따라 <strong>적응적</strong>으로 컴포넌트 수를 정한다는 점이 좋다. 예컨대 여백이 많은 Google 검색 화면은 1296개 패치가 291개 컴포넌트로 압축되고, 텍스트가 빽빽한 Overleaf 화면은 986개 컴포넌트로 덜 압축된다. 즉 “압축률을 사람이 정하지 않아도 화면이 알아서 결정한다”.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig5-uigraph-examples-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig5-uigraph-examples-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig5-uigraph-examples-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig5-uigraph-examples.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 5. 모바일(a–d)·PC(e–f)·웹(g–h) 스크린샷에서 구성된 UI-connected graph. 여백이 많은 화면(예: 1272→175)은 컴포넌트가 적고, 텍스트가 밀집한 화면(예: 1296→740)은 많다. 압축이 정보량에 적응한다.</figcaption> </figure> <h3 id="token-selection-vs-token-merging-위치를-지켜라">Token Selection vs Token Merging: 위치를 지켜라</h3> <p>UI connected graph를 얻었으니 이제 이걸로 토큰을 줄여야 한다. 두 가지 방법이 있다.</p> <ul> <li><strong>Token Merging</strong>: 한 컴포넌트의 모든 패치를 하나의 토큰으로 풀링(pooling)한다. 토큰 수는 컴포넌트 수까지 줄지만, 풀링 과정에서 <strong>개별 패치의 위치 정보가 사라진다</strong>. 그라운딩에서 치명적이다.</li> <li><strong>Token Selection (ShowUI의 선택)</strong>: 한 컴포넌트 안에서 일부 토큰을 학습 중 <strong>무작위로 건너뛰고</strong>, 단일 패치 컴포넌트는 건드리지 않는다. 살아남은 토큰은 <strong>원래 위치 임베딩을 그대로 유지</strong>하므로, 더 짧은 시퀀스 위에서도 self-attention이 원래 공간 관계를 보존한 채 동작한다. 추가 학습 파라미터가 없다.</li> </ul> <p>이 token selection은 self-attention 블록에 삽입되는데, Un-Pooling/Pooling으로 감싸 일부 레이어에서만 토큰을 줄였다 복원한다. 학습 때는 정해진 비율로 무작위 마스킹을 하고, 추론 때는 토큰 선택을 켜거나(속도↑) 끌 수(정확도↑) 있다. 두 경우 모두 전체 시퀀스의 위치 관계가 일관되게 유지된다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig4-token-selection-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig4-token-selection-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig4-token-selection-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig4-token-selection.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 4. (왼쪽) 스크린샷을 28×28 패치로 나눠 RGB가 같은 인접 패치를 묶어 UI connected graph를 만든다. (오른쪽) token merging은 한 컴포넌트를 한 토큰으로 합쳐 위치 정보를 잃지만, token selection은 일부 토큰만 샘플링하고 원래 위치 관계를 유지한다.</figcaption> </figure> <p>참고로 1344×756 PC 스크린샷은 14×14 패치로 약 5184개의 raw 토큰이 되고, Qwen2-VL의 2×2 merge를 거치면 1296개가 된다. 여기서도 self-attention 비용은 여전히 부담스럽다. ShowUI는 그 위에서 token selection으로 한 번 더 줄인다.</p> <h3 id="interleaved-vision-language-action-streaming">Interleaved Vision-Language-Action Streaming</h3> <p>두 번째 컴포넌트는 행동을 다른 모달리티와 어떻게 엮느냐다. ShowUI는 모든 행동을 JSON으로 표준화한다.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{'action': 'CLICK', 'value': None, 'position': [0.18, 0.50]}
{'action': 'TYPE',  'value': 'las vegas', 'position': [0.54, 0.42]}
</code></pre></div></div> <p>좌표 <code class="language-plaintext highlighter-rouge">[x, y]</code>는 0–1로 정규화돼 기기·해상도에 무관하게 통일된다. 게다가 시스템 프롬프트에 각 행동의 사용법을 적은 <strong>README</strong>를 함께 넣는다(예: “<code class="language-plaintext highlighter-rouge">CLICK</code>: 요소를 클릭, value는 불필요, position 필요”). 이렇게 하면 모델이 고정된 행동 집합을 외우는 대신 <strong>action space 문서를 읽고 test time에 함수 호출처럼 해석</strong>하게 되어, 학습 때 보지 못한 새 행동도 다룰 수 있다.</p> <p>행동을 다른 모달리티와 엮는 방식은 태스크에 따라 두 가지다(Figure 6).</p> <ul> <li><strong>Action-Visual Streaming (내비게이션)</strong>: 스크린샷과 행동을 시간 순서대로 번갈아 쌓는다. $i$번째 행동을 한 뒤 $(i+1)$번째 스크린샷이 큐에 들어가고, 모델은 그걸 보고 다음 행동을 낸다. 응용에 따라 시각 히스토리의 일부를 마스킹할 수 있다. 모바일은 소프트웨어가 화면을 크게 바꾸므로 스크린샷을 유지하고, 웹은 화면이 비교적 정적이라 마스킹해 효율을 높인다.</li> <li><strong>Action-Query Streaming (그라운딩)</strong>: 한 스크린샷에 여러 질의-행동 쌍을 묶어 <strong>한 번의 forward로 동시에</strong> 예측한다. 스크린샷 토큰(1–2K)에 비해 질의는 10토큰 미만으로 훨씬 짧기 때문에, 이미지 하나에 행동 하나만 붙이는 방식은 낭비다. 여러 질의를 한 화면에 묶으면 학습 데이터 활용도가 크게 오른다.</li> </ul> <p>히스토리 길이는 2로 설정했다. 이 streaming 설계 덕분에 그라운딩과 내비게이션이라는 입력 구조가 다른 두 태스크를 <strong>하나의 모델·하나의 형식</strong>으로 학습할 수 있다.</p> <h4 id="학습-목표--손실-함수">학습 목표 / 손실 함수</h4> <p>ShowUI의 학습 목표는 특별한 보조 손실 없이, interleaved 스트림 위에서 행동 토큰에 대한 <strong>표준 language modeling 손실</strong>이다. Figure 6에서 보듯 시각 토큰은 조건(컨텍스트)으로만 들어가고, LM loss는 행동(action-visual 모드) 또는 질의에 대응하는 행동(action-query 모드)에만 걸린다. 즉 스크린샷을 $s$, 행동을 $a$로 쓰면 시퀀스 ${s_1, a_1, s_2, a_2, \dots}$에서 행동 부분만 다음 토큰 예측 대상이 된다.</p> \[\mathcal{L} = - \sum_{t \in \mathcal{A}} \log p_\theta\!\left(a_t \mid a_{&lt;t}, \; v_{\le t}\right)\] <p>여기서 $\mathcal{A}$는 행동 토큰의 인덱스 집합, $a_t$는 $t$번째 행동 토큰, $v_{\le t}$는 그 시점까지의 시각 관찰(token selection을 거친 압축 표현)이다. UI-guided token selection은 이 목적함수를 바꾸지 않고 $v$의 시퀀스 길이만 줄인다 — 추가 파라미터도, 추가 손실 항도 없다는 점이 설계의 깔끔함이다.</p> <h4 id="학습-데이터와-파이프라인">학습 데이터와 파이프라인</h4> <p>세 번째 기여는 데이터다. 저자들은 “모든 데이터를 다 긁어 넣기”를 거부하고 타입별 가치를 분석한다. 웹에서 <code class="language-plaintext highlighter-rouge">static text</code> 태그는 전체의 40%를 차지하지만, 대부분의 VLM이 이미 OCR 능력을 갖췄으므로 <strong>정보 가치가 낮다</strong>. 그래서 <code class="language-plaintext highlighter-rouge">Button</code>·<code class="language-plaintext highlighter-rouge">Checkbox</code>처럼 시각적으로 풍부한 요소만 남긴다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab1-data-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab1-data-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab1-data-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab1-data.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 1. ShowUI instruction-tuning 데이터 개요. 그라운딩(Web/Mobile/Desktop)과 내비게이션(Web/Mobile) 합쳐 256K 샘플, 2.7M 요소.</figcaption> </figure> <table> <thead> <tr> <th>종류</th> <th>출처</th> <th>규모</th> <th>비고</th> </tr> </thead> <tbody> <tr> <td>그라운딩·웹</td> <td>자체 수집 (PyAutoGUI 파서, 22개 사이트)</td> <td>22K 스크린샷 / 576K 요소</td> <td>static text 제거 후 시각 요소만 (스크린샷당 평균 26개)</td> </tr> <tr> <td>그라운딩·모바일</td> <td>AMEX</td> <td>97K 스크린샷 / 926K 요소</td> <td>element grounding + functionality</td> </tr> <tr> <td>그라운딩·데스크톱</td> <td>OmniAct</td> <td>100 스크린샷 / 2K raw 주석</td> <td>GPT-4o로 appearance·spatial·intention 질의 증강 → 6K 요소</td> </tr> <tr> <td>내비게이션·웹</td> <td>GUIAct</td> <td>72K / 569K (행동 9종, 평균 길이 7.9)</td> <td>one/multi-step</td> </tr> <tr> <td>내비게이션·모바일</td> <td>GUIAct</td> <td>65K / 585K (행동 5종, 평균 길이 9.0)</td> <td>multi-step</td> </tr> <tr> <td><strong>합계</strong></td> <td>Diverse</td> <td><strong>256K 샘플 / 2.7M 요소</strong></td> <td> </td> </tr> </tbody> </table> <p>데이터 규모가 타입마다 크게 다르다(데스크톱은 100개뿐). 그대로 섞으면 큰 데이터셋에 학습이 쏠린다. 그래서 <strong>균형 샘플링(balanced sampling)</strong>으로 각 배치가 모든 타입을 비슷한 확률로 포함하게 한다. 가중치는 (Web : Mobile : Desktop : GUIAct-Web : GUIAct-Mobile) = (1 : 1 : 1 : 1 : 1).</p> <p><strong>학습 설정.</strong> 백본은 Qwen2-VL-2B. LoRA(rank 64, alpha 128)를 language model과 vision encoder 양쪽에 적용해 전체의 4%만 학습한다. instruction-tuning은 V100 32장, downstream 적응은 8장. GPU당 batch size 1에 gradient accumulation 2, float16, DeepSpeed Zero-2, SDPA attention, 학습률 1e-4, 최대 visual patch 1280. UI-graph는 vision encoder와 LM 양쪽에 비율 0.75 마스킹·layer 14 cross-layer 삽입으로 적용하고, 히스토리는 2. 전체 instruction-tuning은 약 2일.</p> <h4 id="실험-결과">실험 결과</h4> <h3 id="그라운딩--screenspot">그라운딩 — Screenspot</h3> <p>Screenspot은 모바일·데스크톱·웹 세 기기에서 텍스트/아이콘 요소를 zero-shot으로 찾는 그라운딩 벤치마크다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab2-grounding-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab2-grounding-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab2-grounding-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab2-grounding.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 2. Screenspot zero-shot 그라운딩. ShowUI(2B, 256K)가 평균 75.1%로 UGround-7B(73.3, 1.3M)와 GPT-4V 기반 OmniParser(73.0)를 앞선다.</figcaption> </figure> <p>ShowUI(2B, 256K)는 평균 <strong>75.1%</strong>로, UGround-7B(73.3, 데이터 1.3M)·GPT-4V를 쓰는 OmniParser(73.0)·SeeClick-9.6B(53.4)·CogAgent-18B(47.4)를 모두 앞선다. 그라운딩 데이터만 쓴 변형 ShowUI-G(119K)도 74.9로 거의 동등하다. 즉 <strong>더 작은 모델이 더 적은 데이터로 더 잘한다</strong>.</p> <p>세부 경향도 시사적이다. (i) 모든 모델에서 텍스트 트랙 점수가 아이콘 트랙보다 높다 — 텍스트 그라운딩 능력은 웹·모바일 데이터에서 잘 전이되지만, 아이콘은 순수한 시각 그라운딩이라 더 어렵다. (ii) 아이콘은 모바일에서 점수가 높은데, 데스크톱·웹보다 모바일 UI 그라운딩 데이터가 풍부했기 때문이다. (iii) 내비게이션 데이터를 섞어도 균형 샘플링을 쓰면 그라운딩 성능이 떨어지지 않는다.</p> <h3 id="모바일-내비게이션--aitw">모바일 내비게이션 — AITW</h3> <p>AITW(Android in the Wild)는 11종 행동을 가진 안드로이드 환경이다. ShowUI는 전체 평균 <strong>70.0</strong>으로 Qwen2-VL-2B(67.2)·SeeClick(59.3)·OmniParser(57.7)를 앞선다. 특히 interleaved streaming(시각 히스토리)을 뺀 변형 ShowUI†(68.3)와 비교하면 <strong>시각 컨텍스트가 +1.7</strong>을 기여한다. 행동 공간이 11종으로 큰 모바일에서, 직전 화면을 보는 것이 적절한 행동 선택에 중요함을 보여준다. 또한 GUIAct에서 배운 zero-shot 내비게이션이 전이된다는 점도 확인된다.</p> <h3 id="웹-내비게이션--mind2web">웹 내비게이션 — Mind2Web</h3> <p>Mind2Web은 Cross-Task·Cross-Website·Cross-Domain 세 split으로 일반화를 본다. ShowUI는 Step Success Rate에서 각각 37.2 / 35.1 / 35.2로 SeeClick과 비슷한 수준이며, 2B zero-shot으로도 Operation F1 80%+를 달성한다. 흥미로운 발견은 <strong>시각 컨텍스트의 이득이 AITW보다 작다</strong>는 것 — Mind2Web은 시각적으로 비슷한 단일 웹사이트에 집중되고 행동이 3종뿐이라 직전 화면 정보의 가치가 작다. 그리고 cross-website·cross-domain 설정이 더 어려운 점에서, <strong>병목은 텍스트 태스크 이해가 아니라 UI 시각 인지</strong>(처음 보는 사이트·도메인)에 있다고 저자들은 결론짓는다. 후속 개선의 방향은 “시각적으로 다양한 도메인의 학습 데이터”다.</p> <h3 id="온라인--miniwob">온라인 — MiniWob</h3> <p>MiniWob은 35-task split의 온라인 인터랙티브 환경이다. ShowUI는 71.5로 SeeClick(67.0)·Qwen2-VL-2B(66.8)를 앞선다. 다만 zero-shot 변형 ShowUI-ZS는 27.1로, fine-tune된 Qwen-VL(48.4)과 큰 격차가 난다. 저자들의 해석은 “offline instruction-tuning만으로는 out-of-distribution 오류를 충분히 다루지 못하며, <strong>온라인 환경에 맞는 학습 전략(예: RL)</strong>이 필요하다”는 것이다.</p> <h4 id="결과-분석--ablation">결과 분석 / Ablation</h4> <p>ablation은 UI-guided token selection의 설계 선택을 정조준한다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig9-ablation-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig9-ablation-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig9-ablation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig9-ablation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 9. UI-Guided Token Selection ablation. (a) 압축 방식: token merging은 위치 손실로 급락(테스트 적용 34.7), random selection도 56.2, UI-graph selection은 70.4/64.9로 baseline 70.8에 근접. (b) 삽입 레이어: cross-layer가 최선. (c) 선택 비율: 0.5가 속도·성능 균형점.</figcaption> </figure> <p><strong>압축 방식(9a).</strong> baseline(압축 없음)은 #Vis.Ctx 1344.0에 Screenspot 70.8이다. token merging은 학습 시 토큰을 852.8로 줄이지만 정확도가 42.3(학습만 적용)·34.7(테스트도 적용)로 무너진다 — 위치 정보 손실의 직접적 증거다. random token selection은 65.3 / 56.2로 merging보다는 낫지만 여전히 손해다. ShowUI의 UI-graph token selection은 947.4 토큰에서 <strong>70.4(학습만)·64.9(테스트도 적용)</strong>로, baseline 70.8에 거의 근접한다. 즉 “어떤 토큰을 버릴지”의 기준으로 UI connected graph가 무작위보다 훨씬 효과적임을 보여준다. 테스트 때 적용하면 해상도 손실로 살짝 떨어지지만(64.9), random(56.2)보다 훨씬 안정적이다.</p> <p><strong>삽입 레이어(9b).</strong> 같은 수(14개)의 레이어에 token selection을 삽입할 때, 초반 레이어에만(68.2)·후반에만(67.6)·전체 28개(65.7)보다 cross-layer(삽입 레이어와 비삽입 레이어를 번갈아, 70.5)가 확연히 낫다. 모든 레이어에서 줄이면 오히려 손해라는 점이 흥미롭다 — 일부 레이어는 full 토큰으로 정보를 복원해야 한다.</p> <p><strong>선택 비율(9c).</strong> 비율 0(압축 없음, 70.8)에서 1.0(최대 압축, 64.5)까지 갈수록 토큰은 1344→762로 줄지만 정확도도 떨어진다. 0.5(947 토큰, 70.4)가 속도와 성능의 균형점으로 제시된다. 배포 설정은 부록 기준 0.75 마스킹으로, Figure 2의 “33% 감소·1.4배 가속”이 이에 대응한다.</p> <p><strong>Interleaved streaming(Fig 10·11)·데이터(Table 6).</strong> 다중 턴 streaming은 학습 초기 warmup 단계에서 진행이 빠르고 데이터 활용도가 높다. 시각+행동+다중턴 설정이 행동만 쓰는 설정을 일관되게 앞선다. 데이터 측면에서는 (i) 품질이 중요하다 — OmniAct는 요소가 2K뿐인데도 웹 데이터에 필적하고, GPT-4o 증강으로 더 좋아진다. (ii) 자체 수집 22K 웹 데이터가 SeeClick의 270K를 앞선다 — static text를 걸러 요소 크기를 줄여도 성능이 떨어지지 않는다(정적 텍스트는 VLM에 덜 유익하다는 방증). (iii) 균형 샘플링이 +3.7% 정확도를 준다.</p> <h4 id="한계와-비판적-평가">한계와 비판적 평가</h4> <ul> <li><strong>저자가 인정한 한계</strong>: ShowUI는 주로 offline 데이터로 학습됐다. 온라인 환경에서의 깊은 탐색을 위해 RL 등으로 강화하는 것이 향후 과제이며, MiniWob zero-shot(27.1 vs fine-tune 48.4)의 큰 격차가 이 한계를 드러낸다.</li> <li><strong>RGB 정확 일치의 취약성</strong>: UI connected graph는 인접 패치의 RGB가 거의 정확히 같다는 가정에 의존한다. 안티앨리어싱·그라데이션·사진/썸네일이 많은 화면(이미지 위주 쇼핑·미디어 페이지, 앱 아이콘 그리드)에서는 압축이 잘 안 될 수 있고, 임계값 $\delta$의 민감도 분석이 본문에 깊게 제시되지 않았다.</li> <li><strong>학습 가속 ≠ 추론 가속</strong>: Figure 2의 1.4배는 <strong>학습</strong> 가속이다. 추론에서 토큰 선택을 끄면(정확도 70.4 유지) 속도 이득이 없고, 켜면(64.9) 정확도가 떨어진다. 실제 에이전트 루프에서의 latency·비용은 보고되지 않았다.</li> <li><strong>그라운딩 평가의 단일성</strong>: 그라운딩 정량 평가가 Screenspot 계열에 집중돼 있다. 다양한 해상도·언어권·테마(다크 모드 등)에 대한 강건성은 정성 예시 수준에서만 확인된다.</li> <li><strong>베이스라인 비교의 폭</strong>: 그라운딩에서 더 큰 모델을 앞선 점은 인상적이나, “같은 2B 백본을 더 많은 데이터/더 큰 모델로 키웠을 때”의 상한이 비교되지 않아, 효율 이득과 데이터 효과가 분리되어 측정되지 않는다.</li> </ul> <h4 id="시사점--takeaways">시사점 / Takeaways</h4> <ul> <li><strong>스케일보다 도메인 구조.</strong> 2B 모델이 256K 데이터로 7B·18B GUI 모델을 그라운딩에서 앞섰다. 데이터 품질·균형 샘플링과 시각 토큰 효율화가 단순 스케일업을 이긴 사례다.</li> <li><strong>GUI에는 GUI만의 redundancy prior가 있다.</strong> RGB 기반 UI connected graph는 자연 이미지엔 없는, GUI에만 통하는 무료·무파라미터 압축 신호다. “도메인의 구조를 공짜로 활용한다”는 발상이 핵심.</li> <li><strong>그라운딩에서는 위치가 전부다.</strong> token merging(위치 손실)이 무너지고 token selection(위치 유지)이 살아남는 ablation은, 좌표를 찍는 태스크에서 위치 정보 보존이 협상 불가임을 명확히 보여준다.</li> <li><strong>행동을 함수 호출처럼.</strong> 행동을 JSON으로 표준화하고 README로 action space를 문서화하면, 기기마다 다른 행동을 한 모델로 다루고 학습 때 못 본 새 행동도 해석할 수 있다.</li> <li><strong>GUI 내비게이션의 병목은 텍스트가 아니라 시각 인지다.</strong> cross-domain 일반화를 막는 건 태스크 이해가 아니라 처음 보는 화면을 보는 능력이다. 다음 데이터는 “시각적으로 다양한” 쪽으로 모아야 한다.</li> </ul> <h4 id="설치-및-사용법">설치 및 사용법</h4> <p>ShowUI는 <a href="https://github.com/showlab/ShowUI">showlab/ShowUI</a>에 공개돼 있고, 체크포인트는 Hugging Face의 <code class="language-plaintext highlighter-rouge">showlab/ShowUI-2B</code>로 받을 수 있다. Qwen2-VL 계열이라 <code class="language-plaintext highlighter-rouge">transformers</code>로 바로 로드된다. 아래는 그라운딩(좌표 예측)의 최소 골격으로, 정확한 전처리 헬퍼는 레포의 예제를 따르는 것이 좋다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">Qwen2VLForConditionalGeneration</span><span class="p">,</span> <span class="n">AutoProcessor</span>

<span class="n">model</span> <span class="o">=</span> <span class="n">Qwen2VLForConditionalGeneration</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">showlab/ShowUI-2B</span><span class="sh">"</span><span class="p">,</span> <span class="n">torch_dtype</span><span class="o">=</span><span class="sh">"</span><span class="s">auto</span><span class="sh">"</span><span class="p">,</span> <span class="n">device_map</span><span class="o">=</span><span class="sh">"</span><span class="s">auto</span><span class="sh">"</span>
<span class="p">)</span>
<span class="n">processor</span> <span class="o">=</span> <span class="n">AutoProcessor</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">showlab/ShowUI-2B</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># 행동 공간을 설명하는 system README + 스크린샷 + 질의를 넣으면
# 모델이 {'action': 'CLICK', 'value': None, 'position': [x, y]} 형식으로 출력
</span><span class="n">messages</span> <span class="o">=</span> <span class="p">[{</span>
    <span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">image</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">image</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">screenshot.png</span><span class="sh">"</span><span class="p">},</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Click the search button</span><span class="sh">"</span><span class="p">},</span>
    <span class="p">],</span>
<span class="p">}]</span>
<span class="n">text</span> <span class="o">=</span> <span class="n">processor</span><span class="p">.</span><span class="nf">apply_chat_template</span><span class="p">(</span><span class="n">messages</span><span class="p">,</span> <span class="n">add_generation_prompt</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="c1"># 이후 processor로 토크나이즈 → model.generate → position [x,y] (0–1 정규화) 파싱
</span></code></pre></div></div> <h4 id="참고-자료">참고 자료</h4> <ul> <li>논문: <a href="https://arxiv.org/abs/2411.17465">https://arxiv.org/abs/2411.17465</a></li> <li>Code: <a href="https://github.com/showlab/ShowUI">https://github.com/showlab/ShowUI</a></li> <li>모델 카드: <a href="https://huggingface.co/showlab/ShowUI-2B">https://huggingface.co/showlab/ShowUI-2B</a></li> <li>CVPR 2025 Open Access: <a href="https://openaccess.thecvf.com/content/CVPR2025/html/Lin_ShowUI_One_Vision-Language-Action_Model_for_GUI_Visual_Agent_CVPR_2025_paper.html">https://openaccess.thecvf.com/content/CVPR2025/html/Lin_ShowUI_One_Vision-Language-Action_Model_for_GUI_Visual_Agent_CVPR_2025_paper.html</a></li> </ul> <h4 id="더-읽어보기">더 읽어보기</h4> <ul> <li><strong><a href="https://arxiv.org/abs/2401.10935">SeeClick: Harnessing GUI Grounding for Advanced Visual GUI Agents</a></strong> (Cheng et al., 2024) — 웹 스크린샷으로 GUI 그라운딩 능력을 사전학습한 대표 연구로, ShowUI의 직접 비교 대상이자 데이터 비교 기준.</li> <li><strong><a href="https://arxiv.org/abs/2408.00203">OmniParser for Pure Vision Based GUI Agent</a></strong> (Lu et al., 2024) — 화면을 구조화 요소로 파싱해 GPT-4V에 넘기는 순수 비전 GUI 파서. ShowUI가 end-to-end로 이를 앞선 비교군.</li> <li><strong><a href="https://arxiv.org/abs/2410.05243">Navigating the Digital World as Humans Do: Universal Visual Grounding for GUI Agents (UGround)</a></strong> (Gou et al., 2024) — 대규모 데이터로 7B 보편 그라운딩 모델을 만든 작업. ShowUI-2B가 더 적은 데이터로 추월.</li> <li><strong><a href="https://arxiv.org/abs/2409.12191">Qwen2-VL: Enhancing Vision-Language Model’s Perception of the World at Any Resolution</a></strong> (Wang et al., 2024) — ShowUI의 백본. 임의 해상도 처리와 동적 토큰 수가 GUI 고해상도 입력의 토대.</li> <li><strong><a href="https://arxiv.org/abs/2404.02258">Mixture-of-Depths: Dynamically Allocating Compute in Transformer-Based Language Models</a></strong> (Raposo et al., 2024) — 토큰을 라우팅으로 건너뛰어 연산을 동적 할당하는 아이디어. ShowUI의 token selection이 직접 영감을 받은 연구.</li> <li><strong><a href="https://arxiv.org/abs/2312.08914">CogAgent: A Visual Language Model for GUI Agents</a></strong> (Hong et al., 2023) — 고해상도 GUI를 다루는 18B VLM. 큰 모델·큰 데이터의 대척점에서 ShowUI의 효율성을 부각.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="gui-agent"/><category term="vision-language-action"/><category term="multimodal"/><category term="visual-grounding"/><category term="token-selection"/><category term="computer-use"/><summary type="html"><![CDATA[스크린샷을 사람처럼 '보고' 클릭하는 GUI 에이전트. 2B 모델과 256K 데이터로 zero-shot 그라운딩 75.1%를 달성한 ShowUI의 UI-guided 토큰 선택과 interleaved vision-language-action 스트리밍을 깊게 본다.]]></summary></entry><entry xml:lang="en"><title type="html">[Paper Review] ShowUI: One Vision-Language-Action Model for GUI Visual Agent</title><link href="https://baba9811.github.io/en/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/" rel="alternate" type="text/html" title="[Paper Review] ShowUI: One Vision-Language-Action Model for GUI Visual Agent"/><published>2026-05-29T05:00:00+00:00</published><updated>2026-05-29T05:00:00+00:00</updated><id>https://baba9811.github.io/en/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent</id><content type="html" xml:base="https://baba9811.github.io/en/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/"><![CDATA[<div class="lang-toggle" style="margin-bottom: 1.5rem;"> <a href="/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/" class="btn btn-sm btn-outline-secondary">🇰🇷 한국어로 읽기</a> </div> <h4 id="metadata">Metadata</h4> <table> <thead> <tr> <th>Field</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Authors</td> <td>Kevin Qinghong Lin et al. (9 co-authors, National University of Singapore · Microsoft)</td> </tr> <tr> <td>Venue</td> <td>CVPR · 2025</td> </tr> <tr> <td>arXiv</td> <td><a href="https://arxiv.org/abs/2411.17465">2411.17465</a></td> </tr> <tr> <td>Code</td> <td><a href="https://github.com/showlab/ShowUI">showlab/ShowUI</a></td> </tr> <tr> <td>Data</td> <td>Self-collected web grounding 22K + AMEX mobile 97K + OmniAct desktop + GUIAct navigation — 256K samples total</td> </tr> <tr> <td><span style="white-space: nowrap">Review date</span></td> <td>2026-05-29</td> </tr> </tbody> </table> <h4 id="tldr">TL;DR</h4> <ul> <li>ShowUI is a GUI visual agent that operates from <strong>pixels alone</strong> — no HTML, no accessibility tree — clicking and typing the way a human does. Built on top of Qwen2-VL-2B with three design choices, a <strong>2B model with just 256K data hits 75.1% zero-shot Screenspot grounding</strong>, beating both 7B and 18B models.</li> <li>The centerpiece is <strong>UI-Guided Visual Token Selection</strong>: neighboring screenshot patches are grouped by RGB into a UI connected graph, and redundant tokens within a component are randomly skipped during training. Because positions are preserved (unlike token merging, which pools them away), grounding barely degrades while visual tokens drop ~33% and training speeds up 1.4x.</li> <li><strong>Interleaved Vision-Language-Action Streaming</strong> unifies navigation’s visual-action history and grounding’s multi-query setup into a single stream, and <strong>careful data curation plus balanced sampling</strong> yields a small-but-strong corpus. Everything is open-sourced at <a href="https://github.com/showlab/ShowUI">showlab/ShowUI</a>.</li> </ul> <h4 id="introduction">Introduction</h4> <p>Graphical User Interfaces are the primary channel through which people interact with the digital world, so “an agent that takes natural-language instructions and operates the screen” has long been a goal for workflow automation. Most early approaches were <strong>language agents</strong>: they feed text-rich metadata behind the screen — HTML, accessibility trees — to a closed-source LLM like GPT-4 and let it pick the next action. That’s powerful when clean text structure is available, but real users have no access to such a “structural oracle.” They <strong>look at the screen and move the mouse</strong>. On mobile apps, canvas-based design tools, or games, the DOM carries almost no meaning, or there is no reliable text metadata at all.</p> <p>The recent shift is toward <strong>GUI visual agents</strong> — models that take the screenshot directly and perceive and operate the UI visually, like a person. This transition comes with three domain-specific challenges. (a) <strong>Expensive visual modeling</strong>: UI screenshots are typically 2K resolution, so patchifying them explodes the token count and the long-context cost of self-attention. (b) <strong>Managing interleaved vision-language-action</strong>: actions differ across devices (mobile’s <code class="language-plaintext highlighter-rouge">PRESS HOME</code> doesn’t exist on web; <code class="language-plaintext highlighter-rouge">SCROLL</code> is two-directional on web but four-directional on mobile), and navigation accumulates multi-step screenshot-and-action history. (c) <strong>Diverse training data</strong>: GUI data spans web, mobile, and desktop with different purposes (grounding vs. navigation), and it’s unclear how to select and mix it.</p> <p>ShowUI answers all three with three targeted designs. What makes it worth reading is that the answer isn’t “scale up a bigger model” but <strong>efficiency that exploits the structure of the GUI domain</strong>. Token-compression tricks borrowed from natural-image research break down on GUIs, and this paper cleanly shows why they break and what should fix them.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig2-comparison-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig2-comparison-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig2-comparison-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig2-comparison.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 2. (Left) zero-shot Screenspot grounding accuracy vs. model size and training scale. ShowUI-2B is the smallest, trained on the least data, yet most accurate. (Right) UI-guided token selection cuts visual tokens by 33% and speeds up training 1.4x.</figcaption> </figure> <h4 id="key-contributions">Key Contributions</h4> <ul> <li><strong>UI-Guided Visual Token Selection</strong>: build a UI connected graph that groups redundant screenshot patches in RGB space, and use it as the criterion for token selection inside self-attention — reducing visual-token redundancy and compute with no extra trainable parameters.</li> <li><strong>Interleaved Vision-Language-Action Streaming</strong>: standardize actions as JSON and unify navigation’s visual-action history with grounding’s multi-query setup into one interleaved stream, so a single model handles diverse GUI scenarios.</li> <li><strong>Small, high-quality instruction-following datasets</strong>: analyze the value of each data type (static text is 40% of web data but low-information), keep only visually rich elements, and use balanced sampling to correct imbalance — training on 256K samples total.</li> <li><strong>Results</strong>: a 2B model leads zero-shot Screenspot grounding at 75.1% (ahead of 7B/18B models) and is competitive on AITW (mobile), Mind2Web (web), and MiniWob (online) navigation. Model and code fully released.</li> </ul> <h4 id="background">Background</h4> <p><strong>Two families of GUI agents.</strong> One is <strong>training-free</strong>: convert the GUI to HTML, read the accessibility tree, or textualize the screen via OCR / Set-of-Marks, then hand it to a closed-source LLM. Powerful, but reliant on expensive APIs and useless when no text oracle exists. The other is <strong>training-based</strong>: take a model pretrained on large vision-text corpora and directly train element grounding or navigation. SeeClick is the canonical example that grew grounding ability from web screenshots; ShowUI belongs to this second family.</p> <p><strong>Grounding vs. navigation.</strong> GUI tasks come in two flavors. <strong>Grounding</strong> maps an instruction (“click this button”) to a coordinate $[x, y]$ — one screenshot, one query. <strong>Navigation</strong> changes the screen over multiple steps to reach a goal — accumulated history. The two have different input structures and are usually handled separately; ShowUI fuses them into a single streaming format.</p> <p><strong>Why natural-image token compression fails on GUIs.</strong> The compute bottleneck in a Vision Transformer is the cascaded self-attention over long token sequences. For natural images, token pruning or token merging (ToMe) collapses similar patches to shrink the sequence. But GUI grounding lives or dies by <strong>exact position</strong>. Pooling tokens together (merging) erases where on the screen a token came from, so you can no longer emit a coordinate. ShowUI draws inspiration from Mixture-of-Depth (MoD): rather than merging, <strong>route past a subset of tokens, but keep the surviving tokens’ original positional embeddings</strong>. The open question is the routing criterion — and ShowUI replaces a learned router with a <strong>free prior: the UI connected graph</strong>.</p> <h4 id="method--architecture">Method / Architecture</h4> <p>ShowUI uses Qwen2-VL-2B as its backbone and adds three components. The loop is: take the task query, a predefined action space (provided as a README in the system prompt), and an initial screenshot → predict the next action → update the screen → repeat. Visual tokens and textual (action) tokens are interleaved into one sequence through self-attention.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig3-architecture-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig3-architecture-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig3-architecture-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig3-architecture.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 3. Overview of ShowUI. Starting from a task query, action space, and initial screenshot, it stacks visual observations (green) and action history (orange) into an interleaved stream and generates the next action step by step.</figcaption> </figure> <h3 id="ui-connected-graph-catching-gui-redundancy-with-rgb">UI Connected Graph: catching GUI redundancy with RGB</h3> <p>The key insight is that UI screenshots are fundamentally different from natural images. Natural images are rich in texture and pattern, so nearly every patch carries information; UIs are full of empty space, flat backgrounds, and repeated regions, so <strong>a large fraction of patches are redundant</strong>. And because UIs use consistent color schemes for readability, visually identical regions have <strong>almost exactly the same RGB values</strong>. That property is a free redundancy signal.</p> <p>ShowUI divides the screenshot into a patch grid, treats each patch as a graph node, and connects two neighboring patches when their RGB difference is below a threshold $\delta$. The screen then collapses into $K$ connected components, with $K$ far smaller than the original patch count $G_h \times G_w$. The connection test is:</p> \[\| \mathrm{RGB}(i, j) - \mathrm{RGB}(i', j') \| &lt; \delta\] <p>where $(i’, j’)$ are the right and below neighbors of $(i, j)$. Components are found efficiently with Union-Find.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Algorithm 1: Find Connected Components on UI-Graph
Input:  screenshot of size H × W, patch size c, threshold δ
Output: assignment map from patch to connected component
1: split image into G_h × G_w patches (G_h = H/c, G_w = W/c); each patch is a node
2: initialize Union-Find structure UF over all nodes
3: for each node (i, j) do
4:     for neighbors (i', j') to the right and below do
5:         if ‖RGB(i,j) − RGB(i',j')‖ &lt; δ then
6:             UF.union((i, j), (i', j'))
7: return assignment map from UF
</code></pre></div></div> <p>The nice part is that this graph sets the component count <strong>adaptively</strong> to the screen’s information content. A whitespace-heavy Google search page compresses 1296 patches into 291 components, while a text-dense Overleaf page only goes down to 986 — the compression rate isn’t a hand-tuned hyperparameter; the screen decides it.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig5-uigraph-examples-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig5-uigraph-examples-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig5-uigraph-examples-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig5-uigraph-examples.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 5. UI-connected graphs built from mobile (a–d), PC (e–f), and web (g–h) screenshots. Sparse screens (e.g. 1272→175) yield few components; text-dense screens (e.g. 1296→740) yield many. Compression adapts to information content.</figcaption> </figure> <h3 id="token-selection-vs-token-merging-keep-the-position">Token Selection vs. Token Merging: keep the position</h3> <p>With the UI connected graph in hand, we use it to shrink the tokens. Two options:</p> <ul> <li><strong>Token Merging</strong>: pool all patches in a component into a single token. The token count drops to the number of components, but pooling <strong>destroys the positional information</strong> of the individual patches — fatal for grounding.</li> <li><strong>Token Selection (ShowUI’s choice)</strong>: within a component, <strong>randomly skip</strong> a fraction of tokens during training, and leave single-patch components untouched. The surviving tokens <strong>keep their original positional embeddings</strong>, so self-attention operates over a shorter sequence while preserving the original spatial relationships. No extra trainable parameters.</li> </ul> <p>Token selection is inserted into self-attention blocks, wrapped by Pooling / Un-Pooling so tokens are reduced and restored only in some layers. During training a fixed ratio is randomly masked; at inference, selection can be turned on (faster) or off (more accurate). Either way the positional relationships across the full sequence stay consistent.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig4-token-selection-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig4-token-selection-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig4-token-selection-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig4-token-selection.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 4. (Left) the screenshot is split into 28×28 patches and patches with identical RGB are grouped into a UI connected graph. (Right) token merging collapses a component into one token and loses position, while token selection samples a subset of tokens and keeps their original spatial relationships.</figcaption> </figure> <p>For scale: a 1344×756 PC screenshot becomes roughly 5184 raw tokens at 14×14 patching, and Qwen2-VL’s 2×2 merge brings it down to 1296. Self-attention over that is still heavy, and token selection shaves it further.</p> <h3 id="interleaved-vision-language-action-streaming">Interleaved Vision-Language-Action Streaming</h3> <p>The second component is how actions weave into the other modalities. ShowUI standardizes every action as JSON:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{'action': 'CLICK', 'value': None, 'position': [0.18, 0.50]}
{'action': 'TYPE',  'value': 'las vegas', 'position': [0.54, 0.42]}
</code></pre></div></div> <p>The coordinate <code class="language-plaintext highlighter-rouge">[x, y]</code> is normalized to 0–1, unifying actions across devices and resolutions. On top of that, a <strong>README</strong> documenting each action’s usage goes into the system prompt (e.g., “<code class="language-plaintext highlighter-rouge">CLICK</code>: click an element; value not applicable; position required”). This pushes the model to <strong>read the action-space document and interpret it at test time, function-call style</strong>, rather than memorizing a fixed action set — so it can handle novel actions unseen during training.</p> <p>How actions weave with other modalities depends on the task (Figure 6):</p> <ul> <li><strong>Action-Visual Streaming (navigation)</strong>: screenshots and actions alternate in temporal order. After the $i$-th action, the $(i+1)$-th screenshot enters the queue, and the model emits the next action from it. Parts of the visual history can optionally be masked — mobile keeps screenshots because the software changes the screen substantially, while web masks them since the page stays relatively static, improving efficiency.</li> <li><strong>Action-Query Streaming (grounding)</strong>: multiple query-action pairs over a single screenshot are predicted <strong>in one forward pass</strong>. Screenshot tokens (1–2K) dwarf the queries (fewer than 10 tokens), so one-image-per-action is wasteful. Bundling many queries per screen sharply raises data utilization.</li> </ul> <p>History length is set to 2. Thanks to this streaming design, grounding and navigation — two tasks with different input structures — are trained with <strong>one model and one format</strong>.</p> <h4 id="training-objective">Training Objective</h4> <p>ShowUI’s objective is a plain <strong>language-modeling loss</strong> over action tokens on the interleaved stream, with no auxiliary loss. As Figure 6 shows, visual tokens enter only as context (conditioning), and the LM loss applies to the action (action-visual mode) or the action corresponding to a query (action-query mode). In a sequence ${\text{screenshot}, \text{action}_1, \text{screenshot}_2, \text{action}_2, \dots}$, only the action spans are next-token-prediction targets:</p> \[\mathcal{L} = - \sum_{t \in \mathcal{A}} \log p_\theta\!\left(a_t \mid a_{&lt;t}, \; v_{\le t}\right)\] <p>where $\mathcal{A}$ is the set of action-token indices, $a_t$ is the $t$-th action token, and $v_{\le t}$ is the visual observation up to that point (the compressed representation after token selection). UI-guided token selection leaves the objective untouched and only shortens the length of $v$ — no extra parameters, no extra loss term, which is the elegance of the design.</p> <h4 id="training-data-and-pipeline">Training Data and Pipeline</h4> <p>The third contribution is data. The authors refuse to “scrape everything in” and instead analyze the value of each type. On the web, the <code class="language-plaintext highlighter-rouge">static text</code> tag accounts for 40% of elements, but since most VLMs already have OCR, it carries <strong>low information value</strong>. So they keep only visually rich elements like <code class="language-plaintext highlighter-rouge">Button</code> and <code class="language-plaintext highlighter-rouge">Checkbox</code>.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab1-data-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab1-data-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab1-data-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab1-data.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 1. Overview of ShowUI's instruction-tuning data: grounding (web/mobile/desktop) and navigation (web/mobile), totaling 256K samples and 2.7M elements.</figcaption> </figure> <table> <thead> <tr> <th>Type</th> <th>Source</th> <th>Scale</th> <th>Notes</th> </tr> </thead> <tbody> <tr> <td>Grounding · web</td> <td>self-collected (PyAutoGUI parser, 22 sites)</td> <td>22K screenshots / 576K elements</td> <td>static text filtered, visual elements only (avg 26 per screenshot)</td> </tr> <tr> <td>Grounding · mobile</td> <td>AMEX</td> <td>97K screenshots / 926K elements</td> <td>element grounding + functionality</td> </tr> <tr> <td>Grounding · desktop</td> <td>OmniAct</td> <td>100 screenshots / 2K raw annotations</td> <td>augmented with GPT-4o for appearance/spatial/intention queries → 6K elements</td> </tr> <tr> <td>Navigation · web</td> <td>GUIAct</td> <td>72K / 569K (9 actions, avg len 7.9)</td> <td>one/multi-step</td> </tr> <tr> <td>Navigation · mobile</td> <td>GUIAct</td> <td>65K / 585K (5 actions, avg len 9.0)</td> <td>multi-step</td> </tr> <tr> <td><strong>Total</strong></td> <td>Diverse</td> <td><strong>256K samples / 2.7M elements</strong></td> <td> </td> </tr> </tbody> </table> <p>Scale varies wildly across types (desktop has only 100). Mixed naively, training would tilt toward the largest sets. So <strong>balanced sampling</strong> gives each batch a comparable probability of including every type, with weights (Web : Mobile : Desktop : GUIAct-Web : GUIAct-Mobile) = (1 : 1 : 1 : 1 : 1).</p> <p><strong>Training setup.</strong> Backbone Qwen2-VL-2B. LoRA (rank 64, alpha 128) on both the language model and the vision encoder trains only 4% of parameters. Instruction-tuning runs on 32 V100s, downstream adaptation on 8. Batch size 1 per GPU with gradient accumulation 2, float16, DeepSpeed Zero-2, SDPA attention, learning rate 1e-4, max visual patch 1280. The UI-graph is applied to both the vision encoder and the LM with a 0.75 masking ratio and cross-layer insertion at layer 14, history length 2. Full instruction-tuning takes about 2 days.</p> <h4 id="experiments">Experiments</h4> <h3 id="grounding--screenspot">Grounding — Screenspot</h3> <p>Screenspot is a zero-shot grounding benchmark that locates text/icon elements across mobile, desktop, and web.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab2-grounding-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab2-grounding-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab2-grounding-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/tab2-grounding.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 2. Zero-shot grounding on Screenspot. ShowUI (2B, 256K) reaches 75.1% average, ahead of UGround-7B (73.3, 1.3M) and GPT-4V-based OmniParser (73.0).</figcaption> </figure> <p>ShowUI (2B, 256K) hits <strong>75.1%</strong> average, ahead of UGround-7B (73.3, with 1.3M data), GPT-4V-based OmniParser (73.0), SeeClick-9.6B (53.4), and CogAgent-18B (47.4). The grounding-only variant ShowUI-G (119K) is nearly tied at 74.9. In short, <strong>a smaller model with less data does better</strong>.</p> <p>The breakdown is telling. (i) Text-track scores beat icon-track across all models — text grounding transfers well from web/mobile data, while icons are pure visual grounding and harder. (ii) Icons score higher on mobile, because mobile UI grounding data was more abundant than desktop/web. (iii) Mixing in navigation data doesn’t hurt grounding when balanced sampling is used.</p> <h3 id="mobile-navigation--aitw">Mobile Navigation — AITW</h3> <p>AITW (Android in the Wild) is an Android environment with 11 action types. ShowUI reaches <strong>70.0</strong> overall, ahead of Qwen2-VL-2B (67.2), SeeClick (59.3), and OmniParser (57.7). Compared to the variant without interleaved streaming (visual history), ShowUI† (68.3), <strong>visual context contributes +1.7</strong>. With a large action space (11), seeing the previous screen matters for picking the right action. It also confirms that zero-shot navigation learned from GUIAct transfers.</p> <h3 id="web-navigation--mind2web">Web Navigation — Mind2Web</h3> <p>Mind2Web probes generalization across Cross-Task, Cross-Website, and Cross-Domain splits. ShowUI’s Step Success Rate is 37.2 / 35.1 / 35.2 respectively — comparable to SeeClick — and even the 2B zero-shot variant reaches 80%+ Operation F1. The interesting finding is that <strong>visual context helps less here than on AITW</strong>: Mind2Web concentrates on visually similar single websites with only three actions, so the previous screen carries little value. And since cross-website/cross-domain are the harder splits, the authors conclude the <strong>bottleneck is UI visual perception (unseen sites/domains), not textual task understanding</strong>. The path forward is “training data with good visual domain diversity.”</p> <h3 id="online--miniwob">Online — MiniWob</h3> <p>MiniWob is an online interactive environment on a 35-task split. ShowUI scores 71.5, ahead of SeeClick (67.0) and Qwen2-VL-2B (66.8). But the zero-shot variant ShowUI-ZS is only 27.1, a large gap from fine-tuned Qwen-VL (48.4). The authors read this as: offline instruction-tuning alone doesn’t adequately address out-of-distribution errors, and an <strong>online-tailored learning strategy (e.g., RL)</strong> is needed.</p> <h4 id="analysis--ablations">Analysis / Ablations</h4> <p>The ablations target the design choices of UI-guided token selection.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig9-ablation-480.webp 480w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig9-ablation-800.webp 800w,/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig9-ablation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0017-showui-one-vision-language-action-model-for-gui-visual-agent/fig9-ablation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 9. Ablations on UI-guided token selection. (a) compression method: token merging collapses (34.7 with test-time application), random selection 56.2, UI-graph selection 70.4/64.9, close to baseline 70.8. (b) insertion layers: cross-layer wins. (c) selection ratio: 0.5 balances speed and accuracy.</figcaption> </figure> <p><strong>Compression method (9a).</strong> Baseline (no compression) is 1344.0 #Vis.Ctx at 70.8 Screenspot. Token <strong>merging</strong> shrinks training tokens to 852.8 but accuracy collapses to 42.3 (training-only) / 34.7 (test-time too) — direct evidence of lost position. <strong>Random</strong> token selection gives 65.3 / 56.2, better than merging but still costly. ShowUI’s <strong>UI-graph</strong> token selection achieves <strong>70.4 (training-only) / 64.9 (test-time too)</strong> at 947.4 tokens, essentially matching baseline 70.8. So as a criterion for “which tokens to drop,” the UI connected graph is far more effective than random. Applying it at test time loses a little to resolution (64.9), but stays far more reliable than random (56.2).</p> <p><strong>Insertion layers (9b).</strong> With the same number (14) of layers receiving token selection, <strong>cross-layer</strong> insertion (alternating selected and non-selected layers, 70.5) clearly beats early-only (68.2), late-only (67.6), and all-28 (65.7). Interestingly, reducing tokens at every layer hurts — some layers need full tokens to restore information.</p> <p><strong>Selection ratio (9c).</strong> From ratio 0 (no compression, 70.8) to 1.0 (max compression, 64.5), tokens fall 1344→762 but so does accuracy. 0.5 (947 tokens, 70.4) is the proposed sweet spot. The deployed setting uses a 0.75 masking ratio per the appendix, which corresponds to Figure 2’s “33% reduction, 1.4x speedup.”</p> <p><strong>Interleaved streaming (Figs 10–11) and data (Table 6).</strong> Multi-turn streaming progresses faster in the early warmup phase and uses data better; the visual+action+multi-turn setting consistently beats action-only. On data: (i) quality matters — OmniAct, with only 2K elements, rivals web data and improves further with GPT-4o augmentation. (ii) The self-collected 22K web data beats SeeClick’s 270K — filtering static text shrinks element size without hurting performance (evidence that static text is less informative to a VLM). (iii) Balanced sampling adds +3.7% accuracy.</p> <h4 id="limitations-and-critical-assessment">Limitations and Critical Assessment</h4> <ul> <li><strong>Author-acknowledged</strong>: ShowUI is trained mostly on offline data. Strengthening it for deeper exploration in online environments (e.g., via RL) is future work, and the MiniWob zero-shot gap (27.1 vs. 48.4 fine-tuned) exposes this limit.</li> <li><strong>Fragility of exact RGB matching</strong>: the UI connected graph assumes neighboring patches have nearly identical RGB. On screens heavy with anti-aliasing, gradients, or photos/thumbnails (image-centric shopping/media pages, app-icon grids), compression may be poor, and the sensitivity to the threshold $\delta$ isn’t deeply analyzed in the main text.</li> <li><strong>Training speedup ≠ inference speedup</strong>: the 1.4x in Figure 2 is a <strong>training</strong> gain. Turning token selection off at inference keeps accuracy (70.4) but yields no speedup, while turning it on (64.9) costs accuracy. Latency/cost inside a real agent loop is not reported.</li> <li><strong>Narrow grounding evaluation</strong>: quantitative grounding is concentrated on the Screenspot family. Robustness to varied resolutions, locales, and themes (dark mode, etc.) is only shown qualitatively.</li> <li><strong>Baseline breadth</strong>: beating larger models on grounding is impressive, but there’s no comparison of the ceiling from “scaling the same 2B backbone with more data / a bigger model,” so the efficiency gain and the data effect aren’t disentangled.</li> </ul> <h4 id="takeaways">Takeaways</h4> <ul> <li><strong>Domain structure over scale.</strong> A 2B model with 256K data beat 7B/18B GUI models on grounding. Data quality, balanced sampling, and visual-token efficiency outran brute scaling.</li> <li><strong>GUIs have a redundancy prior that natural images don’t.</strong> The RGB-based UI connected graph is a free, parameter-free compression signal unique to GUIs — the idea is “exploit the domain’s structure for free.”</li> <li><strong>In grounding, position is everything.</strong> The ablation where token merging (loses position) collapses and token selection (keeps position) survives makes plain that preserving position is non-negotiable when emitting coordinates.</li> <li><strong>Treat actions like function calls.</strong> Standardizing actions as JSON and documenting the action space via a README lets one model handle device-specific actions and interpret novel ones unseen in training.</li> <li><strong>The bottleneck in GUI navigation is visual perception, not text.</strong> What blocks cross-domain generalization is the ability to read an unseen screen, not task understanding. The next data push should be toward “visually diverse.”</li> </ul> <h4 id="installation-and-usage">Installation and Usage</h4> <p>ShowUI is open-sourced at <a href="https://github.com/showlab/ShowUI">showlab/ShowUI</a>, with checkpoints on Hugging Face as <code class="language-plaintext highlighter-rouge">showlab/ShowUI-2B</code>. Being a Qwen2-VL derivative, it loads directly with <code class="language-plaintext highlighter-rouge">transformers</code>. Below is a minimal grounding (coordinate-prediction) skeleton; for exact preprocessing helpers, follow the repo’s examples.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">Qwen2VLForConditionalGeneration</span><span class="p">,</span> <span class="n">AutoProcessor</span>

<span class="n">model</span> <span class="o">=</span> <span class="n">Qwen2VLForConditionalGeneration</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">showlab/ShowUI-2B</span><span class="sh">"</span><span class="p">,</span> <span class="n">torch_dtype</span><span class="o">=</span><span class="sh">"</span><span class="s">auto</span><span class="sh">"</span><span class="p">,</span> <span class="n">device_map</span><span class="o">=</span><span class="sh">"</span><span class="s">auto</span><span class="sh">"</span>
<span class="p">)</span>
<span class="n">processor</span> <span class="o">=</span> <span class="n">AutoProcessor</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">showlab/ShowUI-2B</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Feed a system README describing the action space + a screenshot + a query,
# and the model outputs {'action': 'CLICK', 'value': None, 'position': [x, y]}.
</span><span class="n">messages</span> <span class="o">=</span> <span class="p">[{</span>
    <span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">image</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">image</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">screenshot.png</span><span class="sh">"</span><span class="p">},</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Click the search button</span><span class="sh">"</span><span class="p">},</span>
    <span class="p">],</span>
<span class="p">}]</span>
<span class="n">text</span> <span class="o">=</span> <span class="n">processor</span><span class="p">.</span><span class="nf">apply_chat_template</span><span class="p">(</span><span class="n">messages</span><span class="p">,</span> <span class="n">add_generation_prompt</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="c1"># then tokenize with the processor -&gt; model.generate -&gt; parse position [x, y] (0–1 normalized)
</span></code></pre></div></div> <h4 id="references">References</h4> <ul> <li>Paper: <a href="https://arxiv.org/abs/2411.17465">https://arxiv.org/abs/2411.17465</a></li> <li>Code: <a href="https://github.com/showlab/ShowUI">https://github.com/showlab/ShowUI</a></li> <li>Model card: <a href="https://huggingface.co/showlab/ShowUI-2B">https://huggingface.co/showlab/ShowUI-2B</a></li> <li>CVPR 2025 Open Access: <a href="https://openaccess.thecvf.com/content/CVPR2025/html/Lin_ShowUI_One_Vision-Language-Action_Model_for_GUI_Visual_Agent_CVPR_2025_paper.html">https://openaccess.thecvf.com/content/CVPR2025/html/Lin_ShowUI_One_Vision-Language-Action_Model_for_GUI_Visual_Agent_CVPR_2025_paper.html</a></li> </ul> <h4 id="further-reading">Further Reading</h4> <ul> <li><strong><a href="https://arxiv.org/abs/2401.10935">SeeClick: Harnessing GUI Grounding for Advanced Visual GUI Agents</a></strong> (Cheng et al., 2024) — the canonical work pretraining GUI grounding from web screenshots; ShowUI’s direct comparison and data baseline.</li> <li><strong><a href="https://arxiv.org/abs/2408.00203">OmniParser for Pure Vision Based GUI Agent</a></strong> (Lu et al., 2024) — a pure-vision GUI parser that hands structured elements to GPT-4V; a baseline ShowUI beats end-to-end.</li> <li><strong><a href="https://arxiv.org/abs/2410.05243">Navigating the Digital World as Humans Do: Universal Visual Grounding for GUI Agents (UGround)</a></strong> (Gou et al., 2024) — a 7B universal grounding model trained on large data, which ShowUI-2B overtakes with far less data.</li> <li><strong><a href="https://arxiv.org/abs/2409.12191">Qwen2-VL: Enhancing Vision-Language Model’s Perception of the World at Any Resolution</a></strong> (Wang et al., 2024) — ShowUI’s backbone; any-resolution processing and dynamic token counts underpin high-resolution GUI inputs.</li> <li><strong><a href="https://arxiv.org/abs/2404.02258">Mixture-of-Depths: Dynamically Allocating Compute in Transformer-Based Language Models</a></strong> (Raposo et al., 2024) — routing past tokens to allocate compute dynamically; the direct inspiration for ShowUI’s token selection.</li> <li><strong><a href="https://arxiv.org/abs/2312.08914">CogAgent: A Visual Language Model for GUI Agents</a></strong> (Hong et al., 2023) — an 18B VLM for high-resolution GUIs; the big-model, big-data foil that highlights ShowUI’s efficiency.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="gui-agent"/><category term="vision-language-action"/><category term="multimodal"/><category term="visual-grounding"/><category term="token-selection"/><category term="computer-use"/><summary type="html"><![CDATA[A GUI agent that 'sees' screenshots like a human and clicks. A deep dive into ShowUI's UI-guided token selection and interleaved vision-language-action streaming, which hit 75.1% zero-shot grounding with a 2B model and 256K data.]]></summary></entry><entry xml:lang="ko"><title type="html">[논문 리뷰] MinT: Managed Infrastructure for Training and Serving Millions of LLMs</title><link href="https://baba9811.github.io/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/" rel="alternate" type="text/html" title="[논문 리뷰] MinT: Managed Infrastructure for Training and Serving Millions of LLMs"/><published>2026-05-19T13:00:00+00:00</published><updated>2026-05-19T13:00:00+00:00</updated><id>https://baba9811.github.io/papers/0016-mint-managed-infrastructure-for-training-and-serving-million</id><content type="html" xml:base="https://baba9811.github.io/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/"><![CDATA[<div class="lang-toggle" style="margin-bottom: 1.5rem;"> <a href="/en/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/" class="btn btn-sm btn-outline-secondary">🇺🇸 Read in English</a> </div> <h2 id="메타정보">메타정보</h2> <table> <thead> <tr> <th>항목</th> <th>내용</th> </tr> </thead> <tbody> <tr> <td>저자</td> <td>Mind Lab (Andrew Chen et al., 13명 core contributors + 60명 team)</td> </tr> <tr> <td>학회</td> <td>arXiv preprint · 2026</td> </tr> <tr> <td>arXiv 또는 DOI</td> <td><a href="https://arxiv.org/abs/2605.13779">2605.13779</a></td> </tr> <tr> <td>Code</td> <td><a href="https://github.com/MindLab-Research/mint-cookbook">MindLab-Research/mint-cookbook</a> — cookbook recipes (서비스 코드는 비공개)</td> </tr> <tr> <td>데이터</td> <td>Qwen3 패밀리 (0.6B/4B 그리고 30B-A3B / 235B-A22B MoE), Moonlight-16B-A3B, Kimi K2 1.04T, GLM-5/5.1. 학습 레시피는 SFT (FinEval / FinGPT 스위트), DPO (chat pairs), GRPO (DAPO-AIME24), LawBench AutoResearch</td> </tr> <tr> <td><span style="white-space: nowrap">리뷰 일자</span></td> <td>2026-05-19</td> </tr> </tbody> </table> <h2 id="tldr">TL;DR</h2> <ul> <li>학습된 정책마다 풀-체크포인트를 복사하는 기존 인프라는 후처리 (post-training) 가 다정책 (multi-policy) RL 로 진화한 시점에서 더 이상 확장되지 않는다. MinT 는 베이스 모델은 한 번만 상주시키고 LoRA <strong>adapter revision</strong> 만 학습-서빙 경계를 오가는 단위로 삼는 관리형 인프라다.</li> <li>세 축의 확장: <strong>Scale Up</strong> (1T급 MoE 와 MLA/DSA 까지 LoRA RL 가능), <strong>Scale Down</strong> (어댑터-only 핸드오프로 머지 경로 대비 4B 모델 18.3×, 30B MoE 모델 2.85× 빠른 step. 동일 베이스 할당에서 concurrent GRPO 가 wall time 1.77× / 1.45× 단축), <strong>Scale Out</strong> (10^6 규모 어드레서블 정책 카탈로그를 CPU 캐시·GPU 배치 워킹셋과 분리).</li> <li>MoE LoRA 텐서를 packed serving representation 으로 묶어 37,248 → 672 텐서로 줄이면 live engine load 가 8.5-8.7× 빨라진다. 콜드 로드를 <em>서빙 워크</em> 로 명시적으로 다루는 게 핵심 기여 중 하나.</li> <li>“체크포인트가 아니라 어댑터 revision 을 학습-서빙 경계의 일급 객체로 본다” 는 추상화 한 줄에서 정책 lifecycle, MoE 라우터 replay, DSA 보정, 캐시 tier 가 모두 정렬된다.</li> </ul> <h2 id="소개-introduction">소개 (Introduction)</h2> <p>후처리 (post-training) 는 한때 사전학습 끝에 한 번 붙이는 단계였지만, 2024-2026 년 사이 다정책·다단계 RL workload 로 진화했다. 프론티어 모델 보고서들은 reasoning, coding, tool use, agentic 평가가 단일 SFT 한 번으로 끝나지 않고, <em>수십~수백 개의 정책 변종</em> — 태스크별 fine-tune, 제품 브랜치, 실험 버전, 테넌트별 어댑터, 롤백 포인트 — 을 동시에 굴리는 형태로 굳어졌음을 보여 준다.</p> <p>기존 인프라가 이런 워크로드를 직격당하는 지점은 명확하다. 어떤 정책 변종이든 <em>결과물</em> 이 풀 fine-tuned 체크포인트라면, 학습→서빙 경계마다 모델 전체를 옮겨야 한다. 1T급 베이스에서는 30 GB 짜리 LoRA 어댑터도 8 GB 풀 체크포인트와 자릿수가 다르다. 머지된 LoRA 체크포인트는 학습 메모리는 줄여 주지만 결국 어댑터를 베이스에 합쳐 <em>합쳐진 풀 체크포인트</em> 를 옮기므로, 학습-서빙 경계의 byte 부담은 그대로다.</p> <p>MinT (MindLab Toolkit) 는 이 경계를 통째로 바꾼다. 베이스 모델은 학습/롤아웃/서빙 워커에 한 번 상주하고, 그 위에 학습된 LoRA 만 <em>어댑터 revision</em> (정해진 학습 step 에서 freeze 된, serving tensor layout 의 LoRA 스냅샷) 으로 export 한다. 정책 lifecycle 의 모든 단계 — rollout, update, export, evaluation, serving, rollback — 가 한 어댑터 revision 을 지목하면서 진행된다. 이 글은 그 추상화가 어떻게 1T MoE 까지 검증된 학습 곡선, 어댑터-only 핸드오프의 18.3× 가속, 10^6 규모의 정책 카탈로그를 한 시스템 디자인으로 묶는지 정리한다.</p> <h2 id="핵심-기여-key-contributions">핵심 기여 (Key Contributions)</h2> <ul> <li><strong>Adapter revision 을 학습-서빙 단위로 정착시킨 lifecycle.</strong> export → rollout → evaluation → serving → rollback 이 <em>exported LoRA file</em> 하나로 일관되게 흐른다. 정책 record 는 그 file 의 정책 메타데이터·rollout record·라우터 정보를 관리하는 service-side state 이다.</li> <li><strong>대규모 multi-LoRA RL 학습.</strong> 단일 베이스를 상주시키고 그 위에서 여러 LoRA 정책을 <em>time-slice</em> 한다. Single-worker PEFT 와 분산 Megatron 학습 경로를 모두 지원하며, 235B-A22B GRPO 와 Kimi K2 1.04T countdown-task RL 까지 실측 곡선이 있다.</li> <li><strong>Policy-population multi-LoRA 서빙.</strong> vLLM 엔진을 공유 베이스로 두고 어댑터 revision 을 attach. 어드레서블 카탈로그·CPU 캐시·GPU 배치를 <em>세 개의 독립적인 capacity dimension</em> 으로 분리하고, MoE 어댑터의 작은-텐서 폭주를 packing 으로 줄여 live load 를 8.5-8.7× 가속.</li> <li><strong>공개 재현 경로.</strong> Tinker-compatible API 와 <code class="language-plaintext highlighter-rouge">mint-cookbook</code> 레시피 (SFT, preference optimization, rollout-based RL, AutoResearch) 를 통해 같은 어댑터 lifecycle 위에서 한 set 의 예제를 재현 가능.</li> </ul> <h2 id="관련-연구--배경-지식">관련 연구 / 배경 지식</h2> <p>본 논문이 디딘 자리는 <em>LoRA 학습</em> 과 <em>LoRA 서빙</em> 이 각각 자체 생태계를 갖춘 뒤, 양쪽을 하나의 lifecycle 로 묶는 <em>서비스</em> 단계다.</p> <ul> <li><strong>LoRA 와 그 변종.</strong> Hu et al. 의 LoRA 는 베이스 가중치를 freeze 하고 저-rank 행렬만 학습한다. AdaLoRA 는 rank budget 을 layer 간에 동적으로 분배, QLoRA 는 4-bit quantized base 위에 LoRA 를 얹어 메모리를 또 한 자릿수 줄인다. “LoRA Without Regret” (Schulman &amp; Thinking Machines Lab, 2025) 는 <em>LoRA 가 post-training 에서 풀 fine-tune 에 근접한 품질을 낼 수 있다</em> 는 점을 실증해, LoRA 를 메모리 절감 수단이 아닌 <em>service-level unit</em> 으로 보는 본 논문의 전제를 정당화한다.</li> <li><strong>Multi-LoRA 학습 인프라.</strong> mLoRA (Ye et al., 2024) 는 한 배치 안에서 여러 어댑터를 동시 fine-tune. MinT 는 RL workload 가 정책당 rollout token 이 이미 많아 <em>시간 슬라이싱</em> 으로도 충분히 efficient 하다는 점을 활용해 다른 길을 택한다.</li> <li><strong>Multi-LoRA 서빙.</strong> Punica (Chen et al., 2024), S-LoRA (Sheng et al., 2023), dLoRA (Wu et al., 2024), LoRAServe (Jaiswal et al., 2025) 는 <em>이미 존재하는 어댑터</em> 를 한 베이스 위에서 어떻게 라우팅·메모리 관리할지 다룬다. MinT 는 그 위에 <em>서빙 카탈로그 ↔ 학습 export</em> 의 lifecycle 을 얹어, 콜드 미스를 “scheduled service work” 로 만든다.</li> <li><strong>RL execution frameworks.</strong> HybridFlow/verl, AReaL, OpenRLHF, ROLL, StreamRL, Laminar 등은 rollout 스케줄링·실패 격리·colocated/disaggregated 실행 같은 <em>공통 RL 시스템 문제</em> 를 다룬다. MinT 는 그 자리에 <em>LoRA-specific service state</em> (어댑터 revision, optimizer state, rollout record, MoE route record, DSA 보정 메타데이터) 를 추가한다.</li> <li><strong>Training-serving consistency.</strong> 최근 R3 (Ma et al., 2025) 와 IcePop (Ling Team et al., 2025) 는 MoE 라우터 미스매치·token-level 확률 mismatch 가 RL 안정성을 깨뜨리는 결함임을 정량화했다. MinT 는 그 보정 정책을 rollout record 의 일부로 저장한다.</li> </ul> <h2 id="방법--아키텍처-상세">방법 / 아키텍처 상세</h2> <h3 id="시스템-개관">시스템 개관</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig1-mint-overview-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig1-mint-overview-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig1-mint-overview-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig1-mint-overview.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1: MinT 의 사용자 의도 → 서비스 → 공유 베이스 위 정책 집합 → 인프라 컴포넌트 라는 4-계층 view. 스케줄러·fault tolerance·어댑터 lifecycle·serving residency 는 서비스 인터페이스 뒤에 숨어 있다.</figcaption> </figure> <p>사용자가 의도 (base model · data + reward · LoRA RL recipe · evaluate + serve 타깃) 를 던지면, MinT 는 그것을 <em>큐잉된 작업 · 정책 record · exported revision</em> 으로 변환한다. 베이스 모델은 학습/롤아웃/서빙 워커에 <em>상주</em> 하며, 그 위에서 정책 $r_1, r_2, \ldots$ 의 집합이 train / evaluate / serve / rollback 의 모든 단계를 같은 어댑터 lifecycle 로 통과한다. 아래쪽 박스 — 스케줄러, fault tolerance, adapter lifecycle, serving residency — 가 인프라의 <em>복잡도 hidden underneath</em> 다.</p> <h3 id="학습-서빙-경계-무엇이-옮겨지는가">학습-서빙 경계: 무엇이 옮겨지는가</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig2-training-serving-paths-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig2-training-serving-paths-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig2-training-serving-paths-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig2-training-serving-paths.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 2: 세 가지 학습→서빙 경로. Full fine-tuning 은 전체 가중치 $W$ 를 변종마다 옮기고, LoRA merge 는 어댑터를 베이스에 합쳐 합쳐진 체크포인트 $W'$ 를 옮긴다. MinT 는 베이스 $W$ 를 양쪽 워커에 상주시키고 어댑터 $L\_i$ 만 옮긴다.</figcaption> </figure> <p>이 단순한 그림이 본 논문의 추상화 전체를 함축한다. <em>full fine-tuning</em> 은 학습 워커가 만든 풀 가중치 $W_1, W_2, \ldots, W_n$ 을 그대로 추론 워커로 이동한다. <em>LoRA merge</em> 는 학습 시 메모리는 줄여 주지만, 서빙으로 보내려면 어댑터 $L_i$ 를 다시 베이스에 머지해 새 풀 체크포인트 $W’_i$ 를 만들어 옮긴다. <em>MinT multi-LoRA</em> 는 베이스 $W$ 를 양 워커 모두 <em>상주</em> 시키고 어댑터 $L_i$ 만 보낸다. 어댑터 byte 만 byte size 가 줄어드는 게 아니다 — 추론 엔진이 베이스를 <em>이미</em> 들고 있어 새 어댑터를 슬롯에 admit 만 하면 된다는 점에서 <em>load 단계 자체가 달라진다</em>.</p> <h3 id="어댑터-revision-vs-정책-record">어댑터 revision vs 정책 record</h3> <p>이 분리는 디자인에서 가장 중요한 결정 중 하나이다.</p> <ul> <li><strong>Adapter revision</strong>: 어떤 학습 step 에서 freeze 되고 serving tensor layout 으로 export 된 LoRA 의 <em>고정된 스냅샷</em>. 학습 체크포인트가 아니다 — optimizer state 와 rank-local training file 은 제거된 상태로, PEFT 어댑터 file 한 개만 남는다.</li> <li><strong>Policy record</strong>: 그 payload 를 <em>조회·재로드·롤백 가능</em> 하게 만드는 service-side state. 호환 베이스 버전, LoRA rank/target modules, 최근 학습 체크포인트 위치, rollout records, exported revisions 목록을 모두 들고 있다.</li> </ul> <p>이 둘이 한 객체로 묶여 있다면 학습 중 모든 update 가 서빙 카탈로그를 oscillate 시켰을 것이다. 분리하면 <em>훈련 진행 중에도</em> 안정된 revision 이 카탈로그에 남고, 그 revision 만 evaluation / serving 이 선택한다.</p> <h3 id="시스템-디자인-서비스-plane-vs-컴퓨트-plane">시스템 디자인: 서비스 plane vs 컴퓨트 plane</h3> <p>MinT 는 <em>서비스/제어 plane</em> (API, queue, policy lookup, resource admission, operation state) 과 <em>컴퓨트 plane</em> (PEFT/Megatron trainers, vLLM samplers) 을 분리한다. 그 사이를 잇는 durable storage 는 정책 record, 체크포인트, rollout record, exported adapter file 네 종류를 갖는다. 본 논문이 강조하는 <em>operation visibility</em> 는 모든 client call 이 pollable operation id 를 받고, 워커가 결과 file 을 <em>모두</em> 쓰고 메타데이터를 commit 한 뒤에야 그 결과가 client 에 visible 해진다는 invariant 이다 — 부분 file 이 카탈로그에 새지 않게 보장한다.</p> <h3 id="scale-up-1t-moe-까지-lora-rl">Scale Up: 1T MoE 까지 LoRA RL</h3> <p>대형 베이스에서 LoRA 가 안 깨지려면 <em>베이스의 분산 배치</em> 와 <em>어댑터 export</em> 가 정합해야 한다. MinT 는 Megatron 학습 그룹에서 dense module LoRA 는 tensor-parallel shard 를 따라가고, MoE 의 expert LoRA 는 expert id 로 keyed 되어 EP shard 가 들고 있는 expert 만 학습한다. Shared-expert LoRA 는 EP shard 당 한 번씩 저장되어 export 시 deduplicate 된다.</p> <h4 id="moe-라우터-replay-r3-style">MoE 라우터 replay (R3-style)</h4> <p>MoE RL 은 한 가지 함정이 있다. <em>rollout 시 token 을 처리한 expert path</em> 와 <em>학습 시 그 token 을 scoring 하는 expert path</em> 가 어긋나면, on-policy 가정이 깨진다. MinT 는 rollout record 에 selected expert ids 를 저장하고, 학습 backend 가 그 id 를 자기 EP layout 으로 mapping 할 수 있으면 그 route 를 replay 한다. Mapping 불가능한 token (id 빠짐, 또는 layout 변경) 은 정책-그래디언트 term 에서 mask 처리한다.</p> <h4 id="sparse-attention-provenance-dsa">Sparse-attention provenance (DSA)</h4> <p>GLM-5 / GLM-5.1 의 dynamic sparse attention 도 비슷한 채널을 만든다. 인덱서·top-$k$ 가 어떤 token 을 sparse attention 에 포함시킬지 결정하므로, 수치 차이가 token set 을 바꾸면 rollout/training mismatch 가 생긴다. MinT 는 IcePop-style rollout 보정을 사용한다: 학습/rollout 확률 비율이 신뢰 대역을 벗어나면 그 token 에 importance weight 0 을 부여해 불안정한 scoring term 을 <em>필터링</em> 한다. 정확한 token 셋을 재구성하지는 않지만, 불안정한 그래디언트를 제거해 안정성을 회복하는 절충안이다.</p> <h3 id="scale-down-어댑터-only-핸드오프">Scale Down: 어댑터-only 핸드오프</h3> <h4 id="핸드오프-byte">핸드오프 byte</h4> <p>Qwen3-4B 에서 rank-32 PEFT 어댑터 file 은 264,310,274 byte (약 252 MiB). 같은 4B 베이스의 bf16 체크포인트는 약 8.0 GB. 어댑터는 <em>베이스 가중치 floor 의 약 3.3%</em>. Rank-1 + tighter target module 이면 약 0.10% 까지 떨어진다 — abstract 의 “1% 이하” 주장이 보수적이라는 의미다.</p> <h4 id="동시-multi-policy-학습-시간-슬라이싱">동시 multi-policy 학습 시간 슬라이싱</h4> <p>한 트레이너 actor 가 베이스 모델 $W$ 를 상주시킨 채 LoRA A → switch → LoRA B → switch → LoRA A 순서로 시간 슬라이스한다 (논문 figure 5). 정책 switch 마다 이전 정책의 LoRA tensor·optimizer state·gradient·rollout record 를 정책 record 로 commit 하고, 다음 정책의 state 를 restore. 베이스는 GPU 메모리에 그대로 남아 <em>정책 switch 비용이 베이스 reload 비용을 포함하지 않는다</em>.</p> <h4 id="동시-학습-타임라인-측정">동시 학습 타임라인 측정</h4> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig7-concurrent-multi-lora-timeline-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig7-concurrent-multi-lora-timeline-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig7-concurrent-multi-lora-timeline-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig7-concurrent-multi-lora-timeline.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 7: 동일한 베이스 모델 할당 하에서 3개의 GRPO 정책을 순차 (Seq.) vs MinT concurrent 로 실행한 타임라인. Qwen3-4B 는 51.4 → 28.9 분 (1.77× 가속, 22.4 분 절약), Qwen3-30B 는 168.8 → 116.8 분 (1.45× 가속, 52.0 분 절약). 두 경우 모두 peak memory 는 동일 (65.6 GiB / 68.0 GiB).</figcaption> </figure> <p>핵심은 peak memory 가 변하지 않으면서 wall time 만 줄어든다는 점이다. 순차 schedule 의 <em>idle gap</em> 에 다른 정책의 rollout/update/evaluation 단계가 채워 들어가 self-pipelining 이 되는 구조다.</p> <h4 id="어댑터-only-vs-머지-and-load-핸드오프">어댑터-only vs 머지-and-load 핸드오프</h4> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig8-adapter-handoff-vs-merge-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig8-adapter-handoff-vs-merge-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig8-adapter-handoff-vs-merge-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig8-adapter-handoff-vs-merge.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 8: 한 학습 step 후 sampler 에게 새 정책을 넘기는 두 경로의 wall time. Qwen3-4B 에서 어댑터-only 4.1s vs merge 74.9s (18.3× 빠름). Qwen3-30B 에서 어댑터 208.7s vs merge 595.5s (2.9× 빠름).</figcaption> </figure> <p>표 4 (논문 본문) 는 더 자세한 분해를 준다: Qwen3-4B 의 경우 어댑터 path 는 <em>materialization/load 0.036s + cold first sample 4.114s</em>, 머지 path 는 <em>materialization/load 71.820s + cold first sample 55.704s</em>. 머지 path 의 71.8 초가 <em>온전히</em> 어댑터를 베이스에 합치고 새 풀 체크포인트를 sampler 에 admit 하는 데 쓰인다. 같은 모델 추론 throughput (warm 15,568 vs 20,595 tok/s) 을 비교하면 머지가 <em>더 빠른 throughput</em> 을 보이긴 하지만, 그 차이는 sampling 절약분이 핸드오프 비용을 회수할 만큼 짧은 run 에서는 무의미하다.</p> <h3 id="scale-out-정책-집합-서빙">Scale Out: 정책 집합 서빙</h3> <h4 id="세-cache-tier">세 cache tier</h4> <table> <thead> <tr> <th>Tier</th> <th>Scale</th> <th>Lifetime</th> <th>Promotion / eviction</th> </tr> </thead> <tbody> <tr> <td>Addressable catalog</td> <td>$10^3 - 10^6$ entries</td> <td>Durable (control plane)</td> <td>Adapter export 시 promote, manual retire</td> </tr> <tr> <td>CPU adapter cache</td> <td>엔진당 수백 개</td> <td>Actor run lifetime</td> <td>Router / cache-miss load 시 promote, LRU evict</td> </tr> <tr> <td>GPU batch</td> <td>$\le 64$ distinct</td> <td>One decoding step</td> <td>Batch scheduler 가 promote, step 끝에 release</td> </tr> </tbody> </table> <p>표 2 (논문) 가 명시하듯 <em>어드레서블 카탈로그 크기</em> 와 <em>동시 GPU 거주</em> 는 다른 capacity dimension 이다. $10^6$ 은 <em>얼마나 많은 정책 이름이 resolvable 한가</em> 지, <em>얼마나 많이 동시에 GPU 에 살아 있나</em> 가 아니다.</p> <h4 id="warm-vs-cold-경로-측정">Warm vs cold 경로 측정</h4> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig13-warm-vs-cold-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig13-warm-vs-cold-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig13-warm-vs-cold-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig13-warm-vs-cold.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 13: 좌: CPU 캐시된 요청 p95 21.35s vs cache-miss 199.81s. 중: 카탈로그가 1k → 100k 로 커져도 warm/cold 두 regime 은 안정. 우: 16 개 distinct cold adapter 는 1.36 s/adapter 의 staircase 형태로 직렬화되어 23.27 초까지 누적.</figcaption> </figure> <p>세 panel 의 메시지는 명확하다 — 카탈로그 사이즈는 <em>이름 해결 (name resolution)</em> 의 scale 이지 <em>실행</em> 의 scale 이 아니다. 동시에 도착한 cold adapter 16 개는 <em>동시 로드되지 않고</em> 순차로 staircase 처럼 누적되므로, cold-load 자체를 backpressure 와 deduplication 이 있는 <em>scheduled service work</em> 로 다루는 게 정답이다.</p> <h4 id="moe-의-작은-텐서-폭주">MoE 의 작은-텐서 폭주</h4> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab7-packed-moe-lora-loading-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab7-packed-moe-lora-loading-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab7-packed-moe-lora-loading-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab7-packed-moe-lora-loading.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 7: Packed MoE LoRA representation 의 효과. File size 는 거의 같지만 (110.75 → 105.58 MB, 1.05×) 텐서 객체가 37,248 → 672 로 55.4× 줄어, read tensors 54.8× / build loader objects 29.5× / live engine load 8.5-8.7× 가속.</figcaption> </figure> <p>MoE LoRA 가 cold path 를 <em>눈에 띄게</em> 만드는 이유다. rank-1 어댑터도 expert tensor 수천 개로 fragment 되어, 학습-쪽 byte 는 작아도 서빙-쪽 Python object 생성·loader 등록 비용이 폭증한다. Packing 은 <em>선언된 byte</em> 는 거의 그대로 두면서 <em>텐서 객체</em> 만 한 자릿수 줄여 cold-load staircase 의 단위 비용을 떨어뜨린다.</p> <h2 id="학습-목표--손실-함수">학습 목표 / 손실 함수</h2> <p>본 논문은 새 loss 를 제안하지 않는다 — 기존 SFT loss, DPO reward-margin, GRPO (또는 PPO 변종) 의 group relative policy optimization 을 LoRA 어댑터에 적용할 뿐이다. 다만 <em>MoE RL 에서 정확히 어느 logit 으로 scoring 하는가</em> 가 안정성에 결정적이므로, 그 식별이 정책-그래디언트의 형태를 결정한다:</p> \[\nabla_\theta J(\theta) = \mathbb{E}_{\tau \sim \pi_{\text{rollout}}} \left[ \sum_t \nabla_\theta \log \pi_\theta(a_t | s_t; \,\text{route}_t) \cdot A_t \right]\] <p>여기서 $\text{route}_t$ 는 rollout 시 expert path 다. MinT 는 이를 rollout record 에 저장하고 학습 시 가능하면 replay, 불가능하면 그 token 을 mask. DSA mismatch 보정 (IcePop) 의 경우:</p> \[\begin{aligned} w_t &amp;= \mathbb{1}\!\left[\, \rho_t \in [\rho_{\text{lo}}, \rho_{\text{hi}}] \,\right], \\ \rho_t &amp;= \frac{\pi_\theta^{\text{train}}(a_t | s_t)}{\pi_\theta^{\text{rollout}}(a_t | s_t)} \end{aligned}\] <p>신뢰 대역 $[\rho_{\text{lo}}, \rho_{\text{hi}}]$ 를 벗어난 token 은 $w_t = 0$ 으로 그래디언트에서 빠진다. 정확한 sparse-attention token set 을 재구성하지 않는 <em>필터링</em> 보정이라는 점이 핵심이다.</p> <h2 id="학습-데이터와-파이프라인">학습 데이터와 파이프라인</h2> <table> <thead> <tr> <th>항목</th> <th>값</th> </tr> </thead> <tbody> <tr> <td>Dense 모델 SFT</td> <td>Qwen3-4B, FinEval / FinGPT 스위트 (Fineval, FPB, FiQA-SA, TFNS, NWGI)</td> </tr> <tr> <td>Dense 모델 DPO</td> <td>Qwen3-4B, chat pairs</td> </tr> <tr> <td>Dense 모델 GRPO</td> <td>Qwen3-8B base, DAPO-AIME24</td> </tr> <tr> <td>MoE GRPO (30B)</td> <td>Qwen3-30B-A3B, AIME24</td> </tr> <tr> <td>MoE GRPO (235B)</td> <td>Qwen3-235B-A22B, AIME24, 32-GPU Megatron (TP=4, EP=8, PP=1) + 16-GPU TP=16 vLLM 서빙</td> </tr> <tr> <td>1T countdown-task RL</td> <td>Kimi K2 (32.6B active), 64-GPU H800</td> </tr> <tr> <td>Serving 실험</td> <td>Qwen3-30B rank-1 MoE LoRA, 4-GPU TP=4 serving actor, prompt len 1024, max output 64</td> </tr> <tr> <td>Cookbook</td> <td>LawBench AutoResearch (28 experiments, 6 kept), DAPO-AIME24, chat-DPO, FinGPT</td> </tr> </tbody> </table> <h2 id="실험-결과">실험 결과</h2> <h3 id="scale-down-핸드오프-비용">Scale Down: 핸드오프 비용</h3> <table> <thead> <tr> <th>Model</th> <th>Path</th> <th>Checkpoint size</th> <th>Materialization or load</th> <th>Cold first sample</th> <th>Sample tok/s (total/warm)</th> </tr> </thead> <tbody> <tr> <td>Qwen3-4B</td> <td>Adapter</td> <td>252 MiB</td> <td>0.036 s</td> <td>4.114 s</td> <td>15,568 / 15,567</td> </tr> <tr> <td>Qwen3-4B</td> <td>Merge</td> <td>8.061 GB</td> <td>71.820 s</td> <td>55.704 s</td> <td>4,697 / 20,595</td> </tr> <tr> <td>Qwen3-30B</td> <td>Adapter</td> <td>1.692 GB</td> <td>46.455 s</td> <td>117.304 s</td> <td>1,874 / 5,700</td> </tr> <tr> <td>Qwen3-30B</td> <td>Merge</td> <td>61.084 GB</td> <td>402.245 s</td> <td>156.074 s</td> <td>1,573 / 6,904</td> </tr> </tbody> </table> <p>머지 path 는 <em>warm sampling throughput</em> 이 더 높지만 (4B: 20,595 vs 15,567 tok/s) 핸드오프-+-cold-first-sample 합이 너무 커서 짧은 RL step 에서는 어댑터 path 가 압도적으로 유리하다.</p> <h3 id="scale-up-학습-곡선">Scale Up: 학습 곡선</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig10-moe-rl-curves-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig10-moe-rl-curves-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig10-moe-rl-curves-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig10-moe-rl-curves.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 10: MoE RL 학습 곡선. Qwen3-30B-A3B 와 235B-A22B 는 AIME24 mean@1, Kimi K2 1T 는 countdown-task reward. 30B 는 ~0.6 → 0.9 mid-band, 235B 는 peak 0.967 mean@1 (AIME24 포화에 가까움).</figcaption> </figure> <p>세 panel 의 메시지: <em>같은 LoRA RL 경로</em> 가 30B sparse → 235B-A22B Hopper deployment → 1T-class base 까지 학습-서빙 핸드오프를 <em>변경 없이</em> 통과한다는 점이다. Dense 결과는 Table 5 (논문) 로 따로 보고 — SFT 5개 FinGPT 벤치마크 모두에서 약 +20 ~ +36 정확도 (예: FinEval 0.4226 → 0.7811), DPO chat pairs reward margin -0.03 → 30.88, GRPO AIME24 train accuracy 0.11 → 0.47 (best raw 0.568 at step 76).</p> <h3 id="scale-up-검증된-모델-패밀리">Scale Up 검증된 모델 패밀리</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab1-model-family-support-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab1-model-family-support-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab1-model-family-support-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab1-model-family-support.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 1: MinT 스택이 현재 검증한 세 가족. Qwen3 (dense + MoE, 0.6B-235B-A22B), Moonlight &amp; Kimi K2 (MLA MoE, up to 1.04T), GLM-5/5.1 (MLA, DSA, MTP, MoE). 모델별로 어떤 어댑터/서빙 경로가 검증됐는지 명시.</figcaption> </figure> <h3 id="scale-out-정책-집합-서빙-1">Scale Out: 정책 집합 서빙</h3> <table> <thead> <tr> <th>Metric</th> <th>Measurement</th> </tr> </thead> <tbody> <tr> <td>카탈로그 sweep</td> <td>1k → 100k entries 모두 warm regime 유지</td> </tr> <tr> <td>CPU 캐시 (512-adapter hotset)</td> <td>369 / 550 loaded adapters</td> </tr> <tr> <td>Same-batch GPU active</td> <td>16, 32, 64 모두 0 error</td> </tr> <tr> <td>Cold-cache p95</td> <td>199.81 s (warm 21.35 s 의 ~9.4×)</td> </tr> <tr> <td>Cold staircase (16 distinct)</td> <td>1.375 → 23.267 s, ~1.36 s/adapter</td> </tr> <tr> <td>Packed MoE LoRA live load</td> <td>0.156-0.164 s (vs 1.36-1.39 s, 8.5-8.7× 가속)</td> </tr> </tbody> </table> <p>Appendix table 13 은 이를 <em>2300-distinct-adapter active wave</em> 가정 하에서 $10^6$-entry 카탈로그로 외삽한 fleet-level sizing sketch 를 준다 (warm distinct concurrency 36 engines / 144 GPUs, cold-load isolation 72 engines / 288 GPUs).</p> <h2 id="결과-분석--ablation">결과 분석 / Ablation</h2> <p>세 axis 의 측정이 <em>각자 다른 시스템 문제</em> 를 분리해 보여 준다는 점이 가치다.</p> <ul> <li><strong>Scale Down</strong> 의 18.3× / 2.85× 가속은 <em>어댑터 byte</em> 자체에서 오는 게 아니다. Table 4 를 보면 어댑터 path 의 cold first sample (4.1s, 117.3s) 은 머지 path (55.7s, 156.1s) 와 같은 자릿수다. 격차의 본체는 <em>materialization / load</em> 단계 (4B: 71.8s vs 0.036s, 30B: 402.2s vs 46.5s) 다. 즉 진짜 saving 은 “어댑터 file 이 작다” 가 아니라 “베이스가 이미 sampler 에 있어서 merge+load 단계 자체가 사라진다” 는 것.</li> <li><strong>Concurrent multi-LoRA</strong> 의 1.77× / 1.45× 는 <em>idle gap 채우기</em> 다. 동일 peak memory 에서 wall time 만 단축되는 게 그 증거 — 더 큰 베이스 슬롯을 잡아 throughput 을 늘린 게 아니다.</li> <li><strong>Packed MoE LoRA</strong> 의 8.5-8.7× live load 가속이 <em>file size 1.05× 감소</em> 에서 오지 않는다는 점이 핵심 해석이다. Read tensors 54.8× / build loader objects 29.5× 가속이 dominates — 즉 보틀넥은 byte transfer 가 아니라 <em>작은 텐서 객체의 Python-side fanout</em> 이다.</li> <li><strong>Carousel 효과 부재.</strong> 카탈로그가 1k → 100k 로 100배 커져도 warm p95 는 (20.89s → 12.16s → 12.12s) 로 <em>오히려 약간 감소</em>. 이름 해결과 로컬 캐시가 <em>완전히 분리됐기</em> 때문에 카탈로그 크기가 라우팅에 비용을 추가하지 않는다는 강한 증거.</li> </ul> <h2 id="한계와-비판적-평가">한계와 비판적 평가</h2> <ul> <li><strong>베이스 모델 호환성이 lifecycle 의 전제.</strong> “어댑터 revision 만 옮긴다” 는 추상화는 <em>base 가 binary 동일</em> 일 때만 성립한다. Base 가 fix 되는 동안 어댑터 catalog 가 누적되는 것은 강력하지만, base 자체를 swap 하는 시나리오 — 새 사전학습 release, quantization 변경, MoE expert 재배치 — 는 lifecycle 의 약점이다. 본 논문은 그 시나리오를 다루지 않는다 (rollback 은 어댑터 revision 끼리만).</li> <li><strong>DSA 보정의 정확성 한계.</strong> IcePop-style filter 는 <em>불안정한 token 을 mask</em> 할 뿐, sparse-attention 의 정확한 token set 을 복원하지 않는다. 즉 학습이 실제로 어떤 attention pattern 으로 update 됐는지에 대한 ground truth 는 없다. 본 논문도 이를 인정한다 (“It does not prove that training used the exact sparse-attention token set selected by the inference engine”). 안정성을 위한 절충안이지 완전한 정합성이 아니다.</li> <li><strong>단일-vendor 검증.</strong> Qwen, Moonlight/Kimi, GLM — 모두 중국계 frontier 모델로, 학습/서빙 패턴이 비슷할 가능성이 높다. Llama-3, Mistral, Claude family 같은 다른 family 에서 같은 lifecycle 가 어떤 마찰을 겪는지는 미지수.</li> <li><strong>콜드 staircase 의 절댓값.</strong> “1.36 s/adapter” 는 작은 듯 보이지만 16 distinct cold adapter 가 동시에 도착하면 23 초 staircase 다. 100 distinct 동시 도착 시나리오 (예: 새 배포 직후 traffic spike) 의 sizing 은 Appendix 외삽에 의존하며 실측이 아니다.</li> <li><strong>Cookbook coverage.</strong> Public 재현 경로가 cookbook 으로 묶여 있다는 건 좋지만, <em>실제 서비스 코드</em> (스케줄러, queue, policy lookup) 는 비공개로 보인다 — <code class="language-plaintext highlighter-rouge">mint-cookbook</code> 은 recipe 묶음일 뿐 system 자체가 아니다. 따라서 inn-house 재현은 cookbook + Tinker API 의 조합으로만 가능.</li> <li><strong>Cost 보고 부재.</strong> “1.77× 빠르다” 는 wall-time 비교지 <em>달러</em> 가 아니다. 동일 베이스 할당 가정에서는 직접적인 GPU-시간 절약이 나오지만, autoscaling 환경에서 concurrent multi-LoRA 가 idle 베이스 deployment 를 <em>재사용</em> 하는 만큼 어떻게 절약되는지의 회계는 빠져 있다.</li> <li><strong>MoE shared-expert export 의 정합성 verification.</strong> EP shard 별로 한 번씩 저장된 shared-expert LoRA 가 <em>완전히 동일</em> 한지 (학습 중 동기화 보장) 에 대한 결함 모드 측정은 없다. <em>export path 가 deduplicate 한다</em> 는 기술 명세는 있지만 mismatch 가 발생할 수 있는 시나리오와 그 detection 메커니즘은 명시되지 않음.</li> </ul> <h2 id="시사점--takeaways">시사점 / Takeaways</h2> <ul> <li><strong>“어댑터 revision = 학습-서빙 경계의 일급 객체” 라는 추상화가 본 논문의 진짜 기여다.</strong> 그 한 줄에서 시간 슬라이싱, MoE 라우터 replay, DSA 보정, 세 개의 캐시 tier, cold-load scheduling 까지 모두 정렬된다. 후속 multi-tenant post-training 인프라가 비슷한 디자인 결정을 할 가능성이 높다.</li> <li><strong>MoE LoRA 의 실제 비용은 byte 가 아니라 텐서 fanout 이다.</strong> rank-1 어댑터도 expert 별로 fragment 되어 Python 객체 폭증·loader 등록 비용을 만든다. Packing 으로 <em>선언된 byte</em> 는 그대로 두고 <em>객체 수</em> 만 줄이는 게 8.5× 가속의 본체. 작은 어댑터를 <em>어떻게 표현하는가</em> 가 점점 중요해질 것이다.</li> <li><strong>Concurrent multi-policy RL 은 free lunch 에 가깝다.</strong> Peak memory 변화 없이 1.45-1.77× wall time 단축이 <em>idle gap 채우기</em> 만으로 나온다. 동일 베이스 할당으로 여러 정책을 굴리는 시나리오라면 거의 모든 팀이 적용할 수 있는 단순한 정렬이다.</li> <li><strong>카탈로그 크기와 실행 capacity 를 <em>반드시</em> 분리해라.</strong> $10^6$ entries 어드레서블 ≠ $10^6$ GPU 거주. 같은 capacity dimension 으로 묶이는 디자인은 곧바로 scaling wall 을 친다.</li> <li><strong>Tinker-style “service-interface” post-training 이 진짜로 운영되고 있다.</strong> Thinking Machines Lab 의 Tinker 발표 이후 1년이 안 된 시점에 <em>그 인터페이스가 1T MoE 까지 검증된 multi-tenant 인프라 위에서 돌아간다</em> 는 보고는, post-training-as-a-service 가 본격적인 운영 단계로 들어섰다는 신호다.</li> </ul> <h2 id="설치-및-사용법">설치 및 사용법</h2> <p><code class="language-plaintext highlighter-rouge">mint-cookbook</code> 은 recipe 묶음이지 서비스 자체는 아니지만, Tinker-compatible API 위에서 같은 lifecycle 을 호출할 수 있다 (실제 service 는 비공개로 보임):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Tinker-compatible client 사용 예 (논문 §3 의 API 모양)
</span><span class="n">client</span> <span class="o">=</span> <span class="n">mint</span><span class="p">.</span><span class="nc">Client</span><span class="p">(</span><span class="n">api_key</span><span class="o">=</span><span class="sh">"</span><span class="s">...</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># 1) 정책 record 생성
</span><span class="n">policy</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">policy</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
    <span class="n">base_model</span><span class="o">=</span><span class="sh">"</span><span class="s">Qwen3-30B-A3B</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">lora_rank</span><span class="o">=</span><span class="mi">16</span><span class="p">,</span>
    <span class="n">lora_target_modules</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">attention</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">mlp</span><span class="sh">"</span><span class="p">],</span>
    <span class="n">recipe</span><span class="o">=</span><span class="sh">"</span><span class="s">grpo</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">data</span><span class="o">=</span><span class="sh">"</span><span class="s">dapo-aime24</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>

<span class="c1"># 2) train step 시작 (pollable operation id 반환)
</span><span class="n">op</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">train</span><span class="p">.</span><span class="nf">step</span><span class="p">(</span><span class="n">policy_id</span><span class="o">=</span><span class="n">policy</span><span class="p">.</span><span class="nb">id</span><span class="p">,</span> <span class="n">batch_size</span><span class="o">=</span><span class="mi">64</span><span class="p">)</span>
<span class="n">result</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">operation</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="n">op</span><span class="p">.</span><span class="nb">id</span><span class="p">)</span>

<span class="c1"># 3) 어댑터 export 와 serving 등록
</span><span class="n">revision</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">policy</span><span class="p">.</span><span class="nf">export</span><span class="p">(</span><span class="n">policy_id</span><span class="o">=</span><span class="n">policy</span><span class="p">.</span><span class="nb">id</span><span class="p">)</span>
<span class="n">client</span><span class="p">.</span><span class="n">serving</span><span class="p">.</span><span class="nf">register</span><span class="p">(</span><span class="n">revision_id</span><span class="o">=</span><span class="n">revision</span><span class="p">.</span><span class="nb">id</span><span class="p">)</span>

<span class="c1"># 4) 같은 베이스 위에서 다른 정책 시작 (time-slice)
</span><span class="n">policy2</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">policy</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span><span class="n">base_model</span><span class="o">=</span><span class="sh">"</span><span class="s">Qwen3-30B-A3B</span><span class="sh">"</span><span class="p">,</span> <span class="p">...)</span>
<span class="n">client</span><span class="p">.</span><span class="n">train</span><span class="p">.</span><span class="nf">step</span><span class="p">(</span><span class="n">policy_id</span><span class="o">=</span><span class="n">policy2</span><span class="p">.</span><span class="nb">id</span><span class="p">,</span> <span class="n">batch_size</span><span class="o">=</span><span class="mi">64</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">mint-cookbook</code> repo 에는 SFT (FinEval), DPO (chat pairs), GRPO (DAPO-AIME24), LawBench AutoResearch 의 maintained recipe 가 들어 있다.</p> <h2 id="참고-자료">참고 자료</h2> <ul> <li>논문: <a href="https://arxiv.org/abs/2605.13779">arXiv:2605.13779</a></li> <li>Cookbook: <a href="https://github.com/MindLab-Research/mint-cookbook">github.com/MindLab-Research/mint-cookbook</a></li> <li>관련 Tinker 발표 (Thinking Machines Lab, 2025): <a href="https://thinkingmachines.ai/blog/announcing-tinker/">thinkingmachines.ai/blog/announcing-tinker</a></li> </ul> <h2 id="더-읽어보기">더 읽어보기</h2> <ul> <li><strong><a href="https://arxiv.org/abs/2106.09685">LoRA: Low-Rank Adaptation of Large Language Models</a></strong> (Hu et al., ICLR 2022) — 본 논문이 service 단위로 다루는 LoRA 의 원전. Frozen base + 저-rank 어댑터의 추상화 그 자체.</li> <li><strong><a href="https://arxiv.org/abs/2305.14314">QLoRA: Efficient Finetuning of Quantized LLMs</a></strong> (Dettmers et al., NeurIPS 2023) — 4-bit quantized base + LoRA. 본 논문의 “어댑터-only handoff” 추상화와 직교하지만 자주 함께 등장.</li> <li><strong><a href="https://arxiv.org/abs/2310.18547">Punica: Multi-Tenant LoRA Serving</a></strong> (Chen et al., MLSys 2024) — Multi-LoRA 서빙의 시초. 여러 어댑터를 한 베이스 위에서 batching 하는 GPU 커널.</li> <li><strong><a href="https://arxiv.org/abs/2311.03285">S-LoRA: Serving Thousands of Concurrent LoRA Adapters</a></strong> (Sheng et al., 2023) — 천 개 단위 LoRA 동시 서빙. MinT 의 카탈로그-CPU-GPU 3-tier 분리의 직계 선조.</li> <li><strong><a href="https://arxiv.org/abs/2309.06180">Efficient Memory Management for Large Language Model Serving with PagedAttention</a></strong> (Kwon et al., SOSP 2023) — vLLM 의 원전. MinT 가 다정책 서빙 엔진으로 사용하는 vLLM 의 메모리 추상화.</li> <li><strong><a href="https://arxiv.org/abs/2407.00066">Compress then Serve: Serving Thousands of LoRA Adapters with Little Overhead</a></strong> (Gabrielsson et al., 2024) — LoRA 어댑터를 압축해 서빙. MinT 가 다루지 않는 <em>byte-level</em> 압축 축의 직교 작업.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="lora"/><category term="rlhf"/><category term="infrastructure"/><category term="moe"/><category term="multi-tenant-serving"/><category term="post-training"/><summary type="html"><![CDATA[수백만 개의 LoRA 정책을 공유 베이스 모델 위에서 학습·서빙하는 관리형 인프라. Adapter revision을 학습-서빙 경계의 단위로 삼아 핸드오프를 18.3× 줄이고, 1T급 MoE까지 검증한다.]]></summary></entry><entry xml:lang="en"><title type="html">[Paper Review] MinT: Managed Infrastructure for Training and Serving Millions of LLMs</title><link href="https://baba9811.github.io/en/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/" rel="alternate" type="text/html" title="[Paper Review] MinT: Managed Infrastructure for Training and Serving Millions of LLMs"/><published>2026-05-19T13:00:00+00:00</published><updated>2026-05-19T13:00:00+00:00</updated><id>https://baba9811.github.io/en/papers/0016-mint-managed-infrastructure-for-training-and-serving-million</id><content type="html" xml:base="https://baba9811.github.io/en/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/"><![CDATA[<div class="lang-toggle" style="margin-bottom: 1.5rem;"> <a href="/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/" class="btn btn-sm btn-outline-secondary">🇰🇷 한국어로 읽기</a> </div> <h2 id="meta">Meta</h2> <table> <thead> <tr> <th>Field</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Authors</td> <td>Mind Lab (Andrew Chen et al., 13 core contributors + ~60-person team)</td> </tr> <tr> <td>Venue</td> <td>arXiv preprint · 2026</td> </tr> <tr> <td>arXiv 또는 DOI</td> <td><a href="https://arxiv.org/abs/2605.13779">2605.13779</a></td> </tr> <tr> <td>Code</td> <td><a href="https://github.com/MindLab-Research/mint-cookbook">MindLab-Research/mint-cookbook</a> — cookbook recipes only (service code not released)</td> </tr> <tr> <td>Data</td> <td>Qwen3 family (0.6B/4B dense and 30B-A3B / 235B-A22B MoE), Moonlight-16B-A3B, Kimi K2 1.04T, GLM-5/5.1. Recipes cover SFT (FinEval / FinGPT suite), DPO (chat pairs), GRPO (DAPO-AIME24), LawBench AutoResearch</td> </tr> <tr> <td><span style="white-space: nowrap">Review date</span></td> <td>2026-05-19</td> </tr> </tbody> </table> <h2 id="tldr">TL;DR</h2> <ul> <li>The traditional pattern of copying a full checkpoint per trained policy stops scaling once post-training becomes a multi-policy RL workload. MinT keeps the base model resident and makes the LoRA <strong>adapter revision</strong> the single artifact that crosses the training-serving boundary.</li> <li>Three scaling axes: <strong>Scale Up</strong> (LoRA RL on 1T-class MoE and MLA/DSA architectures), <strong>Scale Down</strong> (adapter-only handoff is 18.3x faster than merge on a 4B dense model and 2.85x on a 30B MoE; concurrent GRPO under the same base allocation shaves 1.77x / 1.45x off wall time), <strong>Scale Out</strong> (a $10^6$-scale addressable policy catalog kept separate from CPU-cache and GPU-batch working sets).</li> <li>Packing MoE LoRA tensors into a serving representation reduces 37,248 tiny tensor objects to 672, speeding live engine loading by 8.5-8.7x while file size barely changes. Treating cold loading as <em>scheduled service work</em> is one of the key contributions.</li> <li>The single design choice — “the adapter revision, not the checkpoint, is the first-class object at the training-serving boundary” — unifies policy lifecycle, MoE router replay, DSA correction, and the three cache tiers.</li> </ul> <h2 id="introduction">Introduction</h2> <p>Post-training used to be a one-shot stage at the end of pretraining. Between 2024 and 2026 it has turned into a multi-policy, multi-stage RL workload. Frontier-model reports describe reasoning, coding, tool use, and agentic evaluation as workflows that produce <em>dozens to hundreds of policy variants</em>: per-task fine-tunes, product branches, experimental versions, tenant adapters, rollback points.</p> <p>The point where existing infrastructure breaks is sharp. If the <em>result</em> of any trained variant is a full fine-tuned checkpoint, every training-to-serving crossing has to move the entire model. A 1T base makes even a 30 GB LoRA-merged checkpoint qualitatively different from a few-hundred-MB LoRA adapter. Merged LoRA reduces training memory but ends by folding the adapter back into the base and shipping the merged full checkpoint — the byte burden at the boundary is unchanged.</p> <p>MinT (the MindLab Toolkit) rewrites that boundary. The base stays resident across training, rollout, and serving workers, and the trained LoRA is exported as an <em>adapter revision</em>: a fixed LoRA snapshot at a specific training step, frozen in serving tensor layout. Every stage of a policy’s lifecycle — rollout, update, export, evaluation, serving, rollback — selects an adapter revision. This post walks through how that single abstraction connects validated learning curves up to 1T MoE, a 18.3x faster adapter handoff, and a $10^6$-scale policy catalog, all under one system design.</p> <h2 id="key-contributions">Key Contributions</h2> <ul> <li><strong>Adapter-revision lifecycle.</strong> Export → rollout → evaluation → serving → rollback flow through one exported LoRA file. The policy record holds the service-side state (compatible base, rank, target modules, training checkpoint pointers, rollout records, exported revisions list).</li> <li><strong>Large-scale multi-LoRA RL training.</strong> A single resident base is time-sliced across multiple LoRA policies. Both single-worker PEFT and distributed Megatron training paths are supported, with measured curves up to 235B-A22B GRPO and Kimi K2 1.04T countdown-task RL.</li> <li><strong>Policy-population multi-LoRA serving.</strong> vLLM engines hold a resident base and attach adapter revisions. The addressable catalog, CPU cache, and GPU batch are exposed as <em>three independent capacity dimensions</em>; a packed MoE LoRA representation speeds live load by 8.5-8.7x by collapsing the small-tensor fanout.</li> <li><strong>Public reproducibility paths.</strong> A Tinker-compatible API and the <code class="language-plaintext highlighter-rouge">mint-cookbook</code> recipes (SFT, preference optimization, rollout-based RL, AutoResearch) reproduce the same lifecycle from one example set.</li> </ul> <h2 id="related-work--background">Related Work / Background</h2> <p>This paper sits in a stack where <em>LoRA training</em> and <em>LoRA serving</em> both already have their own ecosystems, and the missing piece is the <em>service</em> that connects them into one lifecycle.</p> <ul> <li><strong>LoRA and its variants.</strong> Hu et al.’s LoRA freezes the base and trains only low-rank matrices. AdaLoRA dynamically allocates rank budget across layers; QLoRA combines a 4-bit quantized base with LoRA updates. “LoRA Without Regret” (Schulman &amp; Thinking Machines Lab, 2025) shows LoRA can reach near-full fine-tune quality in post-training — the empirical premise this paper builds on when it treats LoRA as a <em>service-level unit</em>, not a memory optimization.</li> <li><strong>Multi-LoRA training infrastructure.</strong> mLoRA (Ye et al., 2024) fine-tunes several adapters in one batch. MinT takes a different route, exploiting the fact that RL workloads already carry enough rollout tokens per policy to fill a per-policy batch, so time-slicing is sufficient.</li> <li><strong>Multi-LoRA serving.</strong> Punica (Chen et al., 2024), S-LoRA (Sheng et al., 2023), dLoRA (Wu et al., 2024), and LoRAServe (Jaiswal et al., 2025) handle the runtime question — how to route, batch, and memory-manage existing adapters under a shared base. MinT layers a <em>serving catalog ↔ training export</em> lifecycle on top of that, turning cold misses into scheduled service work.</li> <li><strong>RL execution frameworks.</strong> HybridFlow/verl, AReaL, OpenRLHF, ROLL, StreamRL, Laminar, and NeMo-Aligner address <em>generic RL systems concerns</em> — rollout scheduling, failure isolation, colocated/disaggregated execution. MinT adds <em>LoRA-specific service state</em>: adapter revisions, optimizer state, rollout records, MoE route records, DSA correction metadata.</li> <li><strong>Training-serving consistency.</strong> R3 (Ma et al., 2025) and IcePop (Ling Team et al., 2025) quantify how MoE router mismatch and token-level probability gaps destabilize RL. MinT stores the corresponding correction policy as part of the rollout record.</li> </ul> <h2 id="method--architecture">Method / Architecture</h2> <h3 id="system-overview">System overview</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig1-mint-overview-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig1-mint-overview-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig1-mint-overview-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig1-mint-overview.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1: A four-layer view — user intent → MinT service → policy population over shared base deployments → infrastructure components. Scheduler, fault tolerance, adapter lifecycle, and serving residency stay behind the service interface.</figcaption> </figure> <p>User intent (base model, data + reward, LoRA RL recipe, evaluate / serve target) is turned into <em>queued work, policy records, and exported revisions</em>. The base model is <em>resident</em> on training, rollout, and serving workers; a population of policies $r_1, r_2, \ldots$ moves through train / evaluate / serve / rollback under the same adapter lifecycle. The lower box — scheduler, fault tolerance, adapter lifecycle, serving residency — is the complexity <em>hidden underneath</em>.</p> <h3 id="the-training-serving-boundary-what-crosses">The training-serving boundary: what crosses</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig2-training-serving-paths-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig2-training-serving-paths-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig2-training-serving-paths-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig2-training-serving-paths.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 2: Three paths from training to serving. Full fine-tuning ships full weights $W\_i$ per variant; LoRA merge folds the adapter into the base and ships a merged full checkpoint $W'\_i$; MinT keeps the base $W$ resident on both sides and moves only the adapter $L\_i$.</figcaption> </figure> <p>This simple diagram contains most of the paper’s abstraction. <em>Full fine-tuning</em> moves complete weights $W_1, W_2, \ldots, W_n$ from the training worker to the inference worker. <em>LoRA merge</em> trains adapters beside a resident base but, to ship to serving, folds them back into the base and moves a merged full checkpoint $W’_i$. <em>MinT multi-LoRA</em> keeps the base $W$ resident on <em>both</em> workers and moves only the adapter $L_i$. The saving isn’t only adapter bytes — because the inference engine <em>already holds the base</em>, the load step itself collapses to admitting a new adapter into a slot.</p> <h3 id="adapter-revision-vs-policy-record">Adapter revision vs policy record</h3> <p>This separation is one of the design’s most important choices.</p> <ul> <li><strong>Adapter revision</strong>: a fixed, exported LoRA snapshot frozen at a particular training step and stored in serving tensor layout. Not a training checkpoint — optimizer state and rank-local training files are stripped away, leaving one PEFT adapter file.</li> <li><strong>Policy record</strong>: the service-side state that makes that payload <em>queryable, reloadable, and rollbackable</em>. It names the compatible base version, LoRA rank and target modules, the latest training checkpoint, the rollout records, and the list of exported revisions.</li> </ul> <p>If those were one object, every training update would oscillate the serving catalog. By keeping them separate, the catalog can hold a stable revision <em>while training continues</em>, and only that revision is selected by evaluation or serving.</p> <h3 id="system-design-service-plane-vs-compute-plane">System design: service plane vs compute plane</h3> <p>MinT separates the <em>service / control plane</em> (API, queue, policy lookup, resource admission, operation state) from the <em>compute plane</em> (PEFT/Megatron trainers, vLLM samplers). Durable storage between them holds four kinds of artifact: policy records, checkpoints, rollout records, exported adapters. The paper’s <em>operation-visibility</em> invariant is that every client call returns a pollable operation id, and a result becomes client-visible only after the worker writes the metadata entry that names its completed files — partial files never leak into the catalog.</p> <h3 id="scale-up-lora-rl-on-1t-class-moe">Scale Up: LoRA RL on 1T-class MoE</h3> <p>For LoRA to survive on a large base, the <em>distributed placement of the base</em> and the <em>adapter export</em> have to agree. In Megatron training groups, dense-module LoRA tensors follow the tensor-parallel shards of the weights they modify; MoE expert LoRAs are keyed by expert id, with EP shards owning only the LoRAs for experts they hold and TP further sharding within the owner group. Shared-expert LoRA is stored once per EP shard and deduplicated on export.</p> <h4 id="moe-router-replay-r3-style">MoE router replay (R3-style)</h4> <p>MoE RL has a subtle hazard: if the expert path that handled a token during <em>rollout</em> doesn’t match the expert path that scores that token during <em>training</em>, the on-policy assumption breaks. MinT records the selected expert ids in the rollout record and, when the training backend can map those ids to its EP layout, replays that route. Tokens whose route cannot be reconstructed (missing ids, layout change) are masked out of the policy gradient.</p> <h4 id="sparse-attention-provenance-dsa">Sparse-attention provenance (DSA)</h4> <p>GLM-5 / GLM-5.1’s dynamic sparse attention opens a similar channel. The indexer and top-$k$ decide which tokens take part in sparse attention, so numerical differences can change the token set and create a rollout/training mismatch. MinT uses an IcePop-style rollout correction: tokens whose training/rollout probability ratio leaves the configured trusted band receive zero importance weight. This <em>filters out</em> unsafe scoring terms; it does not replay every indexer choice, and it doesn’t prove that training used the exact sparse-attention token set the inference engine selected.</p> <h3 id="scale-down-adapter-only-handoff">Scale Down: adapter-only handoff</h3> <h4 id="handoff-bytes">Handoff bytes</h4> <p>A measured Qwen3-4B rank-32 PEFT adapter file is 264,310,274 bytes — about 252 MiB. The same 4B base has a bf16 floor of about 8.0 GB. The adapter is roughly <em>3.3% of the base-weight floor</em>. At rank-1 with a tighter target-module set, that drops to ~0.10% — the abstract’s “less than 1%” claim is conservative.</p> <h4 id="time-sliced-multi-policy-training">Time-sliced multi-policy training</h4> <p>One trainer actor keeps the base $W$ resident and time-slices LoRA A → switch → LoRA B → switch → LoRA A (paper figure 5). At each policy switch the previous policy’s LoRA tensors, optimizer state, gradients, and rollout records are committed to its policy record, and the next policy’s state is restored. The base stays in GPU memory across switches, so <em>the policy-switch cost does not include base reload</em>.</p> <h4 id="concurrent-training-timeline">Concurrent training timeline</h4> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig7-concurrent-multi-lora-timeline-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig7-concurrent-multi-lora-timeline-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig7-concurrent-multi-lora-timeline-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig7-concurrent-multi-lora-timeline.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 7: Sequential vs MinT concurrent execution of three GRPO policies under the same base-model allocation. Qwen3-4B: 51.4 → 28.9 min (1.77x faster, 22.4 min saved). Qwen3-30B: 168.8 → 116.8 min (1.45x faster, 52.0 min saved). Peak memory unchanged in both cases (65.6 GiB / 68.0 GiB).</figcaption> </figure> <p>The point is that <em>peak memory stays flat while wall time shrinks</em>. The speedup comes from filling the idle gaps in a sequential schedule with another policy’s rollout / update / evaluation phase — a form of self-pipelining at the trainer level.</p> <h4 id="adapter-only-vs-merge-and-load-handoff">Adapter-only vs merge-and-load handoff</h4> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig8-adapter-handoff-vs-merge-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig8-adapter-handoff-vs-merge-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig8-adapter-handoff-vs-merge-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig8-adapter-handoff-vs-merge.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 8: Wall time for shipping a freshly trained policy to the sampler. Qwen3-4B adapter path 4.1s vs merge 74.9s (18.3x faster). Qwen3-30B adapter 208.7s vs merge 595.5s (2.9x faster).</figcaption> </figure> <p>Table 4 in the paper decomposes this. On Qwen3-4B the adapter path is <em>materialization/load 0.036s + cold first sample 4.114s</em>, while the merge path is <em>materialization/load 71.820s + cold first sample 55.704s</em>. The 71.8 s on the merge path goes entirely to folding the adapter back into the base and admitting the resulting full checkpoint into the sampler. Compare warm sample speed (15,568 vs 20,595 tok/s) and the merge path is genuinely faster <em>at steady state</em>, but for short RL steps the handoff cost dominates so much that adapter-only wins by a wide margin.</p> <h3 id="scale-out-policy-population-serving">Scale Out: policy-population serving</h3> <h4 id="three-cache-tiers">Three cache tiers</h4> <table> <thead> <tr> <th>Tier</th> <th>Scale</th> <th>Lifetime</th> <th>Promotion / eviction</th> </tr> </thead> <tbody> <tr> <td>Addressable catalog</td> <td>$10^3 - 10^6$ entries</td> <td>Durable (control plane)</td> <td>Promoted by export, retired manually</td> </tr> <tr> <td>CPU adapter cache</td> <td>Hundreds per engine</td> <td>Per actor run</td> <td>Promoted by router / cache-miss load, LRU eviction</td> </tr> <tr> <td>GPU batch</td> <td>$\le 64$ distinct</td> <td>One decoding step</td> <td>Promoted by batch scheduler, released at step end</td> </tr> </tbody> </table> <p>Table 2 in the paper makes the point that <em>addressable catalog size</em> and <em>concurrent GPU residency</em> are different capacity dimensions. $10^6$ is how many policy names can be resolved, not how many can be simultaneously alive in a GPU batch.</p> <h4 id="warm-vs-cold-path-measurements">Warm vs cold path measurements</h4> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig13-warm-vs-cold-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig13-warm-vs-cold-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig13-warm-vs-cold-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig13-warm-vs-cold.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 13: Left: CPU-cached requests p95 21.35s vs cache-miss 199.81s. Middle: warm/cold regimes stay stable as the catalog grows from 1k to 100k. Right: 16 distinct cold adapters serialize into a 1.36 s/adapter staircase totaling 23.27s.</figcaption> </figure> <p>The middle panel matters most: a 100x larger catalog does not push warm p95 up. Routing and local cache state are scaled by traffic locality, not by catalog size, so addressability is decoupled from execution capacity — exactly the design contract the paper is trying to demonstrate.</p> <h4 id="moe-small-tensor-fanout">MoE small-tensor fanout</h4> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab7-packed-moe-lora-loading-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab7-packed-moe-lora-loading-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab7-packed-moe-lora-loading-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab7-packed-moe-lora-loading.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 7: Effect of the packed MoE LoRA representation. File size barely changes (110.75 → 105.58 MB, 1.05x smaller) but tensor objects collapse from 37,248 to 672 (55.4x fewer), yielding read tensors 54.8x / build loader objects 29.5x / live engine load 8.5-8.7x faster.</figcaption> </figure> <p>This is why MoE LoRA makes the cold path <em>visible</em>. Even a rank-1 adapter fragments into thousands of per-expert tensors, so the serving side pays the cost of creating tens of thousands of Python objects and registering each one with the engine. Packing keeps the <em>declared bytes</em> nearly unchanged while collapsing <em>object count</em>, which is what actually drives the 8.5x speedup.</p> <h2 id="training-objective--loss">Training Objective / Loss</h2> <p>The paper proposes no new loss — it applies standard SFT, DPO reward-margin, and GRPO (a PPO-family group-relative policy optimization) to LoRA adapters. What matters for MoE RL is <em>which logits do the scoring</em>, since route mismatch is the destabilizing channel:</p> \[\nabla_\theta J(\theta) = \mathbb{E}_{\tau \sim \pi_{\text{rollout}}} \left[ \sum_t \nabla_\theta \log \pi_\theta(a_t | s_t; \,\text{route}_t) \cdot A_t \right]\] <p>where $\text{route}_t$ is the expert path selected during rollout. MinT stores it in the rollout record and replays it during training when possible; tokens whose route cannot be replayed are masked. For DSA mismatch (IcePop):</p> \[\begin{aligned} w_t &amp;= \mathbb{1}\!\left[\, \rho_t \in [\rho_{\text{lo}}, \rho_{\text{hi}}] \,\right], \\ \rho_t &amp;= \frac{\pi_\theta^{\text{train}}(a_t | s_t)}{\pi_\theta^{\text{rollout}}(a_t | s_t)} \end{aligned}\] <p>Tokens whose ratio leaves the trust band $[\rho_{\text{lo}}, \rho_{\text{hi}}]$ receive $w_t = 0$ and drop out of the gradient. This is a <em>filtering</em> correction, not a reconstruction of the exact sparse-attention token set.</p> <h2 id="training-data-and-pipeline">Training Data and Pipeline</h2> <table> <thead> <tr> <th>Component</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Dense SFT</td> <td>Qwen3-4B, FinEval / FinGPT suite (Fineval, FPB, FiQA-SA, TFNS, NWGI)</td> </tr> <tr> <td>Dense DPO</td> <td>Qwen3-4B, chat pairs</td> </tr> <tr> <td>Dense GRPO</td> <td>Qwen3-8B base, DAPO-AIME24</td> </tr> <tr> <td>MoE GRPO (30B)</td> <td>Qwen3-30B-A3B, AIME24</td> </tr> <tr> <td>MoE GRPO (235B)</td> <td>Qwen3-235B-A22B, AIME24, 32-GPU Megatron (TP=4, EP=8, PP=1) + 16-GPU TP=16 vLLM serving</td> </tr> <tr> <td>1T countdown-task RL</td> <td>Kimi K2 (32.6B active), 64-GPU H800</td> </tr> <tr> <td>Serving experiments</td> <td>Qwen3-30B rank-1 MoE LoRA, 4-GPU TP=4 serving actor, prompt 1024, max output 64</td> </tr> <tr> <td>Cookbook recipes</td> <td>LawBench AutoResearch (28 experiments, 6 kept), DAPO-AIME24, chat-DPO, FinGPT</td> </tr> </tbody> </table> <h2 id="experimental-results">Experimental Results</h2> <h3 id="scale-down-handoff-cost">Scale Down: handoff cost</h3> <table> <thead> <tr> <th>Model</th> <th>Path</th> <th>Checkpoint size</th> <th>Materialization / load</th> <th>Cold first sample</th> <th>Sample tok/s (total/warm)</th> </tr> </thead> <tbody> <tr> <td>Qwen3-4B</td> <td>Adapter</td> <td>252 MiB</td> <td>0.036 s</td> <td>4.114 s</td> <td>15,568 / 15,567</td> </tr> <tr> <td>Qwen3-4B</td> <td>Merge</td> <td>8.061 GB</td> <td>71.820 s</td> <td>55.704 s</td> <td>4,697 / 20,595</td> </tr> <tr> <td>Qwen3-30B</td> <td>Adapter</td> <td>1.692 GB</td> <td>46.455 s</td> <td>117.304 s</td> <td>1,874 / 5,700</td> </tr> <tr> <td>Qwen3-30B</td> <td>Merge</td> <td>61.084 GB</td> <td>402.245 s</td> <td>156.074 s</td> <td>1,573 / 6,904</td> </tr> </tbody> </table> <p>The merge path has <em>higher warm sampling throughput</em> (4B: 20,595 vs 15,567 tok/s) but its handoff + cold-first-sample sum is so large that short RL steps strongly favor the adapter path.</p> <h3 id="scale-up-learning-curves">Scale Up: learning curves</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig10-moe-rl-curves-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig10-moe-rl-curves-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig10-moe-rl-curves-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/fig10-moe-rl-curves.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 10: MoE RL learning curves. Qwen3-30B-A3B and 235B-A22B use AIME24 mean@1; Kimi K2 1T uses countdown-task reward. 30B reaches ~0.9 mid-band; 235B peaks at 0.967 (near saturation on AIME24).</figcaption> </figure> <p>The point of all three panels is that <em>the same LoRA RL path</em> — adapter revision, rollout record, export, evaluate — survives 30B sparse → 235B-A22B Hopper deployment → 1T-class base without changes. Dense results live in the paper’s Table 5: all five FinGPT SFT benchmarks show large held-out gains over base Qwen3-4B (e.g. FinEval 0.4226 → 0.7811), DPO chat-pairs reward margin -0.03 → 30.88, GRPO AIME24 train accuracy 0.11 → 0.47 (best raw 0.568 at step 76).</p> <h3 id="validated-model-families">Validated model families</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab1-model-family-support-480.webp 480w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab1-model-family-support-800.webp 800w,/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab1-model-family-support-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0016-mint-managed-infrastructure-for-training-and-serving-million/tab1-model-family-support.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 1: Three model families validated by the current MinT stack. Qwen3 (dense + MoE, 0.6B-235B-A22B), Moonlight &amp; Kimi K2 (MLA MoE, up to 1.04T), GLM-5/5.1 (MLA, DSA, MTP, MoE).</figcaption> </figure> <h3 id="scale-out-policy-population-serving-1">Scale Out: policy-population serving</h3> <table> <thead> <tr> <th>Metric</th> <th>Measurement</th> </tr> </thead> <tbody> <tr> <td>Catalog sweep</td> <td>1k → 100k entries, all warm regime</td> </tr> <tr> <td>CPU cache (512-adapter hotset)</td> <td>369 / 550 loaded adapters</td> </tr> <tr> <td>Same-batch GPU active</td> <td>16, 32, 64 all 0 errors</td> </tr> <tr> <td>Cold-cache p95</td> <td>199.81 s (~9.4x the warm 21.35s)</td> </tr> <tr> <td>Cold staircase (16 distinct)</td> <td>1.375 → 23.267 s, ~1.36 s/adapter</td> </tr> <tr> <td>Packed MoE LoRA live load</td> <td>0.156-0.164 s (vs 1.36-1.39 s, 8.5-8.7x faster)</td> </tr> </tbody> </table> <p>Appendix table 13 extrapolates these single-engine limits to a $10^6$-entry catalog under a 2300-distinct-adapter active-wave assumption (warm distinct concurrency at 36 engines / 144 GPUs; cold-load isolation at 72 engines / 288 GPUs).</p> <h2 id="analysis--ablation">Analysis / Ablation</h2> <p>The three-axis design lets each measurement isolate a different systems concern.</p> <ul> <li><strong>Scale Down’s 18.3x / 2.85x speedup</strong> doesn’t come from <em>adapter bytes</em>. Table 4 shows the adapter path’s cold first sample (4.1s / 117.3s) is on the same order as the merge path’s (55.7s / 156.1s). The real saving is in <em>materialization/load</em> (4B: 71.8s vs 0.036s; 30B: 402.2s vs 46.5s). What collapses is not “the file is small” but “the base is already in the sampler, so merge+load goes away.”</li> <li><strong>Concurrent multi-LoRA’s 1.77x / 1.45x</strong> is <em>idle-gap filling</em>. Peak memory stays flat — the speedup is from packing more useful work into the same allocation, not from buying a larger slot.</li> <li><strong>Packed MoE LoRA’s 8.5-8.7x</strong> does not come from the 1.05x file-size reduction. Read tensors 54.8x and build loader objects 29.5x faster — the bottleneck is <em>Python-side small-object fanout</em>, not byte transfer.</li> <li><strong>No carousel effect.</strong> A 100x larger catalog actually leaves warm p95 <em>slightly lower</em> (20.89s → 12.16s → 12.12s). Name resolution and local cache state are decoupled cleanly enough that catalog size adds no routing overhead.</li> </ul> <h2 id="limitations-and-critical-assessment">Limitations and Critical Assessment</h2> <ul> <li><strong>Base-model compatibility is a precondition of the abstraction.</strong> “Move only adapter revisions” assumes the base is <em>binary identical</em> on both sides. The lifecycle is strong while the base is fixed and the adapter catalog accumulates, but base swaps — new pretraining release, quantization change, MoE expert reshuffle — are a weak point that the paper doesn’t address (rollback is only between adapter revisions).</li> <li><strong>DSA correction is filtering, not reconstruction.</strong> The IcePop-style filter masks unsafe tokens but does not recover the exact attention pattern the inference engine used. The paper acknowledges this: “it does not prove that training used the exact sparse-attention token set selected by the inference engine.” It’s a trade-off for stability, not full consistency.</li> <li><strong>Single-vendor validation.</strong> Qwen, Moonlight/Kimi, GLM — all are Chinese frontier families whose training/serving stacks share family resemblances. How the lifecycle interacts with Llama-3, Mistral, or Claude-family training paths is untested here.</li> <li><strong>Absolute cold staircase.</strong> 1.36 s/adapter looks small, but 16 distinct cold adapters arriving together is a 23 s staircase. A scenario like a post-deploy traffic spike with 100 distinct cold adapters relies on appendix extrapolation, not measurement.</li> <li><strong>Cookbook coverage.</strong> Public reproducibility goes through the cookbook, which is welcome — but the <em>service code itself</em> (scheduler, queue, policy lookup) appears to be closed. <code class="language-plaintext highlighter-rouge">mint-cookbook</code> is recipes, not the system. In-house reproduction requires the cookbook + a Tinker-compatible API.</li> <li><strong>No cost reporting.</strong> “1.77x faster” is wall-time, not dollars. Under a fixed base allocation that translates directly into GPU-hour savings, but the accounting for autoscaling environments — where concurrent multi-LoRA reuses an otherwise-idle base — is not given.</li> <li><strong>MoE shared-expert export consistency.</strong> Shared-expert LoRA stored once per EP shard is deduplicated on export, but the paper doesn’t measure whether the per-shard copies are guaranteed identical (training-time synchronization) or what detection mechanisms exist for the failure case.</li> </ul> <h2 id="takeaways">Takeaways</h2> <ul> <li><strong>The real contribution is the abstraction: “an adapter revision, not a checkpoint, is the first-class object at the training-serving boundary.”</strong> That single line aligns time-slicing, MoE router replay, DSA correction, three cache tiers, and cold-load scheduling. Future multi-tenant post-training infrastructure will likely converge on similar design choices.</li> <li><strong>The true cost of MoE LoRA is tensor fanout, not bytes.</strong> Even a rank-1 adapter fragments into per-expert tensors that explode the Python object count and loader registration cost. Packing leaves <em>declared bytes</em> alone and only reduces <em>object count</em> — that’s where the 8.5x comes from. How small adapters are <em>represented</em> will only become more important.</li> <li><strong>Concurrent multi-policy RL is close to a free lunch.</strong> A 1.45-1.77x wall-time reduction at unchanged peak memory, purely from idle-gap filling. Any team running multiple policies under one base allocation can apply this with minimal scheduling work.</li> <li><strong>Keep catalog size and execution capacity strictly separate.</strong> $10^6$ addressable ≠ $10^6$ resident. A design that ties those into one dimension will hit a scaling wall immediately.</li> <li><strong>Tinker-style service-interface post-training is operational.</strong> Less than a year after Thinking Machines Lab’s Tinker announcement, this paper reports that the same interface runs on top of multi-tenant infrastructure validated up to 1T MoE. Post-training-as-a-service has crossed into a serious operational phase.</li> </ul> <h2 id="setup-and-usage">Setup and Usage</h2> <p><code class="language-plaintext highlighter-rouge">mint-cookbook</code> is a recipe set, not the service itself, but the Tinker-compatible API exposes the same lifecycle (the service appears to be closed-source):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Tinker-compatible client (the API shape sketched in paper §3)
</span><span class="n">client</span> <span class="o">=</span> <span class="n">mint</span><span class="p">.</span><span class="nc">Client</span><span class="p">(</span><span class="n">api_key</span><span class="o">=</span><span class="sh">"</span><span class="s">...</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># 1) Create a policy record
</span><span class="n">policy</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">policy</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
    <span class="n">base_model</span><span class="o">=</span><span class="sh">"</span><span class="s">Qwen3-30B-A3B</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">lora_rank</span><span class="o">=</span><span class="mi">16</span><span class="p">,</span>
    <span class="n">lora_target_modules</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">attention</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">mlp</span><span class="sh">"</span><span class="p">],</span>
    <span class="n">recipe</span><span class="o">=</span><span class="sh">"</span><span class="s">grpo</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">data</span><span class="o">=</span><span class="sh">"</span><span class="s">dapo-aime24</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>

<span class="c1"># 2) Train step (returns a pollable operation id)
</span><span class="n">op</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">train</span><span class="p">.</span><span class="nf">step</span><span class="p">(</span><span class="n">policy_id</span><span class="o">=</span><span class="n">policy</span><span class="p">.</span><span class="nb">id</span><span class="p">,</span> <span class="n">batch_size</span><span class="o">=</span><span class="mi">64</span><span class="p">)</span>
<span class="n">result</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">operation</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="n">op</span><span class="p">.</span><span class="nb">id</span><span class="p">)</span>

<span class="c1"># 3) Export adapter revision and register for serving
</span><span class="n">revision</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">policy</span><span class="p">.</span><span class="nf">export</span><span class="p">(</span><span class="n">policy_id</span><span class="o">=</span><span class="n">policy</span><span class="p">.</span><span class="nb">id</span><span class="p">)</span>
<span class="n">client</span><span class="p">.</span><span class="n">serving</span><span class="p">.</span><span class="nf">register</span><span class="p">(</span><span class="n">revision_id</span><span class="o">=</span><span class="n">revision</span><span class="p">.</span><span class="nb">id</span><span class="p">)</span>

<span class="c1"># 4) Start a second policy on the same base (time-sliced)
</span><span class="n">policy2</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">policy</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span><span class="n">base_model</span><span class="o">=</span><span class="sh">"</span><span class="s">Qwen3-30B-A3B</span><span class="sh">"</span><span class="p">,</span> <span class="p">...)</span>
<span class="n">client</span><span class="p">.</span><span class="n">train</span><span class="p">.</span><span class="nf">step</span><span class="p">(</span><span class="n">policy_id</span><span class="o">=</span><span class="n">policy2</span><span class="p">.</span><span class="nb">id</span><span class="p">,</span> <span class="n">batch_size</span><span class="o">=</span><span class="mi">64</span><span class="p">)</span>
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">mint-cookbook</code> repo ships maintained recipes for SFT (FinEval), DPO (chat pairs), GRPO (DAPO-AIME24), and LawBench AutoResearch.</p> <h2 id="references">References</h2> <ul> <li>Paper: <a href="https://arxiv.org/abs/2605.13779">arXiv:2605.13779</a></li> <li>Cookbook: <a href="https://github.com/MindLab-Research/mint-cookbook">github.com/MindLab-Research/mint-cookbook</a></li> <li>Related Tinker announcement (Thinking Machines Lab, 2025): <a href="https://thinkingmachines.ai/blog/announcing-tinker/">thinkingmachines.ai/blog/announcing-tinker</a></li> </ul> <h2 id="further-reading">Further Reading</h2> <ul> <li><strong><a href="https://arxiv.org/abs/2106.09685">LoRA: Low-Rank Adaptation of Large Language Models</a></strong> (Hu et al., ICLR 2022) — The original LoRA paper, whose abstraction (frozen base + low-rank adapters) is the unit this work elevates to a service-level object.</li> <li><strong><a href="https://arxiv.org/abs/2305.14314">QLoRA: Efficient Finetuning of Quantized LLMs</a></strong> (Dettmers et al., NeurIPS 2023) — 4-bit quantized base + LoRA. Orthogonal to the adapter-only handoff but typically combined with it.</li> <li><strong><a href="https://arxiv.org/abs/2310.18547">Punica: Multi-Tenant LoRA Serving</a></strong> (Chen et al., MLSys 2024) — The first systems paper on serving many LoRAs over a shared base; introduces the GPU-kernel primitives MinT eventually layers a catalog on top of.</li> <li><strong><a href="https://arxiv.org/abs/2311.03285">S-LoRA: Serving Thousands of Concurrent LoRA Adapters</a></strong> (Sheng et al., 2023) — Thousand-scale LoRA serving. A direct ancestor of MinT’s three-tier (catalog / CPU / GPU) split.</li> <li><strong><a href="https://arxiv.org/abs/2309.06180">Efficient Memory Management for Large Language Model Serving with PagedAttention</a></strong> (Kwon et al., SOSP 2023) — The vLLM paper. MinT uses vLLM as the serving engine, building on its memory abstraction.</li> <li><strong><a href="https://arxiv.org/abs/2407.00066">Compress then Serve: Serving Thousands of LoRA Adapters with Little Overhead</a></strong> (Gabrielsson et al., 2024) — Adapter compression for serving. An orthogonal byte-level axis that MinT does not pursue.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="lora"/><category term="rlhf"/><category term="infrastructure"/><category term="moe"/><category term="multi-tenant-serving"/><category term="post-training"/><summary type="html"><![CDATA[A managed infrastructure for training and serving millions of LoRA policies over a small set of resident base models. Adapter revisions become the unit that crosses the training-serving boundary, cutting the handoff by 18.3x and validating the path up to 1T-scale MoE.]]></summary></entry><entry xml:lang="ko"><title type="html">[논문 리뷰] Goldfish: Monolingual Language Models for 350 Languages</title><link href="https://baba9811.github.io/papers/0015-goldfish/" rel="alternate" type="text/html" title="[논문 리뷰] Goldfish: Monolingual Language Models for 350 Languages"/><published>2026-05-14T14:00:00+00:00</published><updated>2026-05-14T14:00:00+00:00</updated><id>https://baba9811.github.io/papers/0015-goldfish</id><content type="html" xml:base="https://baba9811.github.io/papers/0015-goldfish/"><![CDATA[<div class="lang-toggle" style="margin-bottom: 1.5rem;"> <a href="/en/papers/0015-goldfish/" class="btn btn-sm btn-outline-secondary">🇺🇸 Read in English</a> </div> <h2 id="메타정보">메타정보</h2> <table> <thead> <tr> <th>항목</th> <th>내용</th> </tr> </thead> <tbody> <tr> <td>저자</td> <td>Tyler A. Chang, Catherine Arnett, Zhuowen Tu, Benjamin K. Bergen (UC San Diego)</td> </tr> <tr> <td>학회</td> <td>arXiv preprint · 2026 (cs.CL, v2)</td> </tr> <tr> <td>arXiv</td> <td><a href="https://arxiv.org/abs/2408.10441">2408.10441</a></td> </tr> <tr> <td>Code</td> <td><a href="https://github.com/tylerachang/goldfish">tylerachang/goldfish</a> · <a href="https://huggingface.co/goldfish-models">goldfish-models on Hugging Face</a></td> </tr> <tr> <td>데이터</td> <td>OSCAR · Glot500 · MADLAD-400 · NLLB seed · Wikipedia · 그 외 ~80개 코퍼스 · byte premium scaling 후 5MB~1GB 절단</td> </tr> <tr> <td><span style="white-space: nowrap">리뷰 일자</span></td> <td>2026-05-14</td> </tr> </tbody> </table> <h2 id="tldr">TL;DR</h2> <ul> <li>350개 언어 각각에 대해 5MB · 10MB · 100MB · 1GB · full 의 다섯 가지 데이터 크기로 GPT-2 계열 단일언어 모델을 사전학습해 <strong>총 1,154개 모델 (350개 언어)</strong> 을 공개한다. 1GB 모델은 125M 파라미터, 5/10MB는 39M.</li> <li>비교 가능성을 확보하기 위해 <strong>byte premium scaling</strong> 으로 모든 언어를 영어 등가 데이터 양에 맞춘다. 즉 1GB 데이터셋은 <em>content-matched</em> 1GB이지 <em>bytes-of-the-language</em> 1GB이 아니다.</li> <li>결과적으로 작은 단일언어 모델이 XGLM 4.5B · BLOOM 7.1B · MaLA-500 10B 같은 <strong>수십 배~수백 배 큰 다국어 모델보다 FLORES perplexity 가 낮다</strong>. 예: MaLA-500 대비 175개 언어 중 111개 (63%) 에서 승리.</li> <li>흥미롭게도 단순 <strong>bigram 베이스라인이 XGLM 4.5B 보다 24% 언어, BLOOM 7.1B 보다 43% 언어에서 더 낮은 perplexity를 보인다</strong> — 거대 다국어 모델이 저자원 언어에서는 기본적인 다음 토큰 예측조차 못 한다는 강한 증거.</li> <li>단 reasoning 벤치마크 (Belebele · XCOPA · XStoryCloze) 에서는 모든 sub-1B 모델이 chance 수준에 머물러, perplexity 향상이 곧 reasoning 향상은 아니다.</li> </ul> <h2 id="소개">소개</h2> <p>저자원 언어 (low-resource language) 모델링 연구는 오랫동안 <strong>거대 다국어 모델 (massively multilingual model)</strong> 에 의존해 왔다. XLM-R, BLOOM, XGLM, MaLA-500 같은 모델은 수십~수백 개 언어를 한 모델로 다루며, 단일 언어로는 데이터가 부족한 상황을 다국어 합성으로 보완한다는 직관에 기반한다. 하지만 이 직관은 두 가지 측면에서 의심받아 왔다. 첫째, <em>curse of multilinguality</em> (Conneau et al., 2020b) — 한 모델에 너무 많은 언어를 넣으면 개별 언어 품질이 떨어진다. 둘째, 학습 데이터가 영어·중국어 등 고자원 언어에 극단적으로 쏠려 있다 (XGLM 4.5B는 노르웨이어가 케추아어보다 7,000배 많고, BLOOM은 Akan 8.1M 화자에 대해 70KB 데이터만 사용).</p> <p>이 논문은 그 의심을 정량적으로 확인한다. 저자들은 Hugging Face 분석에서 350개 후보 언어 중 <strong>215개가 단일언어 텍스트 생성 모델이 전혀 존재하지 않으며, 47개는 어떤 텍스트 생성 모델도 없다</strong>는 점을 보여준다 (Arnett and Chang, 2025). 비교 기준조차 없는 이 공백을 메우려 저자들은 단일언어 모델을 <em>언어당 한 개씩</em> 만들어, 누구나 fine-tuning이나 downstream 비교의 베이스라인으로 사용할 수 있도록 공개한다.</p> <p>흥미로운 점은 결과가 거대 모델에 <em>부정적으로</em> 작용한다는 것이다. <strong>125M 파라미터의 GPT-1 급 단일언어 모델이 XGLM 4.5B, BLOOM 7.1B, MaLA-500 10B 보다 perplexity가 낮다.</strong> 게다가 단순한 bigram 모델조차 거대 다국어 모델을 상당수 언어에서 이긴다. 즉 거대 다국어 모델은 저자원 언어에서 <strong>next-token prediction이라는 가장 기초적인 작업조차 학습하지 못한 채</strong> 출시되고 있었다는 해석이다.</p> <p>저자원 언어 NLP가 점점 더 LLM-as-a-service 패러다임에 종속되어 가는 지금, Goldfish는 <em>small monolingual baseline이 여전히 유효하며 오히려 일부 측면에서 우월하다</em> 는 메시지를 제공한다. 동시에 1GB 데이터에 1.65 × 10²⁰ FLOPs 라는, GPT-3 의 1/1900에 불과한 컴퓨팅 예산으로 350개 언어를 커버한다는 점에서, “limited compute lab 도 의미 있는 저자원 모델링이 가능하다” 는 입장도 제시한다.</p> <h2 id="핵심-기여">핵심 기여</h2> <ul> <li><strong>350개 언어 · 1,154개 모델 공개.</strong> 5MB/10MB/100MB/1GB/full 의 다섯 가지 데이터 브래킷으로, byte premium 보정 후 <em>영어 등가 1MB · 2MB · 20MB · 200MB</em> 토큰에 해당하는 양으로 학습. 215개 언어에 대해서는 <em>최초의</em> 단일언어 모델.</li> <li><strong>거대 다국어 모델의 perplexity 우위를 뒤집음.</strong> XGLM 4.5B · BLOOM 7.1B · MaLA-500 10B 모두 FLORES log-perplexity에서 Goldfish 124M에게 평균 perplexity로 진다. 175개 언어 기준 Goldfish vs MaLA-500 승률 63%.</li> <li><strong>거대 다국어 모델 vs bigram 비교.</strong> 단순 stupid backoff bigram 이 XGLM 4.5B의 24% 언어, BLOOM 7.1B의 43% 언어에서 더 낮은 perplexity를 낸다. <em>거대 모델이 저자원 언어에서 기본기를 잃었다</em> 는 명시적 증거.</li> <li><strong>MultiBLiMP grammaticality에서 모든 sub-1B 다국어 모델을 능가.</strong> Goldfish 124M = 78.8 vs Gemma 3 270M = 72.3, BLOOM 560M = 64.2.</li> <li><strong>Byte premium scaling 의 실용적 도입.</strong> 단순 “MB 단위” 비교가 언어마다 인코딩 효율이 다르다는 함정을 어떻게 피하는지 실증.</li> </ul> <h2 id="관련-연구--배경-지식">관련 연구 / 배경 지식</h2> <h3 id="거대-다국어-모델의-한계">거대 다국어 모델의 한계</h3> <p>다국어 사전학습은 두 가지 가설로 정당화돼 왔다. 첫째 <strong>transfer</strong>: 유사 언어 데이터가 저자원 언어 학습을 돕는다 (Conneau et al., 2020b). 둘째 <strong>scale</strong>: 데이터·모델이 커지면 저자원 측 손실도 줄어든다. 두 가설 모두 일부 맞지만 한계가 명확하다.</p> <ul> <li><strong>Curse of multilinguality</strong> (Conneau et al., 2020b; Chang et al., 2024a): 일정 모델 크기에서 언어를 추가할수록 개별 언어 quality가 떨어진다.</li> <li><strong>Data imbalance</strong>: XGLM 4.5B 학습셋은 노르웨이어 (71GB) vs 케추아어 (0.01GB), BLOOM은 Akan 70KB (전체의 4e-6%). 토큰 비율이 이 정도면 모델 입장에서는 “거의 본 적 없는” 언어다.</li> </ul> <p>Goldfish 저자들의 이전 작업 Chang et al. (2024a) 는 250개 언어에서 monolingual vs multilingual을 직접 비교해, <strong>모델 크기가 작을수록 monolingual이 우세하고 데이터가 충분할수록 monolingual이 이긴다</strong> 는 결과를 보였다. Goldfish는 이 결론을 production 형태로 확장한 논문에 해당한다.</p> <h3 id="byte-premium-이란">Byte premium 이란</h3> <p>언어마다 같은 의미의 텍스트를 UTF-8로 인코딩할 때 필요한 바이트 수가 다르다. 영어를 기준 1로 두면, 크메르어 (Khmer, <code class="language-plaintext highlighter-rouge">khm_khmr</code>) 는 같은 의미에 <em>3.91 배</em> 의 바이트가 필요하다. 만약 raw byte 기준으로 “1GB 데이터로 학습했다” 고 비교하면, 크메르어 모델이 본 <em>콘텐츠 양</em> 은 영어 모델의 약 1/3.91 ≈ 26% 에 불과하다. 즉 단순 MB 비교는 fair comparison이 아니다.</p> <p>Arnett et al. (2024) 의 <strong>byte premium tool</strong> 은 언어별 byte premium $b$ 를 측정하거나 문자 엔트로피와 script 타입 (alphabet/abjad/abugida/logography) 으로 회귀 추정한다. Goldfish는 모든 데이터셋 크기를 byte premium 으로 나눠 <strong>content-matched</strong> 크기로 환산한 뒤 5MB/10MB/100MB/1GB로 절단한다. 이 보정 자체는 작은 디테일이지만, 350개 언어를 공정하게 비교하려면 필수적인 전처리다.</p> <h3 id="비교-대상-다국어-모델">비교 대상 다국어 모델</h3> <ul> <li><strong>XGLM</strong> (Lin et al., 2022): 134개 (4.5B) 또는 30개 (7.5B) 언어 GPT-2 계열. 7.5B 모델은 30개 고자원 언어만 다룸.</li> <li><strong>BLOOM</strong> (Scao et al., 2022): 46개 언어, 7.1B / 176B. Akan 같은 극단적 데이터 부족 언어 포함.</li> <li><strong>MaLA-500</strong> (Lin et al., 2024): LLaMA 2 7B/10B 위에 Glot500 코퍼스로 continued pretraining, 534개 언어.</li> <li><strong>MaLA-500 10B</strong>: Goldfish와 가장 직접 비교 가능 (언어 수도 많고 generative).</li> </ul> <h2 id="방법--아키텍처-상세">방법 / 아키텍처 상세</h2> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0015-goldfish/fig1-language-map-480.webp 480w,/assets/img/papers/0015-goldfish/fig1-language-map-800.webp 800w,/assets/img/papers/0015-goldfish/fig1-language-map-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0015-goldfish/fig1-language-map.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1: 좌측은 350개 Goldfish 언어 분포 (Glottolog 좌표). 우측은 영어 모델 (eng_latn) 이 데이터 크기별로 'Goldfish are' 프롬프트에 대해 생성한 결과. 5MB·10MB 모델은 문장이 거의 무너지지만 100MB 부터 문법적 텍스트가 등장하기 시작한다.</figcaption> </figure> <h3 id="아키텍처-선택">아키텍처 선택</h3> <p>Goldfish는 모두 <strong>GPT-2 아키텍처</strong> (Radford et al., 2019) 를 사용한다. 데이터 크기에 따라 두 가지 사이즈:</p> <ul> <li><strong>125M parameters</strong> (100MB / 1GB / full): GPT-1, BERT-base, RoBERTa 와 동급. 12 layers, hidden 768, 12 heads, FFN 3072.</li> <li><strong>39M parameters</strong> (5MB / 10MB): Turc et al. (2019) 의 small BERT 사이즈. 4 layers, hidden 512, 8 heads, FFN 2048.</li> </ul> <p>작은 데이터에 큰 모델을 쓰면 overfitting 이 심해진다 (Chang et al., 2024a 가 실증)는 이전 관찰을 그대로 따른 선택이다. 즉 <em>데이터 양이 적을수록 작은 모델</em>.</p> <h3 id="토크나이저">토크나이저</h3> <p>각 언어별로 <strong>언어 전용 Unigram SentencePiece</strong> 토크나이저를 vocabulary size 50K 로 학습한다 (Kudo and Richardson, 2018). 학습 텍스트는 해당 모델이 쓰는 데이터셋과 같은 데이터셋에서 100MB 단위로 sampling. byte premium scaling 도 동일하게 적용. 토크나이저가 단일언어에 fit 되므로 영어가 아닌 언어에서도 vocab 효율이 좋다.</p> <h3 id="입력-포맷">입력 포맷</h3> <p>토크나이즈 후 텍스트 라인을 concat 해 <strong>정확히 512 토큰</strong> 시퀀스를 만든다. 이때 <em>셔플링은 데이터 샘플링 직전에 수행</em> 해 가능한 한 연속된 문맥을 보존한다 (일부 원본 코퍼스는 이미 shuffled 라 예외). 512 token max sequence는 GPT-2 의 1024 보다 짧지만 작은 데이터에 적합하고 학습 효율을 높인다.</p> <h3 id="학습-하이퍼파라미터">학습 하이퍼파라미터</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0015-goldfish/tab4-hyperparams-480.webp 480w,/assets/img/papers/0015-goldfish/tab4-hyperparams-800.webp 800w,/assets/img/papers/0015-goldfish/tab4-hyperparams-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0015-goldfish/tab4-hyperparams.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 4: 데이터 크기별 하이퍼파라미터. 5MB/10MB 는 39M · 4 layer, 100MB/1GB/full 은 125M · 12 layer. 배치 크기는 5MB=4, 10MB=8, 100MB=32, 1GB=64 로 데이터 양에 비례해 키운다. 학습률은 일괄 1e-4, GELU, 10% warmup.</figcaption> </figure> <ul> <li><strong>학습률</strong>: 1e-4 일괄. GPT-1 의 2.5e-4 보다 작지만, Chang et al. (2024a) 에서 작은 모델에 2e-4 가 잘 동작한다는 보고와 일치 — 작은 학습률이 overfitting 위험을 줄인다.</li> <li><strong>배치 크기</strong>: 5MB=4, 10MB=8, 100MB=32, 1GB=64. full-data 모델은 1GB/100MB/10MB/5MB 중 그 언어가 도달한 최대 카테고리에 해당하는 batch size 사용. 의도는 <em>작은 데이터에서 큰 배치 = 빠른 overfitting</em> 을 피하는 것.</li> <li><strong>Epochs</strong>: 10. 데이터 제약 환경에서는 multi-epoch 가 유리하지만 (Muennighoff et al., 2023), 5MB에서 10 epoch 이상은 eval loss 가 상승.</li> <li><strong>Warmup</strong>: 10% pretraining steps, linear decay.</li> <li><strong>총 컴퓨팅</strong>: 모든 1,154개 모델 합쳐 1.65 × 10²⁰ FLOPs. GPT-3 175B (3.14 × 10²³ FLOPs) 의 1/1900. GPU 시간으로는 ~15,600 A6000 시간.</li> </ul> <h3 id="bigram-베이스라인">Bigram 베이스라인</h3> <p>Goldfish 와 같은 데이터·같은 토크나이저로 <strong>stupid backoff bigram</strong> (Brants et al., 2007) 을 학습한다. $P(w_i \mid w_{i-1})$ 가 0 일 때 unigram 확률에 backoff penalty $\lambda = 0.40$ 을 곱해 보정. 더 높은 $n$-gram 은 backoff 의존도가 너무 커서 제외. 이 단순한 베이스라인이 본문의 핵심 발견 — 거대 다국어 모델이 bigram을 못 이긴다는 결과 — 을 만들어 낸다.</p> <h2 id="학습-목표">학습 목표</h2> <p>표준 GPT-2 의 인과적 언어모델링 (causal LM) 손실:</p> \[\mathcal{L}(\theta) = - \mathbb{E}_{s \sim \mathcal{D}}\left[ \sum_{t=1}^{T} \log P_\theta(w_t \mid w_{&lt;t}) \right]\] <p>여기서 $s$ 는 512 토큰 시퀀스, $w_{&lt;t}$ 는 이전 토큰들, $\theta$ 는 39M 또는 125M 파라미터. 평가에서는 같은 손실의 sequence-level 평균을 perplexity로 환산하는데, 토크나이저 차이를 보정하기 위해 <strong>sentence-level log-perplexity</strong> 를 사용한다:</p> \[\text{LogPPL}_{\mathcal{M}} = \operatorname{mean}_s \Big( - \log P_{\mathcal{M}}(s_1 \mid s_0) \Big)\] <p>각 FLORES 문장 $s$ 를 문자 수 기준 절반으로 나눠 $s_0, s_1$ 으로 두고, 첫 절반을 prompt로 둘째 절반의 log-probability를 평가한다. 절반 분할 지점은 가까운 subword 토큰 경계로 반올림. 토크나이저마다 token 수가 달라도 <em>입력 prompt $s_0$</em> 가 동일한 콘텐츠를 담도록 해 다른 모델과 fair comparison이 된다. [UNK] 토큰은 $1/|\mathcal{V}|$ 로 처리해 unseen script 가 아닌 한 perplexity에 영향이 거의 없게 한다.</p> <p>이 방식은 단순 perplexity 비교의 잘 알려진 함정 — 토크나이저가 더 짧게 인코딩하는 모델이 자동으로 perplexity 가 낮아 보이는 — 을 피하는 데 중요하다.</p> <h2 id="학습-데이터와-파이프라인">학습 데이터와 파이프라인</h2> <h3 id="데이터-출처">데이터 출처</h3> <p>총 ~80개의 코퍼스를 통합한다. 큰 흐름은:</p> <ol> <li><strong>Chang et al. (2024a)</strong> 코퍼스 — OSCAR, Wikipedia, NLLB seed, eBible, AfriBERTa, NusaX 등 ~40개 소스.</li> <li><strong>Glot500</strong> (Imani et al., 2023) — 511개 언어를 다루는 corpus, Chang et al. 코퍼스 미포함 언어만 병합.</li> <li><strong>MADLAD-400</strong> (Kudugunta et al., 2023) clean split — CommonCrawl 기반. 위 두 소스로도 1GB 미달인 언어만 추가.</li> <li>일부 언어는 <em>averaged line length가 25 token 미만</em>이면 MADLAD-400 contiguous sequence 도 추가 (라인이 너무 짧으면 LM 학습 신호가 약해지므로).</li> </ol> <h3 id="전처리">전처리</h3> <ul> <li><strong>중복 제거</strong>: 동일 100 UTF-8 바이트 시퀀스 단위 dedup (Lee et al., 2022). 같은 언어가 여러 코퍼스에 등장하므로 필수.</li> <li><strong>언어 코드 정규화</strong>: ISO 639-3 + ISO 15924 (예: <code class="language-plaintext highlighter-rouge">khm_khmr</code> = 크메르어 + 크메르 문자). macrolanguage (<code class="language-plaintext highlighter-rouge">que_latn</code> 케추아어 일반) 와 individual language (<code class="language-plaintext highlighter-rouge">quy_latn</code> Ayacucho Quechua, <code class="language-plaintext highlighter-rouge">quz_latn</code> Cusco Quechua) 둘 다 유지. 일부 코드는 redundancy 로 drop (<code class="language-plaintext highlighter-rouge">ory_orya</code> Odia → macrocode <code class="language-plaintext highlighter-rouge">ori_orya</code> 로 통합).</li> <li><strong>FLORES / AmericasNLI 데이터 제거</strong>: 평가 leakage 방지. 그래도 인터넷 텍스트 contamination 위험이 있어, FLORES sequence의 첫 10 토큰이 학습 데이터에 몇 번 등장하는지 검사. 결과는 <em>98%의 언어에서 2000개 FLORES sequence 중 10개 미만 contamination</em>. 예외는 Samoan (7,155회) 과 Central Kanuri (371회) — 보고는 하되 제거는 하지 않음.</li> <li><strong>Bible-only 언어 drop</strong>: 텍스트가 성경 번역밖에 없는 언어는 domain bias 가 너무 커서 제외.</li> </ul> <h3 id="최종-데이터-통계">최종 데이터 통계</h3> <ul> <li>총 547GB · 523 language-script 조합 · 486 unique language code · 32 script.</li> <li>Goldfish 모델 학습은 350 언어 (5MB 미만 언어 제외) · 1154 모델 (5MB 350개 + 10MB 288개 + 100MB 166개 + 1GB 83개 + full 267개).</li> <li>1GB 데이터셋은 byte premium 보정 후이므로 <em>실제 바이트 양은 0.7~5.0배 변동</em>. 가장 high-premium 언어는 미얀마어 <code class="language-plaintext highlighter-rouge">mya_mymr</code> (5.0× clip).</li> </ul> <h3 id="학습-환경">학습 환경</h3> <p>NVIDIA GTX TITAN X, RTX 2080 Ti, TITAN Xp, Quadro P6000, RTX A4500/A5000/A6000 GPU 다양하게 혼합. 단일 GPU pretraining. <strong>전체 1,154개 모델 학습에 ~15,600 A6000 GPU 시간</strong> (약 651 GPU·일). Inference 평가 (FLORES + 4 reasoning 벤치마크) 에는 ~250 A6000 시간 추가 — 평가에 더 많은 시간이 드는 이유는 비교군의 대형 다국어 모델 inference 때문.</p> <h2 id="실험-결과">실험 결과</h2> <h3 id="flores-log-perplexity">FLORES log-perplexity</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0015-goldfish/tab1-flores-perplexity-480.webp 480w,/assets/img/papers/0015-goldfish/tab1-flores-perplexity-800.webp 800w,/assets/img/papers/0015-goldfish/tab1-flores-perplexity-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0015-goldfish/tab1-flores-perplexity.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 1: XGLM 4.5B · MaLA-500 모두에 포함된 100개 언어에 대한 평균 FLORES perplexity. Goldfish는 모든 데이터 브래킷에서 다국어 baseline을 누른다. 10MB/5MB 의 5개 언어 한정 비교에서도 130.5 &lt; 148.3 (Bigrams) &lt; 183.1 (XGLM 4.5B).</figcaption> </figure> <p>핵심 결과 (Goldfish 가 1GB 모델을 가진 73개 언어 평균):</p> <ul> <li>Goldfish 76.9 — Bigrams 112.3 — XGLM 4.5B 78.6 — MaLA-500 10B 84.7</li> </ul> <p>이 73 + 22 + 5 = 100 언어 (XGLM 4.5B · MaLA-500 · FLORES 모두 포함) 가 Goldfish 최대 데이터 카테고리 별로 분리된 결과다. 흥미로운 점은 <strong>10MB/5MB 카테고리에서도 Goldfish가 XGLM 4.5B (183.1) 를 큰 차이로 이긴다 (130.5)</strong>. 즉 5MB 단일언어 모델이 4.5B 다국어 모델보다 좋은 next-token predictor라는 충격적인 결과.</p> <h3 id="bigram이-거대-다국어-모델을-이긴다">Bigram이 거대 다국어 모델을 이긴다</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0015-goldfish/tab2-win-rates-480.webp 480w,/assets/img/papers/0015-goldfish/tab2-win-rates-800.webp 800w,/assets/img/papers/0015-goldfish/tab2-win-rates-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0015-goldfish/tab2-win-rates.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 2: pairwise 승률. Goldfish 는 XGLM 4.5B 의 102개 공통 언어 중 60개 (59%), MaLA-500 의 175개 중 111개 (63%) 에서 더 낮은 log-perplexity. Bigram 자체도 XGLM 4.5B 의 24%, BLOOM 7.1B 의 43% 언어에서 우세.</figcaption> </figure> <p>이 표가 논문의 가장 강한 메시지를 담는다. 두 가지 신호:</p> <ol> <li><strong>Goldfish vs 거대 다국어</strong>: 102개 언어 중 60개 (59%) 에서 XGLM 4.5B 보다 우세. MaLA-500 10B 와는 63%. 유일한 예외는 XGLM 7.5B (30개 고자원 언어만 다룸) — 거기서는 Goldfish 2/30 만 승.</li> <li><strong>Bigram vs 거대 다국어</strong>: 단순 backoff bigram 도 XGLM 4.5B 의 102개 중 24개, BLOOM 7.1B 의 46개 중 20개 (43%) 에서 perplexity 가 더 낮다. 이는 <em>거대 다국어 모델이 일부 저자원 언어에서 unigram 분포조차 제대로 잡지 못함</em> 을 시사한다.</li> </ol> <p>XGLM 7.5B 만 거의 모든 비교를 이기는데, 이는 그 모델이 <em>오직 30개 고자원 언어</em> 만 다루기 때문이다. 즉 <em>적게 분산하면 잘하지만 많이 분산하면 못 한다</em> 는 curse of multilinguality의 정량적 재확인.</p> <h3 id="다운스트림-벤치마크">다운스트림 벤치마크</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0015-goldfish/tab3-downstream-480.webp 480w,/assets/img/papers/0015-goldfish/tab3-downstream-800.webp 800w,/assets/img/papers/0015-goldfish/tab3-downstream-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0015-goldfish/tab3-downstream.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 3: MultiBLiMP (74개 언어 grammaticality), Belebele (121언어 reading comprehension), XCOPA (11언어 commonsense), XStoryCloze (10언어 story commonsense) 평균. Goldfish 는 MultiBLiMP에서 78.8 로 모든 sub-1B 다국어 모델을 능가하고 Gemma 3 1B (78.0) 도 살짝 앞선다. 그러나 reasoning task 들에서는 모든 small model이 chance 수준.</figcaption> </figure> <ul> <li><strong>MultiBLiMP</strong> (grammaticality, 74개 언어): Goldfish 124M = <strong>78.8</strong> (최고). Gemma 3 270M = 72.3, BLOOM 560M = 64.2, XGLM 564M = 66.6. 심지어 Gemma 3 1B (78.0), LLaMA 3 1B (77.4) 도 살짝 능가.</li> <li><strong>Belebele</strong> (121개 언어 reading comprehension): 전부 chance 수준 (25~29). Goldfish 28.2, XGLM 564M 29.0.</li> <li><strong>XCOPA / XStoryCloze</strong>: 전부 chance + ~5%. 1B 미만 모델은 사실상 의미 있는 reasoning을 하지 못한다.</li> </ul> <p>이 결과는 두 가지 해석을 부른다. 첫째, <strong>small monolingual 은 <em>언어 지식</em> 측면에서 비례 이상의 효과</strong>가 있다 — MultiBLiMP는 fine-tuning 없이 next-token log-probability ranking 만으로 평가하니, 모델의 grammatical competence가 직접 측정된다. 둘째, <strong>reasoning은 사이즈 의존적이라 1B 미만 으로는 안 됨</strong> — Goldfish가 perplexity 에서 거대 모델을 이겨도 reasoning 은 못 한다.</p> <h2 id="결과-분석">결과 분석</h2> <h3 id="왜-작은-단일언어-모델이-거대-다국어-모델을-이기는가">왜 작은 단일언어 모델이 거대 다국어 모델을 이기는가</h3> <p>저자들이 명시적으로 ablate 하진 않지만, 다음 요인들이 추론 가능하다:</p> <ol> <li><strong>Capacity allocation</strong>: 4.5B 파라미터를 134개 언어로 나누면 언어당 평균 34M 파라미터 상당. Goldfish 124M 은 그 4배 + 모든 capacity 가 그 언어에만 헌신.</li> <li><strong>Tokenizer fit</strong>: 각 Goldfish 모델은 <em>자기 언어 50K vocab</em>. 다국어 모델은 100K~250K vocab을 100~500 언어로 나누므로 저자원 언어가 byte-level fallback에 가까워진다.</li> <li><strong>Data balancing</strong>: byte premium scaling 으로 1GB content-matched. 다국어 모델은 sampling temperature를 써도 결국 high-resource 가 dominant.</li> </ol> <p>24% / 43% 의 bigram 우세는 특히 흥미롭다. bigram이 거대 LM 을 이기려면 LM의 next-token 분포가 unigram 분포보다도 멀어져야 하는데, 이는 모델이 <em>그 언어를 거의 학습하지 못했음</em> 을 의미한다.</p> <h3 id="contamination-분석">Contamination 분석</h3> <p>FLORES leakage가 의심된다면 결과는 무효일 수 있다. 저자들은 FLORES 첫 10 토큰을 학습 데이터에서 검색해 <strong>98% 언어에서 10개 미만 contamination</strong> 을 확인한다. 예외는 사모아어 (smo_latn) 7,155회와 Central Kanuri (knc_arab) 371회. 본 결과는 이 예외 언어를 포함한 평균이므로, 제거해도 큰 그림은 변하지 않을 것 (저자도 거기까진 verify 하지 않음).</p> <h3 id="reasoning-한계의-의미">Reasoning 한계의 의미</h3> <p>MultiBLiMP 외 모든 reasoning task에서 chance 라는 것은 단순 <em>모델 사이즈 부족</em> 으로 해석된다. 같은 표 Table 3에서 Gemma 3 1B 와 LLaMA 3 1B 도 XCOPA/XStoryCloze 에서 56~60 수준에 머문다 (chance 50). 즉 instruction tuning이나 prompting이 없는 base LM 평가에서는, 1B 미만 어떤 모델도 의미 있는 reasoning을 하지 못한다는 것이 일반적 현상이다. Goldfish는 그 결과를 <em>350개 언어로 확장</em> 해 확인한 셈.</p> <h2 id="한계와-비판적-평가">한계와 비판적 평가</h2> <p>저자가 명시한 한계:</p> <ul> <li><strong>Comparability and availability</strong>: 5MB 가 모든 언어에 가용한 건 아니다 (350 → 5MB 350개, 단 데이터가 그 이하인 다수 언어는 제외됨). 또 동일 분량이라 해도 코퍼스 quality와 domain (뉴스 vs 소셜 vs 종교) 이 언어마다 달라 <em>완벽한</em> comparability는 불가능.</li> <li><strong>Monolinguality</strong>: 의도된 design choice 지만, 유사 언어로 transfer가 가능한 경우 (Conneau et al., 2020b) 손해를 본다. 즉 <em>비교 가능성과 성능 사이의 trade-off</em>.</li> <li><strong>Model and dataset size caps at 1GB · 125M</strong>: 저자원에 집중한 선택. high-resource 언어에서는 GPT-3 와 비교하면 사이즈가 너무 작다.</li> <li><strong>Downstream limited to perplexity + grammaticality</strong>: reasoning은 1B 미만이면 의미 없으므로 평가에서 사실상 제외. 즉 <em>Goldfish 가 정말 좋은 모델인가</em> 는 next-token 예측 + 문법성 한정.</li> <li><strong>Risks and dataset licensing</strong>: PII / offensive content 보장 불가. publicly released corpora 만 사용했지만 그 자체도 검수가 부족할 수 있음.</li> </ul> <p>리뷰어 관점의 추가 한계:</p> <ul> <li><strong>Bigram 비교의 fairness</strong>: bigram도 같은 토크나이저·같은 학습 데이터를 쓴다고 했지만, 의문 — bigram은 long-range context를 못 보고도 거대 LM 을 24% 이긴다. 이는 <em>거대 LM 이 long-range를 활용 못 한다</em> 는 의미인지, <em>FLORES 평가가 short context라 short-context 모델에 유리</em> 한지 구분이 어렵다. FLORES sequence를 두 반으로 나누는 평가 방식 상, prompt 길이가 충분해 long-range 가 거의 의미 없을 가능성이 있다.</li> <li><strong>Goldfish vs 거대 모델 의 컴퓨팅 비교</strong>: 1.65 × 10²⁰ FLOPs 가 GPT-3 의 1/1900 이라 적게 들었다고 강조하지만, <em>언어당</em> FLOPs 는 1.43 × 10¹⁷. 이는 영어 GPT-3 의 약 1/2,200,000. 비교 perspective 가 <em>언어당</em> 인지 <em>전체</em> 인지에 따라 메시지가 다르다.</li> <li><strong>단일언어 가정의 현실성</strong>: 저자원 언어 화자들이 실제 NLP application에서 원하는 것은 <em>그 언어 monolingual</em> 보다는 <em>그 언어와 영어 (또는 다른 high-resource lang) 사이 task</em> 일 가능성이 크다. Goldfish는 그 use case 에는 직접 적용 못 한다 — fine-tuning 또는 추가 코드를 거쳐야 함.</li> <li><strong>모델 호스팅·유지보수</strong>: 1,154개 모델을 HuggingFace 에 모두 올렸지만, 장기 유지에 대한 commitment 가 명시되지 않음. 저자원 언어 community에 핵심 자원이 되려면 model card · maintenance 약속이 필요.</li> </ul> <h2 id="시사점--takeaways">시사점 / Takeaways</h2> <ul> <li><strong>거대 다국어 모델이 모든 답이 아니다.</strong> 저자원 언어에서는 4.5B 모델이 39M 모델에게 perplexity 로 진다. 무조건적 scaling-up 보다 <em>언어별 capacity allocation</em> 에 대한 진지한 사고가 필요.</li> <li><strong>Bigram이 거대 LM 을 24~43% 언어에서 이긴다는 사실은 충격적이다.</strong> 이는 다국어 LM training 의 sampling 전략·data balancing이 <em>예상보다 훨씬</em> 저자원 언어를 망치고 있다는 정량적 증거. 차세대 다국어 모델 설계자들은 이 결과를 직접 반증할 의무가 있다.</li> <li><strong>저자원 NLP 의 baseline 문제 해결.</strong> 215개 언어가 처음으로 monolingual LM 베이스라인을 가진다는 점만으로도 이 논문의 가치는 크다. fine-tuning 비교, 데이터 quality 측정, language ID 검증 등 다양한 후속 작업의 launch pad.</li> <li><strong>Byte premium 보정은 multilingual 비교에서 default가 되어야 한다.</strong> Goldfish 의 fair comparison 결과는 byte premium 없으면 성립하지 않는다. 다국어 평가 논문이라면 이 보정을 무시할 이유가 없다.</li> <li><strong>1B 미만 모델로 reasoning은 안 된다.</strong> Goldfish 가 perplexity 에서 이겨도 XCOPA / Belebele 은 chance. 저자원 NLP 의 미래가 <em>small monolingual LM + 다른 무언가</em> 인지, <em>큰 monolingual</em> 인지, <em>효율적 transfer 가 있는 다국어</em> 인지는 여전히 열린 질문.</li> </ul> <h2 id="설치-및-사용법">설치 및 사용법</h2> <p>모든 모델은 Hugging Face Hub 에서 사용 가능. 모델 이름은 <code class="language-plaintext highlighter-rouge">goldfish-models/&lt;lang_script&gt;_&lt;size&gt;</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">AutoTokenizer</span><span class="p">,</span> <span class="n">AutoModelForCausalLM</span>

<span class="c1"># 1GB 영어 모델
</span><span class="n">tokenizer</span> <span class="o">=</span> <span class="n">AutoTokenizer</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">goldfish-models/eng_latn_1000mb</span><span class="sh">"</span><span class="p">)</span>
<span class="n">model</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">goldfish-models/eng_latn_1000mb</span><span class="sh">"</span><span class="p">)</span>

<span class="n">prompt</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Goldfish are</span><span class="sh">"</span>
<span class="n">ids</span> <span class="o">=</span> <span class="nf">tokenizer</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">return_tensors</span><span class="o">=</span><span class="sh">"</span><span class="s">pt</span><span class="sh">"</span><span class="p">).</span><span class="n">input_ids</span>
<span class="n">out</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">ids</span><span class="p">,</span> <span class="n">max_new_tokens</span><span class="o">=</span><span class="mi">20</span><span class="p">,</span> <span class="n">do_sample</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">tokenizer</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="n">out</span><span class="p">[</span><span class="mi">0</span><span class="p">]))</span>
<span class="c1"># → "Goldfish are a species of fish that are found in the sea."
</span></code></pre></div></div> <p>다른 사이즈는 suffix 만 바꿔서 (<code class="language-plaintext highlighter-rouge">_5mb</code>, <code class="language-plaintext highlighter-rouge">_10mb</code>, <code class="language-plaintext highlighter-rouge">_100mb</code>, <code class="language-plaintext highlighter-rouge">_full</code>), 다른 언어는 ISO 639-3 + ISO 15924 코드로 (<code class="language-plaintext highlighter-rouge">khm_khmr</code>, <code class="language-plaintext highlighter-rouge">swa_latn</code>, <code class="language-plaintext highlighter-rouge">mya_mymr</code> 등). 학습 데이터는 별도 데이터셋 hub <code class="language-plaintext highlighter-rouge">goldfish-models/fish-food</code> 에서.</p> <p>평가 코드 (FLORES log-perplexity, contamination check) 는 GitHub repo 의 <code class="language-plaintext highlighter-rouge">evaluation/</code> 에 있다.</p> <h2 id="참고-자료">참고 자료</h2> <ul> <li>논문: <a href="https://arxiv.org/abs/2408.10441">https://arxiv.org/abs/2408.10441</a></li> <li>코드: <a href="https://github.com/tylerachang/goldfish">https://github.com/tylerachang/goldfish</a></li> <li>모델 (Hugging Face): <a href="https://huggingface.co/goldfish-models">https://huggingface.co/goldfish-models</a></li> <li>데이터셋: <a href="https://huggingface.co/datasets/goldfish-models/fish-food">https://huggingface.co/datasets/goldfish-models/fish-food</a></li> </ul> <h2 id="더-읽어보기">더 읽어보기</h2> <ul> <li><strong><a href="https://arxiv.org/abs/2311.09205">When Is Multilinguality a Curse? Language Modeling for 250 High- and Low-Resource Languages</a></strong> (Chang et al., EMNLP 2024) — Goldfish 의 직접적 전신. 250개 언어에서 monolingual vs multilingual 을 체계적으로 비교해, 작은 모델 + 충분한 단일언어 데이터 = monolingual 우세라는 결론.</li> <li><strong><a href="https://arxiv.org/abs/2403.00686">A Bit of a Problem: Measurement Disparities in Dataset Sizes Across Languages</a></strong> (Arnett et al., SIGUL @ LREC-COLING 2024) — Goldfish 가 사용하는 byte premium 도구의 원논문. 1,155개 언어의 byte premium을 측정·회귀 추정, 다국어 데이터셋 fair comparison의 기반.</li> <li><strong><a href="https://arxiv.org/abs/2305.12182">Glot500: Scaling Multilingual Corpora and Language Models to 500 Languages</a></strong> (Imani et al., ACL 2023) — Goldfish 가 데이터 소스 중 하나로 사용하는 511 언어 코퍼스. XLM-R 위에 continued pretraining 한 Glot500-m 도 함께 공개.</li> <li><strong><a href="https://arxiv.org/abs/2401.13303">MaLA-500: Massive Language Adaptation of Large Language Models</a></strong> (Lin et al., 2024) — Goldfish 와 가장 직접 비교되는 baseline. LLaMA 2 10B + Glot500 continued pretraining 으로 534개 언어 커버.</li> <li><strong><a href="https://arxiv.org/abs/2112.10668">Few-shot Learning with Multilingual Language Models (XGLM)</a></strong> (Lin et al., EMNLP 2022) — Goldfish 의 핵심 비교 대상 XGLM 4.5B/7.5B. 134/30 언어 GPT-2 계열 다국어 모델.</li> <li><strong><a href="https://arxiv.org/abs/2504.02768">MultiBLiMP 1.0: A Massively Multilingual Benchmark of Linguistic Minimal Pairs</a></strong> (Jumelet et al., TACL 2026) — Goldfish 가 grammaticality 평가에 사용. Universal Dependencies + UniMorph 기반 자동 minimal pair 생성, 101개 언어 12.8만 쌍.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="low-resource-language"/><category term="monolingual-lm"/><category term="multilingual-nlp"/><category term="perplexity"/><category term="pretraining"/><summary type="html"><![CDATA[350개 저자원 언어를 위한 1,154개의 작은 단일언어 GPT-2 모델 모음. 1GB 이하 코퍼스에서 5MB~1GB 데이터로 학습한 39M~125M 모델이 XGLM 4.5B·BLOOM 7.1B·MaLA-500 10B 같은 거대 다국어 모델보다 일관되게 낮은 perplexity를 보인다.]]></summary></entry><entry xml:lang="en"><title type="html">[Paper Review] Goldfish: Monolingual Language Models for 350 Languages</title><link href="https://baba9811.github.io/en/papers/0015-goldfish/" rel="alternate" type="text/html" title="[Paper Review] Goldfish: Monolingual Language Models for 350 Languages"/><published>2026-05-14T14:00:00+00:00</published><updated>2026-05-14T14:00:00+00:00</updated><id>https://baba9811.github.io/en/papers/0015-goldfish</id><content type="html" xml:base="https://baba9811.github.io/en/papers/0015-goldfish/"><![CDATA[<div class="lang-toggle" style="margin-bottom: 1.5rem;"> <a href="/papers/0015-goldfish/" class="btn btn-sm btn-outline-secondary">🇰🇷 한국어로 읽기</a> </div> <h2 id="overview">Overview</h2> <table> <thead> <tr> <th>Field</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Authors</td> <td>Tyler A. Chang, Catherine Arnett, Zhuowen Tu, Benjamin K. Bergen (UC San Diego)</td> </tr> <tr> <td>Venue</td> <td>arXiv preprint · 2026 (cs.CL, v2)</td> </tr> <tr> <td>arXiv</td> <td><a href="https://arxiv.org/abs/2408.10441">2408.10441</a></td> </tr> <tr> <td>Code</td> <td><a href="https://github.com/tylerachang/goldfish">tylerachang/goldfish</a> · <a href="https://huggingface.co/goldfish-models">goldfish-models on Hugging Face</a></td> </tr> <tr> <td>Data</td> <td>OSCAR · Glot500 · MADLAD-400 · NLLB seed · Wikipedia · ~80 other corpora · capped at 5MB–1GB after byte-premium scaling</td> </tr> <tr> <td><span style="white-space: nowrap">Review date</span></td> <td>2026-05-14</td> </tr> </tbody> </table> <h2 id="tldr">TL;DR</h2> <ul> <li>Goldfish is a suite of 1,154 GPT-2-style monolingual language models trained for 350 languages at five dataset brackets (5MB, 10MB, 100MB, 1GB, full). The 1GB models are 125M parameters; the 5/10MB models are 39M.</li> <li>Dataset sizes are normalised by <strong>byte premium</strong> so that “1GB” means content-matched 1GB, not raw bytes of the target language — without this normalisation, languages with bulky UTF-8 encodings (e.g. Khmer at 3.91×) would receive a quarter of the actual training content.</li> <li>The small monolingual models <strong>outperform XGLM 4.5B, BLOOM 7.1B, and MaLA-500 10B on FLORES perplexity</strong> — beating MaLA-500 on 111 of 175 shared languages (63%).</li> <li>Strikingly, plain <strong>stupid-backoff bigrams beat XGLM 4.5B on 24% of languages and BLOOM 7.1B on 43%</strong>. Massively multilingual models have not learned basic next-token prediction for many low-resource languages.</li> <li>The catch: reasoning benchmarks (Belebele, XCOPA, XStoryCloze) are near chance for every sub-1B model, Goldfish included. Better perplexity does not translate into reasoning at this scale.</li> </ul> <h2 id="introduction">Introduction</h2> <p>Most current research in low-resource language modeling leans on <strong>massively multilingual models</strong> — XLM-R, BLOOM, XGLM, MaLA-500 — covering tens to hundreds of languages with a single set of parameters, on the intuition that multilingual data should help languages with too little of their own. Two well-known headaches challenge this intuition. First, the <em>curse of multilinguality</em> (Conneau et al., 2020b): squeeze too many languages into one model and per-language quality drops. Second, training data is grotesquely skewed — XGLM 4.5B’s Norwegian corpus is 7,000× the size of its Quechua corpus, and BLOOM trained on 70KB of Akan text despite 8.1M native speakers.</p> <p>This paper quantifies the consequences. The authors note that of the 350 candidate languages, <strong>215 had no monolingual text-generation model at all before Goldfish, and 47 had no text-generation model of any kind</strong> (Arnett and Chang, 2025). To fill that gap they release one monolingual model per language so the community has at least a baseline — for fine-tuning, for downstream comparison, for measuring data quality, and for sanity-checking language identification.</p> <p>The surprising thing is that the baseline turns out to be <em>better</em> than the massively multilingual models. <strong>A 125M-parameter GPT-1-sized monolingual model achieves lower perplexity than XGLM 4.5B, BLOOM 7.1B, and MaLA-500 10B</strong>, and even simple bigram models edge out the large models on a non-trivial fraction of languages. The interpretation: large multilingual LMs are being released without having learned the most basic task — next-token prediction — for many of the languages they nominally cover.</p> <p>At a moment when low-resource NLP is increasingly pulled toward an LLM-as-a-service paradigm, Goldfish makes the case that small monolingual baselines remain useful, and in some respects superior. The compute budget — 1.65 × 10²⁰ FLOPs total, about 1/1900 of GPT-3’s training run — also frames this as something a compute-limited lab can replicate.</p> <h2 id="key-contributions">Key Contributions</h2> <ul> <li><strong>1,154 monolingual models across 350 languages</strong>, in five dataset brackets (5MB / 10MB / 100MB / 1GB / full) after byte-premium scaling. For 215 of these languages, Goldfish is <em>the first</em> publicly available monolingual model.</li> <li><strong>Beating massively multilingual baselines on FLORES log-perplexity.</strong> Goldfish 124M outperforms XGLM 4.5B, BLOOM 7.1B, and MaLA-500 10B on the 100 languages they all share — and wins on 63% of the 175 languages shared with MaLA-500.</li> <li><strong>Documenting that simple bigrams beat large multilingual LMs.</strong> Stupid-backoff bigrams reach lower log-perplexity than XGLM 4.5B on 24% of languages and BLOOM 7.1B on 43%. Direct evidence that large multilingual models fail at basic LM for many low-resource languages.</li> <li><strong>Best-in-class grammaticality among sub-1B models.</strong> On MultiBLiMP, Goldfish 124M scores 78.8, beating Gemma 3 270M (72.3), BLOOM 560M (64.2), and even Gemma 3 1B (78.0) by a hair.</li> <li><strong>A clean, reproducible use of byte-premium scaling</strong> for cross-lingual dataset-size comparison, demonstrated across 350 languages.</li> </ul> <h2 id="background-and-related-work">Background and Related Work</h2> <h3 id="limits-of-massively-multilingual-pretraining">Limits of massively multilingual pretraining</h3> <p>Multilingual pretraining has been justified along two lines. The first is <strong>transfer</strong>: similar-language data helps a target low-resource language (Conneau et al., 2020b). The second is <strong>scale</strong>: bigger models and bigger data should reduce per-language loss. Both hold partially but break in obvious ways.</p> <ul> <li><strong>Curse of multilinguality</strong> (Conneau et al., 2020b; Chang et al., 2024a): for a given model size, adding languages eventually hurts per-language quality.</li> <li><strong>Severe data imbalance</strong>: XGLM 4.5B sees Norwegian (71GB; 5.4M speakers) vs Quechua (0.01GB; 7.3M speakers); BLOOM was trained on 0.07MB of Akan (4e-6% of pretraining). From the model’s perspective these are essentially unseen languages.</li> </ul> <p>Goldfish’s authors’ prior paper Chang et al. (2024a) compared monolingual vs multilingual setups across 250 languages with up to 45M-parameter models, finding that <strong>smaller models and bigger per-language datasets both favour monolingual</strong>. Goldfish is essentially that result productionised — 1,154 ready-to-use models built on the same hypothesis.</p> <h3 id="what-byte-premium-is">What byte premium is</h3> <p>Different languages need different numbers of UTF-8 bytes to encode equivalent content. English is the baseline; Khmer (<code class="language-plaintext highlighter-rouge">khm_khmr</code>) needs about 3.91× as many bytes for the same meaning. If you compare two “1GB models,” the English one has seen ~10× more content than the Khmer one. Naive byte-based dataset sizes are not a fair basis for cross-lingual comparison.</p> <p>Arnett et al. (2024) introduced a <strong>byte premium tool</strong> that measures or regresses the per-language premium $b$ using character entropy and script type (alphabet/abjad/abugida/logography). Goldfish applies this scaling everywhere — dataset sizes are reported as English-equivalent MB, not raw bytes. A small accounting choice, but without it the cross-lingual conclusions wouldn’t be well-defined.</p> <h3 id="baselines">Baselines</h3> <ul> <li><strong>XGLM</strong> (Lin et al., 2022): GPT-style multilingual LM, 4.5B params for 134 languages or 7.5B for 30 high-resource languages.</li> <li><strong>BLOOM</strong> (Scao et al., 2022): 46-language model at 7.1B and 176B params, includes very-low-resource cases like Akan.</li> <li><strong>MaLA-500</strong> (Lin et al., 2024): LLaMA 2 7B/10B continued-pretrained on Glot500 for 534 languages — the most direct apples-to-apples comparison.</li> </ul> <h2 id="method-and-architecture">Method and Architecture</h2> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0015-goldfish/fig1-language-map-480.webp 480w,/assets/img/papers/0015-goldfish/fig1-language-map-800.webp 800w,/assets/img/papers/0015-goldfish/fig1-language-map-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0015-goldfish/fig1-language-map.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1. Left: the 350 Goldfish languages plotted using Glottolog coordinates; colour encodes the largest dataset bracket available for that language. Right: greedy completions of 'Goldfish are' by the English model at each dataset size. 5MB and 10MB completions are mostly broken loops; grammatical English emerges at 100MB.</figcaption> </figure> <h3 id="architecture-choice">Architecture choice</h3> <p>All Goldfish use <strong>GPT-2</strong> (Radford et al., 2019) with two sizes determined by dataset bracket:</p> <ul> <li><strong>125M parameters</strong> for 100MB / 1GB / full — equivalent to GPT-1, BERT-base, RoBERTa. 12 layers, hidden 768, 12 heads, FFN 3072.</li> <li><strong>39M parameters</strong> for 5MB / 10MB — Turc et al. (2019)’s small BERT. 4 layers, hidden 512, 8 heads, FFN 2048.</li> </ul> <p>The principle is <em>small data → small model</em> to avoid overfitting, validated in Chang et al. (2024a). Larger models hurt on very small datasets even after careful regularisation.</p> <h3 id="tokenizer">Tokenizer</h3> <p>Each language gets its own <strong>Unigram SentencePiece</strong> tokenizer with 50K vocab (Kudo and Richardson, 2018), trained on a 100MB-capped sample of the same dataset used for the corresponding model. Because the tokenizer is monolingual it stays efficient for non-English languages, which would otherwise approach byte-level fallback under a shared multilingual vocab.</p> <h3 id="input-format">Input format</h3> <p>After tokenisation, text lines are concatenated into <strong>exactly 512-token sequences</strong>. Shuffling happens just before sampling so that contiguous context is preserved where possible (some upstream corpora are already shuffled, in which case this is moot). 512 is short by modern standards but reasonable for small datasets and keeps training fast.</p> <h3 id="training-hyperparameters">Training hyperparameters</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0015-goldfish/tab4-hyperparams-480.webp 480w,/assets/img/papers/0015-goldfish/tab4-hyperparams-800.webp 800w,/assets/img/papers/0015-goldfish/tab4-hyperparams-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0015-goldfish/tab4-hyperparams.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 4. Pretraining hyperparameters per dataset bracket. 5MB/10MB use a 39M setup, 100MB/1GB/full use 125M. Batch size scales with data: 4, 8, 32, 64 for 5MB, 10MB, 100MB, 1GB respectively. Learning rate is 1e-4 across the board; 10% warmup, linear decay.</figcaption> </figure> <ul> <li><strong>Learning rate</strong>: 1e-4 uniformly, smaller than GPT-1’s 2.5e-4, motivated by Chang et al. (2024a)’s finding that 2e-4 works for small models — smaller LRs reduce overfit on tiny data.</li> <li><strong>Batch size</strong>: 4 / 8 / 32 / 64 for 5MB / 10MB / 100MB / 1GB. Larger batches overfit small data quickly. Full-data models use the batch size of the largest bracket they reach.</li> <li><strong>Epochs</strong>: 10. Multi-epoch helps in data-constrained regimes (Muennighoff et al., 2023), but past 10 epochs eval loss starts to climb in the 5MB bracket.</li> <li><strong>Warmup</strong>: 10% of pretraining steps, linear decay thereafter.</li> <li><strong>Compute</strong>: ~15,600 A6000 GPU hours for all 1,154 models, totaling 1.65 × 10²⁰ FLOPs — about 1/1900 of GPT-3’s training.</li> </ul> <h3 id="bigram-baseline">Bigram baseline</h3> <p>A <strong>stupid-backoff bigram</strong> (Brants et al., 2007) is trained per language on the same tokenized data: $P(w_i \mid w_{i-1})$ from raw counts, with backoff to unigram weighted by $\lambda = 0.40$ when the bigram is unobserved. Higher-order $n$-grams are excluded because they back off too often. This tiny baseline is what makes the central headline result — large multilingual LMs failing to beat bigrams — possible.</p> <h2 id="training-objective">Training Objective</h2> <p>The standard causal LM loss for GPT-2:</p> \[\mathcal{L}(\theta) = - \mathbb{E}_{s \sim \mathcal{D}}\left[ \sum_{t=1}^{T} \log P_\theta(w_t \mid w_{&lt;t}) \right]\] <p>with $s$ a 512-token sequence and $\theta$ the 39M or 125M parameters. Evaluation uses a <strong>sentence-level log-perplexity</strong> to neutralise tokenizer differences across compared models:</p> \[\text{LogPPL}_{\mathcal{M}} = \operatorname{mean}_s \Big( - \log P_{\mathcal{M}}(s_1 \mid s_0) \Big)\] <p>Each FLORES sentence $s$ is cut in half by character count into $s_0$, $s_1$. The model gets $s_0$ as prompt and is scored on log-probability of $s_1$, rounded to the nearest subword boundary. [UNK] is handled at $1/|\mathcal{V}|$, neutralising the trivial trick where unseen scripts artificially compress perplexity. The setup matters: a naive sequence-level perplexity gives compactly-tokenised models a free advantage, and across 134-language comparisons that matters.</p> <h2 id="data-and-training-pipeline">Data and Training Pipeline</h2> <h3 id="sources">Sources</h3> <p>Goldfish merges roughly 80 corpora, in three priority layers:</p> <ol> <li><strong>Chang et al. (2024a)</strong>’s 250-language collection: OSCAR, Wikipedia, NLLB seed, eBible, AfriBERTa, NusaX, AmericasNLI, and many more (~40 sources).</li> <li><strong>Glot500</strong> (Imani et al., 2023): only for languages not already at 1GB after layer 1.</li> <li><strong>MADLAD-400</strong> (Kudugunta et al., 2023) clean split: CommonCrawl-derived, added only when layers 1+2 still fall short. For languages with very short average line length (&lt;25 tokens), MADLAD-400 contiguous sequences are appended so the LM gets useful context windows.</li> </ol> <h3 id="preprocessing">Preprocessing</h3> <ul> <li><strong>Deduplication</strong> at 100-UTF-8-byte chunks (Lee et al., 2022) — critical because the same language appears across multiple sources.</li> <li><strong>Language code canonicalisation</strong>: ISO 639-3 + ISO 15924 (e.g. <code class="language-plaintext highlighter-rouge">khm_khmr</code>). Macrolanguages and individual languages are both kept (<code class="language-plaintext highlighter-rouge">que_latn</code> for Quechua broadly, <code class="language-plaintext highlighter-rouge">quy_latn</code> for Ayacucho, <code class="language-plaintext highlighter-rouge">quz_latn</code> for Cusco). Some redundant codes are dropped (<code class="language-plaintext highlighter-rouge">ory_orya</code> Odia merges into <code class="language-plaintext highlighter-rouge">ori_orya</code>).</li> <li><strong>FLORES and AmericasNLI held out</strong> from training. To audit for contamination the authors check how often the first 10 tokens of each FLORES sentence appear in the per-language training set: <strong>98% of languages have fewer than 10 contaminating sequences (out of 2000)</strong>. The two exceptions are Samoan (7,155 hits) and Central Kanuri (371). Reported but not excluded.</li> <li><strong>Bible-only languages dropped.</strong> A corpus that is <em>only</em> Bible translation is too domain-skewed to support LM training.</li> </ul> <h3 id="final-corpus-statistics">Final corpus statistics</h3> <ul> <li>547GB of text covering 523 language-script combinations (486 unique language codes, 32 unique script codes).</li> <li>The 350 Goldfish languages train 1,154 models total: 350 at 5MB, 288 at 10MB, 166 at 100MB, 83 at 1GB, 267 full-data.</li> <li>“1GB” is post byte-premium scaling, so the actual on-disk size ranges 0.7–5.0×. Burmese (<code class="language-plaintext highlighter-rouge">mya_mymr</code>) hits the 5.0 clip.</li> </ul> <h3 id="hardware">Hardware</h3> <p>A heterogeneous mix of older and newer NVIDIA cards (GTX TITAN X through RTX A6000), single GPU per run. ~15,600 A6000-equivalent hours for pretraining (about 651 GPU-days). Inference for FLORES + four downstream benchmarks adds ~250 A6000 hours, dominated by inference on the much larger comparison models rather than on Goldfish itself.</p> <h2 id="experimental-results">Experimental Results</h2> <h3 id="flores-log-perplexity">FLORES log-perplexity</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0015-goldfish/tab1-flores-perplexity-480.webp 480w,/assets/img/papers/0015-goldfish/tab1-flores-perplexity-800.webp 800w,/assets/img/papers/0015-goldfish/tab1-flores-perplexity-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0015-goldfish/tab1-flores-perplexity.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 1. Mean FLORES perplexity for the 100 languages shared by XGLM 4.5B, MaLA-500, and FLORES, split by Goldfish max dataset size. Goldfish wins every bracket — even the 10MB/5MB bracket (130.5) is well below XGLM 4.5B's 183.1.</figcaption> </figure> <p>Headline numbers, on the 73 languages where Goldfish has a 1GB model:</p> <ul> <li>Goldfish 76.9 — Bigrams 112.3 — XGLM 4.5B 78.6 — MaLA-500 10B 84.7.</li> </ul> <p>The 73 + 22 + 5 = 100 languages in Table 1 are the ones present in XGLM 4.5B, MaLA-500, and FLORES simultaneously, partitioned by Goldfish’s max dataset size. The 10/5MB row is the most striking — even with just 5MB of content-matched training data, Goldfish (130.5) beats XGLM 4.5B (183.1) by a wide margin. A 5MB monolingual model is a better next-token predictor than a 4.5B multilingual one on those languages.</p> <h3 id="bigrams-beat-large-multilingual-lms">Bigrams beat large multilingual LMs</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0015-goldfish/tab2-win-rates-480.webp 480w,/assets/img/papers/0015-goldfish/tab2-win-rates-800.webp 800w,/assets/img/papers/0015-goldfish/tab2-win-rates-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0015-goldfish/tab2-win-rates.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 2. Pairwise win rates. Goldfish wins on 60/102 shared languages against XGLM 4.5B and 111/175 against MaLA-500. Even bigrams beat XGLM 4.5B on 24/102 languages and BLOOM 7.1B on 20/46 (43%).</figcaption> </figure> <p>This table carries the strongest message in the paper. Two signals:</p> <ol> <li><strong>Goldfish vs large multilingual.</strong> Goldfish wins 60/102 against XGLM 4.5B (59%) and 111/175 against MaLA-500 10B (63%). The single exception is XGLM 7.5B — but XGLM 7.5B only covers 30 high-resource languages, and Goldfish wins only 2/30 there.</li> <li><strong>Bigrams vs large multilingual.</strong> Plain backoff bigrams beat XGLM 4.5B on 24/102 (24%) and BLOOM 7.1B on 20/46 (43%). This means the large LMs aren’t even producing better-than-unigram next-token distributions for a substantial fraction of their nominal coverage.</li> </ol> <p>XGLM 7.5B as outlier underscores the curse-of-multilinguality reading: <em>concentrate on fewer languages and the large model wins; spread across many and it collapses</em>.</p> <h3 id="downstream-benchmarks">Downstream benchmarks</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0015-goldfish/tab3-downstream-480.webp 480w,/assets/img/papers/0015-goldfish/tab3-downstream-800.webp 800w,/assets/img/papers/0015-goldfish/tab3-downstream-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0015-goldfish/tab3-downstream.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 3. Downstream benchmark averages. Goldfish 124M wins MultiBLiMP at 78.8 — beating Gemma 3 270M (72.3), BLOOM 560M (64.2), and even Gemma 3 1B (78.0) and LLaMA 3 1B (77.4) by a small margin. But Belebele/XCOPA/XStoryCloze are near chance for every sub-1B model.</figcaption> </figure> <ul> <li><strong>MultiBLiMP</strong> (grammaticality, 74 languages): Goldfish 124M = <strong>78.8</strong>, the best of any model tested. Gemma 3 270M = 72.3, BLOOM 560M = 64.2, XGLM 564M = 66.6. Even the 1B Gemma 3 (78.0) and LLaMA 3 (77.4) sit slightly behind.</li> <li><strong>Belebele</strong> (121-language reading comprehension): everything is at chance (chance = 25, all models 22.7–29.0).</li> <li><strong>XCOPA / XStoryCloze</strong>: chance + ~5% for every sub-1B model.</li> </ul> <p>Two readings. First, <strong>small monolingual gives outsized returns on grammatical knowledge</strong> — MultiBLiMP scores models on next-token log-probability ranking without fine-tuning, so it measures grammatical competence directly. Second, <strong>reasoning is scale-bound below 1B</strong> — neither XGLM 564M, Gemma 3 270M, Goldfish 124M, nor BLOOM 560M actually reasons on these tasks. The Goldfish setup just confirms it across 350 languages.</p> <h2 id="analysis">Analysis</h2> <h3 id="why-a-small-monolingual-model-beats-a-10b-multilingual-one">Why a small monolingual model beats a 10B multilingual one</h3> <p>The paper doesn’t ablate this directly, but the contributing factors are clear:</p> <ol> <li><strong>Capacity allocation.</strong> XGLM 4.5B’s 4.5B parameters spread over 134 languages averages ~34M per language. Goldfish 124M dedicates all parameters to one language — about 4× more, applied monolingually.</li> <li><strong>Tokenizer fit.</strong> Each Goldfish vocab is 50K monolingual tokens. Multilingual tokenizers (100K–250K vocab over hundreds of languages) approach byte-level fallback on low-resource languages.</li> <li><strong>Data balancing.</strong> Byte-premium scaling gives Goldfish content-matched 1GB. Multilingual training inevitably gets pulled toward high-resource sampling even with temperature reweighting.</li> </ol> <p>The 24–43% bigram-wins fraction is the striking number. For bigrams to beat a 7.1B LM, the LM’s next-token distribution must be worse than a unigram-smoothed distribution — i.e. the LM has essentially not learned the language.</p> <h3 id="contamination-analysis">Contamination analysis</h3> <p>If FLORES is leaked into training, the perplexity claims would be hollow. The authors search the training data for the first 10 tokens of every FLORES sentence and find <strong>&lt;10 hits per 2000 sentences for 98% of languages</strong>. Only Samoan (7,155 hits) and Central Kanuri (371) show non-trivial contamination. The reported averages include those languages, but removing them is unlikely to change the conclusions — and the authors flag it explicitly.</p> <h3 id="what-the-reasoning-ceiling-means">What the reasoning ceiling means</h3> <p>The fact that <em>no</em> sub-1B model — Goldfish, XGLM 564M, BLOOM 560M, Gemma 3 270M — clears chance on Belebele/XCOPA/XStoryCloze suggests this is a property of base LMs without instruction tuning at this scale, not a Goldfish-specific weakness. Even Gemma 3 1B and LLaMA 3 1B sit at 55–60 (chance = 50). Goldfish extends that observation to 350 languages and confirms the ceiling is global.</p> <h2 id="limitations-and-critical-assessment">Limitations and Critical Assessment</h2> <p>Authors’ stated limitations:</p> <ul> <li><strong>Comparability and availability.</strong> 5MB isn’t available for every language; corpus <em>quality</em> and <em>domain</em> (news vs social vs religious) vary. Even with byte premium scaling, perfect comparability is unachievable.</li> <li><strong>Monolinguality.</strong> A deliberate design choice but a real cost: similar-language transfer (Conneau et al., 2020b) is forfeited. Goldfish trades coverage for clarity.</li> <li><strong>Sizes capped at 1GB / 125M.</strong> Reasonable for low-resource focus, but small relative to high-resource state-of-the-art.</li> <li><strong>Downstream evaluation limited to perplexity + grammaticality.</strong> Reasoning evaluations are at chance and thus uninformative.</li> <li><strong>No PII or content-safety guarantees</strong> beyond using publicly released corpora that themselves may not be audited.</li> </ul> <p>Reviewer additions:</p> <ul> <li><strong>Bigram fairness questions.</strong> Bigrams use the same monolingual tokenizer and the same training data, but still beat XGLM 4.5B on 24% of languages despite seeing no long-range context. This could mean <em>XGLM doesn’t use long-range context for low-resource languages</em> — or it could mean <em>FLORES log-perplexity with a half-sentence prompt already truncates the useful context</em>. The two interpretations have different policy consequences and the paper doesn’t disentangle them.</li> <li><strong>Per-language compute framing.</strong> Total compute (1.65 × 10²⁰ FLOPs) is presented as 1/1900 of GPT-3. Per-language compute is 1.43 × 10¹⁷ FLOPs, about 1/2,200,000 of GPT-3. The “tiny model can do this” message depends on which framing you take.</li> <li><strong>Monolingual-only utility.</strong> Low-resource speakers usually want their language <em>paired</em> with English or another high-resource language for downstream tasks. Goldfish is only the first half of that pipeline — fine-tuning or paired-model setups are still required.</li> <li><strong>Long-term hosting.</strong> 1,154 HuggingFace repos is a lot to maintain. The paper doesn’t commit to long-term curation, model cards, or content updates, which matters if Goldfish is to become a community resource rather than a snapshot.</li> </ul> <h2 id="takeaways">Takeaways</h2> <ul> <li><strong>Massively multilingual is not free.</strong> For low-resource languages a 4.5B multilingual model loses to a 39M monolingual model on basic perplexity. Future multilingual-model design needs to take per-language capacity allocation seriously rather than treating “more languages” as a strict win.</li> <li><strong>The bigram result is genuinely shocking.</strong> Plain backoff bigrams beat XGLM 4.5B on 24% and BLOOM 7.1B on 43% of languages. If you build the next massively multilingual model, you should treat this number as a falsification target.</li> <li><strong>Baseline coverage matters.</strong> 215 of these 350 languages had no monolingual LM at all before this release. That alone enables a wave of follow-up work — fine-tuning baselines, language ID validation, corpus quality measurement.</li> <li><strong>Byte-premium scaling should be standard.</strong> Goldfish’s headline comparisons fail to hold without it. There’s no reason future multilingual evaluation papers shouldn’t normalise the same way.</li> <li><strong>Reasoning isn’t going to come from sub-1B base LMs.</strong> Better perplexity doesn’t deliver Belebele/XCOPA. The future of low-resource NLP is probably <em>small monolingual LM + something else</em> — instruction-tuning, retrieval, or efficient transfer to a larger backbone.</li> </ul> <h2 id="installation-and-use">Installation and Use</h2> <p>All models are hosted on Hugging Face under <code class="language-plaintext highlighter-rouge">goldfish-models/&lt;lang_script&gt;_&lt;size&gt;</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">AutoTokenizer</span><span class="p">,</span> <span class="n">AutoModelForCausalLM</span>

<span class="n">tokenizer</span> <span class="o">=</span> <span class="n">AutoTokenizer</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">goldfish-models/eng_latn_1000mb</span><span class="sh">"</span><span class="p">)</span>
<span class="n">model</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">goldfish-models/eng_latn_1000mb</span><span class="sh">"</span><span class="p">)</span>

<span class="n">prompt</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Goldfish are</span><span class="sh">"</span>
<span class="n">ids</span> <span class="o">=</span> <span class="nf">tokenizer</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">return_tensors</span><span class="o">=</span><span class="sh">"</span><span class="s">pt</span><span class="sh">"</span><span class="p">).</span><span class="n">input_ids</span>
<span class="n">out</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">ids</span><span class="p">,</span> <span class="n">max_new_tokens</span><span class="o">=</span><span class="mi">20</span><span class="p">,</span> <span class="n">do_sample</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">tokenizer</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="n">out</span><span class="p">[</span><span class="mi">0</span><span class="p">]))</span>
<span class="c1"># → "Goldfish are a species of fish that are found in the sea."
</span></code></pre></div></div> <p>Swap the dataset suffix (<code class="language-plaintext highlighter-rouge">_5mb</code>, <code class="language-plaintext highlighter-rouge">_10mb</code>, <code class="language-plaintext highlighter-rouge">_100mb</code>, <code class="language-plaintext highlighter-rouge">_full</code>) and language code (<code class="language-plaintext highlighter-rouge">khm_khmr</code>, <code class="language-plaintext highlighter-rouge">swa_latn</code>, <code class="language-plaintext highlighter-rouge">mya_mymr</code>, …) as needed. Training corpora live at <code class="language-plaintext highlighter-rouge">goldfish-models/fish-food</code>, and evaluation code (FLORES log-perplexity, contamination check) is in the GitHub repo under <code class="language-plaintext highlighter-rouge">evaluation/</code>.</p> <h2 id="references">References</h2> <ul> <li>Paper: <a href="https://arxiv.org/abs/2408.10441">https://arxiv.org/abs/2408.10441</a></li> <li>Code: <a href="https://github.com/tylerachang/goldfish">https://github.com/tylerachang/goldfish</a></li> <li>Models (Hugging Face): <a href="https://huggingface.co/goldfish-models">https://huggingface.co/goldfish-models</a></li> <li>Datasets: <a href="https://huggingface.co/datasets/goldfish-models/fish-food">https://huggingface.co/datasets/goldfish-models/fish-food</a></li> </ul> <h2 id="further-reading">Further Reading</h2> <ul> <li><strong><a href="https://arxiv.org/abs/2311.09205">When Is Multilinguality a Curse? Language Modeling for 250 High- and Low-Resource Languages</a></strong> (Chang et al., EMNLP 2024) — the direct predecessor: systematic monolingual-vs-multilingual comparison across 250 languages, concluding small models + enough monolingual data favour monolingual.</li> <li><strong><a href="https://arxiv.org/abs/2403.00686">A Bit of a Problem: Measurement Disparities in Dataset Sizes Across Languages</a></strong> (Arnett et al., SIGUL @ LREC-COLING 2024) — original paper for the byte premium tool. Measures and regresses byte premiums across 1,155 languages, the underpinning of Goldfish’s fair-comparison story.</li> <li><strong><a href="https://arxiv.org/abs/2305.12182">Glot500: Scaling Multilingual Corpora and Language Models to 500 Languages</a></strong> (Imani et al., ACL 2023) — one of Goldfish’s main data sources, plus a 511-language continued-pretraining of XLM-R.</li> <li><strong><a href="https://arxiv.org/abs/2401.13303">MaLA-500: Massive Language Adaptation of Large Language Models</a></strong> (Lin et al., 2024) — Goldfish’s closest comparison: LLaMA 2 10B continued-pretrained on Glot500 for 534 languages.</li> <li><strong><a href="https://arxiv.org/abs/2112.10668">Few-shot Learning with Multilingual Language Models (XGLM)</a></strong> (Lin et al., EMNLP 2022) — the XGLM 4.5B / 7.5B baselines used throughout the paper.</li> <li><strong><a href="https://arxiv.org/abs/2504.02768">MultiBLiMP 1.0: A Massively Multilingual Benchmark of Linguistic Minimal Pairs</a></strong> (Jumelet et al., TACL 2026) — the grammaticality benchmark Goldfish wins. Auto-generated minimal pairs from Universal Dependencies + UniMorph, 101 languages, 128K pairs.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="low-resource-language"/><category term="monolingual-lm"/><category term="multilingual-nlp"/><category term="perplexity"/><category term="pretraining"/><summary type="html"><![CDATA[1,154 small monolingual GPT-2 models covering 350 low-resource languages. With 5MB-1GB of byte-premium-scaled text and 39M-125M parameters, they consistently beat XGLM 4.5B, BLOOM 7.1B, and MaLA-500 10B on FLORES perplexity.]]></summary></entry><entry xml:lang="ko"><title type="html">[논문 리뷰] Algorithmically Establishing Trust in Evaluators</title><link href="https://baba9811.github.io/papers/0014-algorithmically-establishing-trust-in-evaluators/" rel="alternate" type="text/html" title="[논문 리뷰] Algorithmically Establishing Trust in Evaluators"/><published>2026-05-14T13:00:00+00:00</published><updated>2026-05-14T13:00:00+00:00</updated><id>https://baba9811.github.io/papers/0014-algorithmically-establishing-trust-in-evaluators</id><content type="html" xml:base="https://baba9811.github.io/papers/0014-algorithmically-establishing-trust-in-evaluators/"><![CDATA[<div class="lang-toggle" style="margin-bottom: 1.5rem;"> <a href="/en/papers/0014-algorithmically-establishing-trust-in-evaluators/" class="btn btn-sm btn-outline-secondary">🇺🇸 Read in English</a> </div> <h2 id="메타정보">메타정보</h2> <table> <thead> <tr> <th>항목</th> <th>내용</th> </tr> </thead> <tbody> <tr> <td>저자</td> <td>Adrian de Wynter (The University of York · Microsoft)</td> </tr> <tr> <td>학회</td> <td>arXiv preprint · 2026</td> </tr> <tr> <td>arXiv</td> <td><a href="https://arxiv.org/abs/2506.03083">2506.03083</a></td> </tr> <tr> <td>Code</td> <td><a href="https://github.com/adewynter/no_data_algorithm">adewynter/no_data_algorithm</a></td> </tr> <tr> <td>데이터</td> <td>합성 바이너리 문자열 (IP/OOP, 각 498 항목) · West Frisian 저자원 언어 라벨링 코퍼스 (1,015 항목)</td> </tr> <tr> <td><span style="white-space: nowrap">리뷰 일자</span></td> <td>2026-05-14</td> </tr> </tbody> </table> <h2 id="tldr">TL;DR</h2> <ul> <li><strong>No-Data Algorithm</strong>: 정답 라벨이 <em>전혀 없는</em> 상황에서 평가자(evaluator, 예: LLM-as-a-judge)가 “정말 라벨을 안다”고 주장하는 게 사실인지 알고리즘적으로 검증한다.</li> <li>핵심은 두 챌린지(equality up to permutation / up to isomorphism)를 무작위로 던지는 <strong>EV(Evaluator-Verifier) 프로토콜</strong>이다. Babai (1985)의 Arthur-Merlin 프로토콜에 기반한 zero-knowledge 스타일 챌린지-응답.</li> <li><strong>수학적 보장</strong>: $r$ 라운드 후 거짓말 평가자를 놓칠 확률은 $\le (1/4)^r$. 정확도 $\alpha$인 평가자에 대해 $\mathbb{E}[\text{correct}] \le 1 - (1-\alpha)(1 - \phi + \phi(1/4)^r)$.</li> <li><strong>실증 결과</strong>: 합성 바이너리 데이터셋(o3-mini 평가자)에서 <em>모르는</em> 데이터 분포(OOP)일 때 success rate가 28%로 떨어져 거짓을 폭로. West Frisian 라벨링(GPT-4.1 평가자+검증자)에서는 success rate가 IP 86–88% vs OOP 1–2%로 깔끔히 분리.</li> </ul> <h2 id="소개-introduction">소개 (Introduction)</h2> <p>평가는 ML의 가장 근본적인 문제다. 그러나 LLM-as-a-judge가 사실상 표준이 된 지금, <em>평가자 자체</em>의 신뢰가 흔들리고 있다. 전통적으로는 (1) 정답 라벨이 있는 reference set과 비교하거나, (2) 평가자가 “어떻게든” 정답을 안다고 가정하는 두 가지 방식으로 신뢰를 확립해왔다. 두 방식 모두 라벨이 없을 때 무력하다 — 전자는 데이터를 요구하고 후자는 가정에 불과하기 때문이다.</p> <p>라벨이 부족한 시나리오는 예외가 아니라 빈번하다. <strong>저자원 언어</strong> (West Frisian처럼 라벨된 데이터가 사실상 없는 언어), <strong>의료 도메인</strong> (라벨 비용이 막대), <strong>시장 조사</strong> (정답이 unique), <strong>벤치마크 오염</strong> (Sainz et al. 2023의 경고처럼, 라벨이 있어도 모델이 미리 본 데이터일 수 있음). 이런 곳에서 LLM judge를 그냥 쓰는 건 결과의 통계적 의미를 잃는다.</p> <p>본 논문은 <em>수학적으로 엄밀하게</em> — <code class="language-plaintext highlighter-rouge">1 - (1/4)^r</code> 확률로 — 평가자의 라벨 지식을 검증하는 <strong>No-Data Algorithm</strong>을 제안한다. 핵심 아이디어는 두 가지다. 첫째, 평가자에게 <em>비슷한 다른 데이터점</em>을 만들어 보라고 시키고 그 일관성을 따진다 (zero-knowledge proof의 챌린지-응답 게임과 같은 구조). 둘째, 두 챌린지는 <em>정보 이론적으로 상호배타적</em>이라 둘 다 통과하려면 진짜 라벨 함수를 알아야 한다. 따라서 정답을 모르는 평가자는 매 라운드 최대 1/2 확률로 한 챌린지만 우연히 통과할 수 있고, <em>둘 다</em>를 빠져나갈 확률은 1/4 이하다.</p> <p>이 논문은 <em>수학적으로</em> 새로운 발상은 아니다 — Arthur-Merlin 프로토콜과 zero-knowledge proof는 80년대부터 있었다. 그러나 그 아이디어를 <em>평가자 신뢰</em>라는 ML 평가 문제에 옮겨와 실제로 작동함을 보인 것은 처음이다. 정확도 지표가 만연한 환경에서 <em>그 정확도 자체를 어떻게 믿을 것인가</em>라는 메타 질문에 답한다는 점에서, 라벨 없는 평가가 일상이 된 LLM 시대에 읽을 가치가 크다.</p> <h2 id="핵심-기여-key-contributions">핵심 기여 (Key Contributions)</h2> <ul> <li><strong>No-Data Algorithm 제안</strong>: 라벨이 전혀 없는 상황에서 평가자의 신뢰를 $(1/4)^r$ 확률 한계까지 끌어올리는 알고리즘. AM 프로토콜의 챌린지-응답 메커니즘을 분류 평가에 맞게 변형했다.</li> <li><strong>두 정리로 정밀한 정합성 보장</strong>: Lemma 5.1 (EV 프로토콜의 거짓 감지 확률 한계 $(1/4)^r$), Theorem 5.2 (평가자 정확도 $\alpha$와 라벨 뒤집기 확률 $\phi$를 결합한 No-Data Algorithm 전체의 정확도 상한).</li> <li><strong>합성 + 실제 자연어 실험</strong>: 바이너리 문자열에서 in-phenomenon(IP)/out-of-phenomenon(OOP) 두 setup으로 이론을 검증하고, West Frisian 저자원 언어 라벨링으로 현실적 응용까지 보였다.</li> <li><strong>베이스라인과의 비교 (Cohen’s κ, PA, ensemble)</strong>: 라벨 없이는 보정이 불가능한 일치도 지표들이 OOP에서 모순된 신호를 내는 반면, No-Data Algorithm의 success rate는 IP 86.2 vs OOP 1.2로 깔끔히 분리됨을 보인다.</li> </ul> <h2 id="관련-연구--배경-지식">관련 연구 / 배경 지식</h2> <p><strong>Arthur-Merlin 프로토콜 (Babai 1985).</strong> 무한한 계산력을 가진 prover Merlin이 random coin을 가진 verifier Arthur에게 어떤 statement가 참임을 <em>통계적</em>으로 설득하는 프로토콜이다. zero-knowledge proof의 친척으로, Goldreich (2009)와 Arora &amp; Barak (2009)이 정리한 표준 교과서 주제다. 본 논문의 EV 프로토콜은 AM의 챌린지-응답 구조를 차용했지만 <em>secrecy</em> 보장 (Merlin의 비밀이 새지 않음) 대신 <em>평가자가 진짜 안다는 것</em>만 검증하도록 단순화되었다.</p> <p><strong>LLM-as-a-judge의 부상.</strong> Zheng et al.의 MT-Bench/Chatbot Arena (Zheng et al., NeurIPS 2023), Liu et al.의 G-Eval (Liu et al., EMNLP 2023) 같은 작업이 GPT-4류 강력 모델을 자동 평가자로 쓰는 패러다임을 정착시켰다. Li et al. 2024의 종합 survey도 이 분야의 빠른 성장을 정리한다. 그러나 여러 후속 연구가 이 평가자들의 신뢰성을 의심해왔다 — Gehrmann et al. 2023은 평가 실천의 균열을, Bavaresco et al. 2024는 20개 NLP 평가 과제에서 LLM judge가 인간을 대체할 수 없다는 결과를, Hada et al. 2024는 다국어 환경에서 평가 품질이 급락한다는 점을 보였다.</p> <p><strong>일치도(agreement) 메트릭의 한계.</strong> 라벨이 없을 때 베이스라인은 <em>여러 평가자의 합의를 보는 것</em>이다 — Cohen’s κ, Fleiss &amp; Cohen 1973의 ICC, percentage agreement (PA) 같은 지표들. 그러나 이들은 <em>보정(calibration)</em>을 요구한다. κ=0.4가 좋은지 나쁜지는 도메인마다 다르고, 보정에는 reference data가 필요하다. 본 논문이 강조하듯 라벨이 없는 상황에서 이 메트릭들은 “모순적이거나 불완전한 정보”만 준다 (Table 9의 핵심 관찰).</p> <p><strong>이전 trustworthy ML 연구.</strong> Jovanović et al. 2023은 representation learning에서 <em>공정한</em> 표현을 입증가능하게 학습하는 작업을, Dimitrov et al. 2022는 <em>입증가능하게 robust한</em> 적대적 예시를 다뤘다. 본 논문은 같은 <em>입증가능성(provability)</em> 전통을 평가자 신뢰로 확장한다.</p> <h2 id="방법--아키텍처-상세">방법 / 아키텍처 상세</h2> <h3 id="표기와-가정">표기와 가정</h3> <p>데이터점 $x \in X$, 라벨 $y \in Y$. 본 논문은 $Y = {0, 1}$ (이진 분류)에 집중한다. 또한 $X \subseteq {0, 1}^n$ — 모든 데이터점이 $n$비트 바이너리 문자열이라고 가정. 이 가정은 증명 단순화를 위한 것이고 실제로는 자연어 문자열 같은 다른 데이터에 적용 가능하다고 §7에서 논한다.</p> <p>각 $x \in {0,1}^n$에 대해 그 <em>relevant subset</em> $S_x \in 2^x$를 정의한다 (예: 부분 문자열의 집합). 구체적 구성은 setup에 따라 §4.1에서 다룬다.</p> <p><strong>기준(criterion)</strong> $c\colon X \to {0,1}$은 데이터점을 분류한다. <strong>루브릭(rubric)</strong> $C\colon \times_{i,\ldots,n} {0,1} \to {0,1}^n$은 <em>기준의 순서 집합</em> $\mathcal{C} = {c_1, \ldots, c_n}$의 출력을 비트 문자열로 이은 것: $C = c_1 c_2 \ldots c_n$. 어떤 기준이 <em>arity &gt; 1</em>인 비선형 기준 (예: $c_i = c_a \oplus c_b$, XOR)으로 분해되면 그 비선형 기준이 분해된 형태를 $\bar{C}$로 쓴다. 모든 기준이 명시적으로 분해되어 있으면 그 루브릭을 <strong>total</strong>이라 한다.</p> <p><strong>Aggregator</strong> $\sigma\colon {0,1}^n \to Y$는 $n$비트 출력을 최종 라벨로 mapping한다 (e.g., 다수결).</p> <p>두 점 $x, x’ \in X$가 <strong>similar</strong>하다는 것은 $x \cong x’$ <em>그리고</em> $x \equiv x’$ 둘 다 — 즉 isomorphism (구조적 동치)과 permutation (순열) 양쪽에서 동치라는 뜻이다.</p> <p><strong>Lying</strong> 평가자는 “라벨을 안다”는 <em>근거 없는 주장</em>을 펴는 모든 평가자다. 두 종류 모두 포함: 정보 없이 추측하는 uninformed lying과 자신 있게 틀리는 sycophantic lying. 전자는 in-distribution에서도 실패하는 반면 후자는 in-distribution에선 잘 맞춘다. 둘을 라벨 없이 구별하는 게 본 논문의 핵심 도전이다.</p> <p><strong>Assumption 3.1</strong> (Composition assumption): 진짜 매핑 $f\colon X \to Y$는 루브릭과 aggregator의 합성이다:</p> \[f = \sigma C \quad (1)\] <p>이는 약한 가정이다 — 데이터점이 라벨을 받는 <em>이유</em> (criterion)가 있고, 이유가 여럿이면 모으는 방식 (aggregator)이 있다는 것뿐. 실제 시나리오에서 $C$나 $\sigma$가 알려지지 않으면 사용자 정의 surrogate를 쓰면 되지만 그 경우 <em>신뢰 한계는 surrogate에 대한</em> 것이라는 점에 유의해야 한다.</p> <h3 id="ev-프로토콜--한-라운드의-작동">EV 프로토콜 — 한 라운드의 작동</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/fig1-ev-protocol-flow-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/fig1-ev-protocol-flow-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/fig1-ev-protocol-flow-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/fig1-ev-protocol-flow.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1: EV 프로토콜 한 라운드. 평가자(파랑)는 x와 similar한 x′와 부분 라벨 ỹ′을 만들고, verifier(주황)가 두 챌린지 중 균등 무작위로 하나를 선택해 답을 요구한다. r번 반복.</figcaption> </figure> <p>EV 프로토콜은 데이터점 $x \in X$와 루브릭 $C$를 양쪽이 공유한 상태에서 시작한다. Aggregator $\sigma$는 verifier에게 주어지지 않는다. 평가자는 $f\colon X \to Y$를 <em>안다</em>고 주장한다. $C$가 공개되므로 그 주장은 실질적으로 <em>aggregator $\sigma$를 안다</em>는 주장과 같다. Verifier는 이 주장을 <em>충분히 검증</em>해 라벨링을 신뢰해도 되는지 결정하려 한다.</p> <p><strong>한 라운드 절차:</strong></p> <ol> <li>평가자가 $x$와 similar한 $x’ \in X$를 생성하고, 그에 대응하는 <em>부분 라벨</em> $\tilde{y}’$도 생성. 부분이라 부르는 이유: 평가자는 $f$에 접근 권한이 없으므로 $C(x’)$에 기반한 <em>추측</em>에 불과하기 때문. 일단 생성하면 이 라운드에서 변경 불가.</li> <li>Verifier가 두 챌린지 중 하나를 균등 무작위로 선택: <ul> <li><strong>Challenge 1 (Equality up to Permutation)</strong> — “$x’ \equiv x$인 이유를 보여라”. Verifier는 \(\forall s \in S_x, \exists t \in S_{x'}.\ \forall c \in \bar{C},\ c(s) = c(t) \quad (2)\) 를 체크. 즉 $x’$의 substring 중 $x$의 어떤 substring과 <em>모든 기준의 (total) 평가가 일치</em>하는 것이 적어도 하나 있어야 함.</li> <li><strong>Challenge 2 (Equality up to Isomorphism)</strong> — “$f(x’) = y$인 이유를 보여라”. $y$는 verifier가 모르므로 대신 $x’ \cong x$를 체크: verifier가 $C(x’) = C(x)$임을 검증.</li> </ul> </li> <li>평가자가 챌린지에 실패하면 프로토콜은 즉시 failure 반환. 통과하면 다음 라운드. $r$라운드 모두 통과하면 success 반환. 어느 경우든 $\tilde{y}’$도 반환.</li> </ol> <h3 id="두-챌린지가-왜-함께-필요한가">두 챌린지가 왜 함께 필요한가</h3> <p>Lemma A.1의 핵심: 두 챌린지는 <em>상호 배타적인 정보</em>를 제공하며, <em>함께</em>만이 평가자의 거짓을 잡아낸다 (necessary and sufficient).</p> <ul> <li><strong>Challenge 1만</strong>으로는 부족: $C(x’) = C(x)$가 성립해도 $x’$의 <em>내부 구조</em>는 다를 수 있다 (e.g., disjunctive clause로 같은 평가가 나오는 다른 문자열). 즉 <em>permutation 동치</em>는 통과 가능하지만 <em>isomorphism 동치</em>는 보장 안 됨.</li> <li><strong>Challenge 2만</strong>으로는 부족: $C(x’) = C(x)$는 <em>최종 인코딩</em>이 같음을 보일 뿐, 내부 비트 구조의 동치성을 보장하지 않음.</li> </ul> <p>따라서 평가자가 진짜 $f$를 알면 두 챌린지 모두 매번 통과 가능. 모른다면 — 둘 다 통과하려면 <em>둘 다</em>에 대응되는 $x’$를 만들어야 하는데, 정보 없이 그건 본질적으로 $f$를 아는 것과 같다.</p> <p>거짓 평가자가 우연히 통과할 수 있는 경우는 둘:</p> <ul> <li><strong>Lie 1: “좋은” $x’$ 생성</strong>: $S_{x’} = S_x$인 $x’$를 만들어 Challenge 1을 통과 가능하지만 Challenge 2 ($C(x’) = C(x)$)에서 1/2 확률로 실패.</li> <li><strong>Lie 2: $f(x’) \ne y$</strong>: $C(x’) = C(x)$가 되도록 만들어 Challenge 2 통과 가능하지만 Challenge 1에서 1/2 확률로 실패.</li> </ul> <p>챌린지가 1/2 확률로 선택되므로 한 라운드 통과 확률은 <em>최대</em> $1/2 \times 1/2 = 1/4$. $r$라운드 후엔 $(1/4)^r$.</p> <h3 id="no-data-algorithm--전체-구조">No-Data Algorithm — 전체 구조</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/alg1-no-data-algorithm-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/alg1-no-data-algorithm-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/alg1-no-data-algorithm-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/alg1-no-data-algorithm.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Algorithm 1: No-Data Algorithm. 데이터셋 X의 매 점에 대해 EV 프로토콜을 r 라운드 실행하고, 실패한 라운드에 대해서는 확률 φ로 라벨을 뒤집어 success 카운트와 예측을 반환한다.</figcaption> </figure> <p>알고리즘은 데이터셋 $X$의 각 점에 대해 EV 프로토콜을 $r$라운드 호출한다. EV가 성공하면 평가자의 라벨 $y$를 그대로 받아들이고 success를 +1. 실패하면 — 평가자가 잘못된 라벨을 줬을 가능성이 있으므로 — 확률 $\phi$로 라벨을 뒤집어 ($\tilde{y} = \neg y$) predictions에 추가하고 success는 0. 최종적으로 (1) 라벨 예측 리스트, (2) success 카운트를 반환한다.</p> <p>라벨 뒤집기 단계가 중요한 이유: EV가 실패했다는 건 평가자가 라벨을 모른다는 신호다. 만약 평가자가 <em>일관되게</em> 틀린 라벨을 주는 sycophantic liar라면 뒤집은 라벨이 옳을 확률이 높다. $\phi$는 평가자의 예상 오류율에 가깝게 튜닝한다 — Theorem 5.2가 보여주듯 $\phi$가 정확하면 알고리즘의 출력이 <em>알려진 케이스의 정확도</em>에 가까워진다.</p> <h2 id="학습-목표--손실-함수">학습 목표 / 손실 함수</h2> <p>이 논문에는 <em>학습되는 모델</em>이 없다 — No-Data Algorithm은 외부 평가자를 <em>검증</em>하는 메타 알고리즘이다. 그래서 손실 함수가 아니라 <strong>정합성 한계(correctness bound)</strong> 두 개로 핵심을 정의한다.</p> <h3 id="lemma-51--ev-프로토콜-정합성-한계">Lemma 5.1 — EV 프로토콜 정합성 한계</h3> <p>verifier가 $r$ 라운드 후 평가자의 거짓을 감지하지 못할 확률은</p> \[\Pr[\text{verifier misses lie}] \le \left(\frac{1}{4}\right)^r\] <p>증명 스케치: 위에서 본 두 거짓 시나리오 (Lie 1, Lie 2) 각각에 대해 평가자가 한 라운드 통과 확률은 1/4. Union bound로 $r$라운드 후 $(1/4)^r$.</p> <h3 id="theorem-52--no-data-algorithm-전체-정합성-한계">Theorem 5.2 — No-Data Algorithm 전체 정합성 한계</h3> <p>평가자의 정확도가 $\alpha$인 균형 데이터셋 $X$에서, 평가자가 mislabel할 때마다 거짓말한다고 가정하면:</p> \[\begin{aligned} \mathbb{E}[\text{correct}] \le 1 - (1 - \alpha)\left(1 - \phi + \phi\left(\frac{1}{4}\right)^r\right) \end{aligned} \quad (3)\] <p>증명 스케치 (Appendix A.4): 잘못된 라벨이 <em>감지되지 않을 확률</em>과 <em>감지됐지만 뒤집히지 않을 확률</em>에 union bound를 적용. 오류 $\epsilon = 1 - \alpha$에 대해</p> \[\begin{aligned} \mathbb{E}[\text{undetected}] &amp;= \epsilon \left(\frac{1}{4}\right)^r, \\ \mathbb{E}[\text{detected, no flip}] &amp;= \epsilon(1 - \phi)\left(1 - \left(\frac{1}{4}\right)^r\right), \\ \mathbb{E}[\text{wrong}] &amp;\le \epsilon\left(1 - \phi + \phi\left(\frac{1}{4}\right)^r\right). \end{aligned}\] <p>이 식이 알려주는 것: (1) 평가자의 진짜 정확도 $\alpha$가 높고 $\phi$가 잘 보정되면 No-Data Algorithm의 출력이 <em>알려진 케이스의 정확도</em>에 가까워진다. (2) 반대로 $\alpha = 0$ (즉 항상 틀리는 평가자)이면 출력에서 라벨을 <em>복원할 방법이 없다</em> — 정보를 무에서 만들 수 없기 때문. 따라서 알고리즘의 주 목적은 <em>라벨링이 아니라 신뢰 확립</em>임을 §7.1에서 강조한다.</p> <h3 id="lemma-b1--실행-시간">Lemma B.1 — 실행 시간</h3> <p>평가자와 verifier가 임의 입력에 대해 다항 시간이면, $|D|$ 크기 데이터셋과 $r$ EV 라운드에 대해 No-Data Algorithm은 $O(r|D|)$ 시간에 실행된다. 단순한 linear sweep이라 stress가 작다.</p> <h2 id="학습-데이터와-파이프라인">학습 데이터와 파이프라인</h2> <p>논문은 두 실험 setup을 쓴다. 두 경우 모두 <em>훈련</em>은 평가자 자체에만 일어나고, No-Data Algorithm은 그 위에서 검증만 한다.</p> <h3 id="experiment-1--합성-바이너리-문자열">Experiment 1 — 합성 바이너리 문자열</h3> <table> <thead> <tr> <th>항목</th> <th>내용</th> </tr> </thead> <tbody> <tr> <td>입력 도메인</td> <td>${0,1}^n$ 바이너리 문자열</td> </tr> <tr> <td>데이터셋</td> <td>In-Phenomenon (IP) / Out-of-Phenomenon (OOP) 두 disjoint 데이터셋</td> </tr> <tr> <td>테스트 크기</td> <td>각 498 항목, balanced</td> </tr> <tr> <td>Aggregator $\sigma$</td> <td>다수결 (majority vote)</td> </tr> <tr> <td>평가자 1</td> <td>Decision Tree (sklearn 기본 파라미터)</td> </tr> <tr> <td>평가자 2</td> <td>o3-mini (OpenAI, version 2025-01-31), max tokens 50,000</td> </tr> <tr> <td>Verifier</td> <td>규칙 기반 매칭 (test set과 적절한 rubric 매칭)</td> </tr> <tr> <td>$\phi$</td> <td>DT는 0.4, LLM은 0.1</td> </tr> <tr> <td>$r$</td> <td>3</td> </tr> </tbody> </table> <p>IP rubric 세 criterion: $c_0$ (1의 개수가 짝수), $c_1 = c_{1,a} \oplus c_{1,b}$ (0으로 시작 XOR 10101 포함), $c_2$ (1이 5개 초과). OOP rubric: $c_0$ (111 포함), $c_1$ (1로 끝남), $c_2$ (110001 포함). 핵심 디자인: DT와 LLM 둘 다 IP에서 <em>훈련/관찰</em>했고, OOP는 본 적이 없다. 즉 OOP에서 success rate가 떨어져야 알고리즘이 작동하는 것.</p> <h3 id="experiment-2--west-frisian-저자원-언어">Experiment 2 — West Frisian 저자원 언어</h3> <table> <thead> <tr> <th>항목</th> <th>내용</th> </tr> </thead> <tbody> <tr> <td>데이터 출처</td> <td>OpenOrca, MMLU, OpenCode, WildChat에서 무작위 prompt 추출</td> </tr> <tr> <td>응답 생성</td> <td>GPT-4o</td> </tr> <tr> <td>번역</td> <td>4명의 native West Frisian 화자가 prompt+response를 전문 번역 + 어노테이션</td> </tr> <tr> <td>어노테이터 보상</td> <td>28.7 USD/hr부터 시작 (시니어리티 기반)</td> </tr> <tr> <td>데이터셋 크기</td> <td>1,015 항목 (train 500 / test 515)</td> </tr> <tr> <td>평가자 + verifier</td> <td>둘 다 GPT-4.1 (version LONGCO-2025-04-14), 다른 prompt</td> </tr> <tr> <td>Evaluator/verifier max tokens</td> <td>256</td> </tr> <tr> <td>Generator (similar datapoint 생성) max tokens</td> <td>5,000</td> </tr> <tr> <td>Temperature</td> <td>0 (모두)</td> </tr> <tr> <td>$\phi$</td> <td>0.1, 0.3, 0.7 세 값 모두 비교</td> </tr> </tbody> </table> <p>IP rubric은 6개 criterion: (1) West Frisian으로 응답, (2) 문화·논리적으로 정확, (3) 코드면 문법 오류 없음, (4) 문법적으로 정확, (5) 잘림 없음, (6) 사용자 지시 정확히 따름. OOP rubric은 7개 criterion으로 거의 직교하게 설계 — 응답이 <em>Dutch</em>로 되어 있는지 등 (GPT-4o가 West Frisian이 아닌 Dutch로 응답하는 경향이 강함). IP rubric은 decomposable하지 않고 OOP rubric만 decomposable — Conclusion에서 다루는 ambiguity의 실제 사례.</p> <h2 id="실험-결과">실험 결과</h2> <h3 id="합성-데이터-ipoop의-success-rate-갭이-핵심-신호">합성 데이터: IP/OOP의 success rate 갭이 핵심 신호</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab1-rubrics-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab1-rubrics-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab1-rubrics-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab1-rubrics.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 1: 합성 실험의 두 루브릭. IP의 c₁은 XOR 분해로 인해 실제 기준은 5개({c₀, c₂, c₁ₐ, c₁ᵦ, c₁}).</figcaption> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab2-synthetic-results-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab2-synthetic-results-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab2-synthetic-results-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab2-synthetic-results.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 2: known(라벨 알려진 참조)과 unknown(No-Data Algorithm 출력) 결과. DT/LLM 모두 IP에선 known과 가깝지만, OOP에선 정확도는 따라가도 success rate가 크게 떨어진다.</figcaption> </figure> <table> <thead> <tr> <th>Setup</th> <th>평가자</th> <th>known Acc/F1</th> <th>unknown Acc/F1</th> <th>unknown Succ/Flips</th> </tr> </thead> <tbody> <tr> <td>IP</td> <td>DT</td> <td>62.2 / 58.8</td> <td>62.2 / 59.8</td> <td><strong>100.0 / 0.0</strong></td> </tr> <tr> <td>IP</td> <td>LLM (o3-mini)</td> <td>99.8 / 99.8</td> <td>97.6 / 97.6</td> <td><strong>81.3 / 1.8</strong></td> </tr> <tr> <td>OOP</td> <td>DT</td> <td>54.2 / 54.2</td> <td>52.8 / 52.1</td> <td><strong>4.8 / 46.4</strong></td> </tr> <tr> <td>OOP</td> <td>LLM (o3-mini)</td> <td>60.6 / 66.2</td> <td>59.0 / 64.0</td> <td><strong>28.0 / 6.0</strong></td> </tr> </tbody> </table> <p>읽는 법: 정확도는 known/unknown 모두 거의 같다. 즉 <em>정확도만으론 평가자의 신뢰성을 알 수 없다</em>. 결정적 차이는 <strong>success rate</strong>다.</p> <ul> <li>IP의 success rate 격차 (100% DT vs 81% LLM): 둘 다 IP를 <em>진짜로</em> 안다.</li> <li>OOP의 success rate 격차 (5% DT vs 28% LLM): 둘 다 OOP를 <em>모른다</em>. LLM이 약간 더 높은 건 추측을 더 잘 해서지 진짜 아는 게 아님.</li> <li>즉 success rate가 28% 아래로 떨어지면 deception 의심.</li> </ul> <h3 id="west-frisian-φ와-무관한-안정적-success-rate">West Frisian: φ와 무관한 안정적 success rate</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab3-westfrisian-results-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab3-westfrisian-results-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab3-westfrisian-results-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab3-westfrisian-results.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 3: GPT-4.1 평가자+verifier의 West Frisian 결과. φ를 0.1에서 0.7까지 바꿔도 success rate는 IP 86–88%, OOP 1–2%로 거의 변하지 않는다.</figcaption> </figure> <table> <thead> <tr> <th>Setup</th> <th>$\phi$</th> <th>IP Succ/Flips</th> <th>OOP Succ/Flips</th> </tr> </thead> <tbody> <tr> <td>Unknown</td> <td>0.7</td> <td>87.8 / 9.3</td> <td>1.4 / 70.3</td> </tr> <tr> <td>Unknown</td> <td>0.3</td> <td>86.8 / 4.9</td> <td>1.2 / 33.6</td> </tr> <tr> <td>Unknown</td> <td>0.1</td> <td>86.2 / 1.8</td> <td>1.2 / 9.1</td> </tr> <tr> <td>Known (predict $y$)</td> <td>–</td> <td>76.3 / 80.3 (Acc/F1)</td> <td>51.5 / 67.2</td> </tr> <tr> <td>Known (avg per $c \in \bar{C}$)</td> <td>–</td> <td>89.5 / 94.0</td> <td>45.3 / 46.5</td> </tr> </tbody> </table> <p>읽는 법: <strong>success rate가 $\phi$와 사실상 독립</strong>이다. flip은 $\phi$에 따라 크게 변하지만 (OOP에서 9 → 70까지) success rate는 거의 고정. 이게 본 알고리즘의 가장 실용적 함의 — 운영 시점에 success rate를 <em>원시(raw) 신호</em>로 쓰고, $\phi$는 사후에 라벨 정확도를 끌어올리는 데만 튜닝하면 된다. 또 OOP에서 정확도가 <em>near-random</em> (50% 부근)인데 success rate는 1–2%로 일관되게 낮음: 정확도가 우연이라도 거짓은 폭로된다.</p> <h3 id="llm-다종-비교-appendix-d">LLM 다종 비교 (Appendix D)</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab5-llm-comparison-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab5-llm-comparison-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab5-llm-comparison-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab5-llm-comparison.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 5: 네 LLM(o3-mini, DeepSeek R1-Distill-Qwen-32B, GPT-4o, Qwen 2.5-VL 7B)의 합성 데이터 결과. IP known에서 o3-mini만 99.8%로 진짜 학습한 것이고, 나머지는 50–61% 수준. 그러나 모두 unknown에선 알려진 케이스에 정확도를 맞춰감 — 즉 정확도만으론 진짜 아는 모델과 알맞게 추측하는 모델을 구별할 수 없다는 점을 강조한다.</figcaption> </figure> <p>이 표가 짚는 핵심: 정확도는 같아 보여도 success rate가 모델별로 갈린다. 진짜 IP를 아는 o3-mini는 success가 53.8 (φ=0) → 55.4 (φ=0.5) → 55.4 (φ=0.9)로 일관. DeepSeek/GPT-4o/Qwen은 OOP에서 정확도가 <em>높아 보이는데</em> (78% 등) success rate가 6–34%로 낮아 deception을 폭로한다.</p> <h3 id="베이스라인-비교-ensemble은-라벨-없으면-무력하다">베이스라인 비교: ensemble은 라벨 없으면 무력하다</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab9-baselines-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab9-baselines-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab9-baselines-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab9-baselines.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 9: GPT-4.1, o3-mini, Qwen 2.5 세 평가자의 ensemble(평균/다수결) vs No-Data Algorithm. ensemble은 OOP에서 우연 이상의 정확도(50–61%)를 보이는데 라벨이 없으니 이게 deception인지 판단 불가. Cohen's κ는 IP에서 0.39(poor)지만 ensemble 성능은 known에 근접 — 일치도가 곧 신뢰가 아님.</figcaption> </figure> <table> <thead> <tr> <th> </th> <th>IP trust</th> <th>OOP trust</th> <th>IP Acc/F1</th> <th>OOP Acc/F1</th> </tr> </thead> <tbody> <tr> <td>Known (GPT-4.1)</td> <td>–</td> <td>–</td> <td>76.3 / 80.3</td> <td>51.5 / 67.2</td> </tr> <tr> <td>No-Data Algorithm ($\phi=0.1$)</td> <td><strong>86.2</strong></td> <td><strong>1.2</strong></td> <td>76.9 / 79.8</td> <td>50.7 / 16.4</td> </tr> <tr> <td>Ensemble (Average), κ / PA</td> <td>0.39 / 0.72</td> <td>0.0 / 0.37</td> <td>80.6 / 81.3</td> <td>50.1 / 0.0</td> </tr> <tr> <td>Ensemble (Majority vote), κ / PA</td> <td>0.39 / 0.72</td> <td>0.0 / 0.37</td> <td>75.5 / 79.9</td> <td>61.6 / 48.7</td> </tr> </tbody> </table> <p>읽는 법: ensemble은 IP에서 정확도가 단일 모델을 따라잡지만 (75–81%), κ=0.39는 Fleiss &amp; Cohen 1973 기준에선 <em>poor agreement</em>다. <em>라벨이 있다면</em> 우리는 이 모순을 해소할 수 있다 (정확도가 좋으니까 κ는 보정하면 된다고). 그러나 라벨이 <em>없으면</em> κ가 낮다는 신호를 신뢰해야 할지 무시해야 할지 결정 불가. OOP에서는 더 심각: majority vote가 61.6% 정확도를 보이지만 — 이건 <em>근본적으로 불가능한</em> 결과다 (OOP 분포에 대해 어떤 정보도 없어야 하므로). 그러나 κ=0.0이라는 <em>모순적</em> 신호로는 판단할 수 없다. 반면 No-Data Algorithm의 trust score (86.2 vs 1.2)는 <em>라벨 없이 단독으로</em> 결정 가능한 신호다.</p> <h2 id="결과-분석--ablation">결과 분석 / Ablation</h2> <h3 id="ablation-1-거짓-평가자와-거짓-generator-appendix-d2">Ablation 1: 거짓 평가자와 거짓 generator (Appendix D.2)</h3> <p>DT 평가자 + $\phi=0.4$로 세 가지 거짓 시나리오:</p> <ul> <li><strong>Lie 1</strong> ($\sigma$ 모름): $C(x)$는 출력하지만 $\bar{C}(x)$는 모름. IP success 17.0 (Flips 35.1). 정확도는 58.6/58.8로 그대로.</li> <li><strong>Lie 2</strong> ($f$ 모름): $y’ = y$인 $x’$만 retrieve 가능. IP success 0.6 (Flips 37.4). 거의 모든 라운드에서 잡힘.</li> <li><strong>Lie 3</strong> (확률 $p=1/10$로 거짓): generator 단계에서 1/10 확률로 잘못된 라벨 출력. IP success 43.6 (Flips 24.9). 노이즈 있는 거짓이라 detection 어려움.</li> </ul> <p>핵심 관찰: $\sigma$를 모르는 거짓 (Lie 1)은 IP에서 success 17%로 <em>상대적으로 높음</em> — 평가자가 $f$의 절반을 알기 때문. $f$를 모르는 거짓 (Lie 2)은 IP에서 0.6%로 <em>완전히 폭로</em>. 후자가 더 심각한 거짓이라는 게 합리적.</p> <h3 id="ablation-2-llm-생성-전략-appendix-d3">Ablation 2: LLM 생성 전략 (Appendix D.3)</h3> <p>평가자가 <em>데이터에서 picking</em> vs <em>rubric으로부터 generation</em> 중 어떤 방식으로 $x’$를 만드는지 비교. o3-mini: picking IP success 81.3 vs generation 55.4 (격차 -26pp). GPT-4o: 28.7 vs 5.8 (격차 -23pp). 즉 LLM이 <em>자연어가 아닌</em> 바이너리 문자열을 새로 생성하는 건 어렵다는 게 드러난다. 그러나 §6.2의 자연어 실험에서는 generation도 잘 작동 — LLM은 자연어에 최적화돼 있기 때문.</p> <h3 id="ablation-3-linguistic-competency-appendix-d4">Ablation 3: Linguistic competency (Appendix D.4)</h3> <p>Qwen 2.5-VL 7B는 West Frisian에 능통하지 않은 평가자. IP에서 정확도는 ~35% 부근 (GPT-4.1의 76% 대비 크게 낮음), 그러나 <em>success rate는 여전히 거짓 평가자와 다른 패턴</em>을 보임. 이는 <em>uninformed liar</em>와 <em>sycophantic liar</em>가 다르게 폭로된다는 §3.2의 주장을 뒷받침한다 — Qwen은 단순히 무지(uninformed)할 뿐 거짓말하지 않으니 success rate가 적당히 떨어지지만 0이 되진 않음.</p> <h3 id="ablation-4-ternary-라벨-appendix-c1">Ablation 4: Ternary 라벨 (Appendix C.1)</h3> <p>$Y = {0, 1, 2}$로 확장: (a) 모든 $c_i$가 0, (b) 한 개만 1, (c) 모두 0이 아닌 경우 셋으로 분류. 이론적으로 one-vs-all로 $k-1$번 돌리면 보장이 유지되지만, 단순히 algorithm을 그대로 돌리면 약 6% 정확도 차이 발생. label flip을 disable하면 known에 약간 더 가까워짐. 본격적 multi-class 보장은 future work로 남겨둠 (Conjecture 1).</p> <h2 id="한계와-비판적-평가">한계와 비판적 평가</h2> <h3 id="저자가-인정한-한계">저자가 인정한 한계</h3> <ul> <li><strong>이진 라벨 가정</strong>: $Y = {0, 1}$에 집중. $k \ge 3$인 다중 라벨은 one-vs-all 분해로 가능하지만 정확한 보장은 추가 발전 필요.</li> <li><strong>루브릭 의존성</strong>: 알고리즘은 <em>주어진</em> 루브릭 $C$에 대해서만 신뢰를 측정한다. 루브릭이 잘못 설계되면 신뢰 점수는 <em>그 잘못된 루브릭에 대한</em> 신뢰일 뿐. §7.3에서 강조하듯 루브릭 설계 책임은 사용자에게 있다.</li> <li><strong>거짓-기반 정확도 가정</strong>: Theorem 5.2의 도출은 “평가자가 mislabel할 때마다 거짓말”한다고 가정. 정답을 단순히 <em>추측해서</em> 맞추는 평가자 (라벨은 맞지만 챌린지는 통과 못함)는 다른 분석이 필요.</li> <li><strong>$\sigma$가 알려지지 않은 시나리오</strong>: 실제 도메인에서는 진짜 aggregator가 알려지지 않을 수 있어 사용자 정의 surrogate가 필요한데, 그 경우 신뢰 보장은 surrogate에 대한 것뿐.</li> </ul> <h3 id="리뷰어가-추가로-보는-한계">리뷰어가 추가로 보는 한계</h3> <ul> <li><strong>Verifier 비용</strong>: 자연어 실험에서 evaluator+verifier 둘 다 GPT-4.1을 호출하고, generator로 또 호출한다. 1 라운드당 최소 3번의 GPT-4.1 호출 (max tokens는 다르지만). 1,015개 항목 × 3 라운드 × 3 호출 = ~9천 호출. 비용·latency 보고가 본문에 없다.</li> <li><strong>Generator 품질이 결과를 흔든다</strong>: §6.1.2의 LLM picking vs generation 격차 (~25pp)는 generator 능력이 평가자 능력만큼이나 중요할 수 있음을 시사. 그러나 본문에는 “picking 방식이 더 효과적”이라는 단순 결론만 있고, generator 품질 측정 자체에 대한 논의는 부재.</li> <li><strong>이론과 실증의 갭</strong>: Theorem 5.2의 $(1/4)^r$ bound는 <em>완벽한 verifier</em> 가정. 실제로 verifier가 GPT-4.1이라면 verifier 자체의 reliability가 보장을 깨뜨릴 수 있다. §7.2에서 “evaluator와 verifier가 다른 task를 푼다”고 변호하지만, 두 GPT-4.1 인스턴스가 서로의 prompt에 sensitive한 정도가 정량화되지 않음.</li> <li><strong>Rubric ambiguity가 결과의 일반성을 흔든다</strong>: §6.2의 IP rubric은 <em>내부 모순</em>을 포함 (“응답은 정확해야 한다” + “사용자 지시를 정확히 따라야 한다 — 답이 틀려도”). 이런 ambiguity에도 알고리즘이 작동한 게 강점인 동시에, “rubric design이 잘 되면” 가정과 충돌한다.</li> <li><strong>수치 신뢰성의 외부 검증 부재</strong>: Table 5에서 “DeepSeek IP known 61.0%”는 의외로 낮은데 (DeepSeek은 일반적으로 강력함), 본 setup 특유의 binary string task에 대한 결과인지, 일반적 reasoning 약점인지 분리 안 됨. 다른 task에서 같은 결과를 재현하는 실험이 없다.</li> </ul> <h2 id="시사점--takeaways">시사점 / Takeaways</h2> <ul> <li><strong>“정확도가 같다”는 “신뢰가 같다”가 아니다</strong>: Table 2의 DT/LLM IP 사례, Table 9의 ensemble OOP 사례에서 정확도는 비슷하거나 ensemble이 더 높은데 <em>알고리즘적 신뢰 신호</em>는 완전히 갈린다. 라벨이 없을 때 정확도는 <em>uninterpretable</em> 양이다.</li> <li><strong>Success rate가 $\phi$와 무관하다는 점은 운영상 큰 자산</strong>: West Frisian 실험에서 success rate가 86 ± 1%로 $\phi \in {0.1, 0.3, 0.7}$ 전체에서 안정. 따라서 운영 시 trust 측정을 하면서 $\phi$는 <em>post-hoc accuracy 향상</em>을 위해 따로 튜닝하면 된다.</li> <li><strong>저자원 언어, 의료, 시장조사 같은 라벨 희소 도메인에서 정말 쓸 수 있는 도구</strong>: 라벨이 없는 환경에서 LLM judge의 “이 모델 이 데이터를 안다”를 <em>알고리즘적으로</em> 검증할 수 있는 첫 번째 도구. West Frisian 실험은 이게 toy가 아니라는 증거.</li> <li><strong>루브릭이 곧 측정</strong>: 알고리즘은 <em>주어진 루브릭</em>에 대한 신뢰만 측정한다. 따라서 루브릭 디자인이 곧 평가의 본질이 된다 — 본 논문이 §7.3에서 강조하듯 이는 과학적 방법론 ($H_0$ 설계)과 동형. 알고리즘이 좋아도 루브릭이 ill-posed하면 결과는 의미 없다.</li> </ul> <h2 id="설치-및-사용법">설치 및 사용법</h2> <p>저자의 <a href="https://github.com/adewynter/no_data_algorithm">GitHub repo</a>에 모든 데이터와 코드가 공개되어 있다. README와 코드 구조를 직접 보는 게 가장 빠르지만, 본문 §6의 setup을 그대로 재현하려면 OpenAI API 키와 (West Frisian 실험을 돌리려면) GPT-4.1 액세스가 필요하다. 합성 실험은 외부 API 없이도 DT 평가자만으로 돌릴 수 있어 알고리즘 동작을 보기에 적합하다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/adewynter/no_data_algorithm
<span class="nb">cd </span>no_data_algorithm
<span class="c"># 자세한 실행은 repo README 참조</span>
</code></pre></div></div> <h2 id="참고-자료">참고 자료</h2> <ul> <li>논문: <a href="https://arxiv.org/abs/2506.03083">https://arxiv.org/abs/2506.03083</a></li> <li>Code &amp; 데이터: <a href="https://github.com/adewynter/no_data_algorithm">https://github.com/adewynter/no_data_algorithm</a></li> </ul> <h2 id="더-읽어보기">더 읽어보기</h2> <ul> <li><strong><a href="https://dl.acm.org/doi/10.1145/22145.22192">Trading Group Theory for Randomness</a></strong> (Babai, STOC 1985) — Arthur-Merlin 프로토콜의 원조 논문. 본 논문의 EV 프로토콜이 변형한 zero-knowledge style 챌린지-응답의 출발점.</li> <li><strong><a href="https://arxiv.org/abs/2306.05685">Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena</a></strong> (Zheng et al., NeurIPS 2023) — LLM-as-a-judge 패러다임을 학계 표준에 가깝게 자리잡힌 작업. position/verbosity bias 같은 본 논문이 풀고자 하는 신뢰 문제의 출발점.</li> <li><strong><a href="https://arxiv.org/abs/2303.16634">G-Eval: NLG Evaluation using GPT-4 with Better Human Alignment</a></strong> (Liu et al., EMNLP 2023) — GPT-4를 NLG 평가자로 쓰는 대표적 작업. 본 논문이 다루는 “평가자 신뢰” 문제의 근본 동기.</li> <li><strong><a href="https://arxiv.org/abs/2412.05579">LLMs-as-Judges: A Comprehensive Survey on LLM-based Evaluation Methods</a></strong> (Li et al., 2024) — LLM-as-judge 분야의 종합 survey. 본 논문이 위치하는 더 큰 맥락 파악에 유용.</li> <li><strong><a href="https://arxiv.org/abs/2310.18018">NLP Evaluation in trouble: On the Need to Measure LLM Data Contamination for each Benchmark</a></strong> (Sainz et al., EMNLP 2023) — 라벨이 <em>있어도</em> 모델이 미리 본 데이터일 수 있다는 우려를 제기한 position paper. 본 논문이 <em>라벨이 없는</em> 영역으로 한 발 더 나아간 동기.</li> <li><strong><a href="https://arxiv.org/abs/2408.15409">Awes, Laws, and Flaws From Today’s LLM Research</a></strong> (de Wynter, ACL 2025) — 같은 저자의 LLM 연구 메타 분석. 2,000여 편 LLM 논문을 비판적으로 검토하며 LLM-as-evaluator의 부상을 정량화. 본 논문이 그 비판에 대한 <em>해결책</em>을 시도한다는 점에서 함께 읽기 좋다.</li> <li><strong><a href="https://arxiv.org/abs/2503.08327">Adding Chocolate to Mint: Mitigating Metric Interference in Machine Translation</a></strong> (Pombal et al., 2025) — 평가 메트릭 자체가 학습 신호와 간섭한다는 문제. 본 논문과 보완적 — 한쪽이 metric 신뢰, 한쪽이 evaluator 신뢰.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="llm-as-a-judge"/><category term="evaluation"/><category term="zero-knowledge-proof"/><category term="trust"/><category term="low-resource-language"/><summary type="html"><![CDATA[라벨 없는 환경에서 LLM-as-a-judge 같은 평가자(evaluator)의 신뢰를 zero-knowledge 스타일의 챌린지-응답 프로토콜로 (1/4)^r 확률 한계까지 증명적으로 확립하는 No-Data Algorithm.]]></summary></entry><entry xml:lang="en"><title type="html">[Paper Review] Algorithmically Establishing Trust in Evaluators</title><link href="https://baba9811.github.io/en/papers/0014-algorithmically-establishing-trust-in-evaluators/" rel="alternate" type="text/html" title="[Paper Review] Algorithmically Establishing Trust in Evaluators"/><published>2026-05-14T13:00:00+00:00</published><updated>2026-05-14T13:00:00+00:00</updated><id>https://baba9811.github.io/en/papers/0014-algorithmically-establishing-trust-in-evaluators</id><content type="html" xml:base="https://baba9811.github.io/en/papers/0014-algorithmically-establishing-trust-in-evaluators/"><![CDATA[<div class="lang-toggle" style="margin-bottom: 1.5rem;"> <a href="/papers/0014-algorithmically-establishing-trust-in-evaluators/" class="btn btn-sm btn-outline-secondary">🇰🇷 한국어로 읽기</a> </div> <h2 id="metadata">Metadata</h2> <table> <thead> <tr> <th>Field</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Authors</td> <td>Adrian de Wynter (The University of York · Microsoft)</td> </tr> <tr> <td>Venue</td> <td>arXiv preprint · 2026</td> </tr> <tr> <td>arXiv</td> <td><a href="https://arxiv.org/abs/2506.03083">2506.03083</a></td> </tr> <tr> <td>Code</td> <td><a href="https://github.com/adewynter/no_data_algorithm">adewynter/no_data_algorithm</a></td> </tr> <tr> <td>Data</td> <td>Synthetic binary strings (IP/OOP, 498 each) · West Frisian low-resource labelling corpus (1,015 entries)</td> </tr> <tr> <td><span style="white-space: nowrap">Review date</span></td> <td>2026-05-14</td> </tr> </tbody> </table> <h2 id="tldr">TL;DR</h2> <ul> <li>The <strong>No-Data Algorithm</strong> algorithmically verifies whether an evaluator (e.g., an LLM-as-a-judge) <em>truly</em> knows the labels it claims to assign, <em>without any reference data at all</em>.</li> <li>It works by running an <strong>Evaluator–Verifier (EV) protocol</strong> that issues one of two mutually exclusive challenges (equality up to permutation, equality up to isomorphism) per round. The construction is a zero-knowledge-style adaptation of Babai’s (1985) Arthur–Merlin protocol.</li> <li><strong>Provable guarantees</strong>: after $r$ rounds, the probability that the verifier misses a lying evaluator is bounded by $(1/4)^r$. For an evaluator with accuracy $\alpha$, $\mathbb{E}[\text{correct}] \le 1 - (1-\alpha)(1 - \phi + \phi(1/4)^r)$.</li> <li><strong>Empirically</strong>: on synthetic binary strings with o3-mini, the success rate drops to 28% under out-of-distribution data, exposing deception. On a West Frisian labelling task with GPT-4.1 as both evaluator and verifier, success rates cleanly separate at 86–88% in-domain vs 1–2% out-of-domain.</li> </ul> <h2 id="introduction">Introduction</h2> <p>Measurement is one of the most fundamental problems in AI, and right now it is in trouble. The LLM-as-a-judge paradigm has become the de facto evaluation standard, yet there is a growing body of evidence that these evaluators are themselves <em>unreliable</em>. Traditional approaches to certifying an evaluator come in two flavours: (1) measure it against labelled reference data, or (2) assume the evaluator “somehow” knows the right answer. Both fail in the regime that matters most — when references are scarce or unavailable.</p> <p>That regime is not a corner case. <strong>Low-resource languages</strong> like West Frisian have essentially no labelled NLP data; <strong>medical</strong> domains have prohibitive annotation costs; <strong>market research</strong> problems are unique by construction; and even when labels do exist, benchmark contamination (Sainz et al. 2023) erodes their value. Naïvely deploying an LLM judge in any of these settings strips the resulting accuracy numbers of statistical meaning.</p> <p>This paper introduces the <strong>No-Data Algorithm</strong>, which establishes trust in an evaluator with <em>mathematical rigor</em> — failing to detect a lying evaluator with probability at most $(1/4)^r$ after $r$ challenge rounds. The construction has two key moves. First, the evaluator is asked to produce <em>similar</em> datapoints and their consistency is probed (the challenge–response game familiar from zero-knowledge proofs). Second, the two challenges are <em>informationally complementary</em> — together they are necessary and sufficient to certify knowledge of the true labelling function. An evaluator without that knowledge can pass at most one challenge in any given round, so its luck across $r$ rounds decays as $(1/4)^r$.</p> <p>None of the underlying mathematics is new — Arthur–Merlin protocols and zero-knowledge proofs go back to the 1980s. What is new is the <em>transposition</em> of these ideas to the problem of evaluator trust, demonstrating that the bounds are tight enough and the protocol cheap enough to be useful in practice. In an era when accuracy numbers are everywhere and labels are nowhere, the meta-question — <em>can we trust our accuracy numbers at all?</em> — deserves a principled answer.</p> <h2 id="key-contributions">Key Contributions</h2> <ul> <li><strong>The No-Data Algorithm</strong>: an algorithmic procedure that establishes evaluator trust to within a $(1/4)^r$ probability of error, with no labelled data required. The construction adapts the AM challenge–response protocol to the labelling setting.</li> <li><strong>Two correctness bounds</strong>: Lemma 5.1 ($(1/4)^r$ bound on the EV sub-protocol) and Theorem 5.2 (the full No-Data Algorithm’s expected correctness in terms of evaluator accuracy $\alpha$ and a label-flip probability $\phi$).</li> <li><strong>Synthetic and real-language experiments</strong>: a controlled synthetic binary-string study (in-phenomenon vs out-of-phenomenon) validates the theory, and a West Frisian labelling task demonstrates real-world applicability.</li> <li><strong>Comparison against baselines</strong>: agreement metrics like Cohen’s κ and percentage agreement (PA) require calibration unavailable without labels; the No-Data Algorithm produces a single trust score (86.2 IP vs 1.2 OOP in Table 9) that cleanly separates trustworthy from deceptive evaluators.</li> </ul> <h2 id="related-work--background">Related Work / Background</h2> <p><strong>The Arthur–Merlin protocol (Babai 1985).</strong> A computationally unbounded prover (Merlin) convinces a probabilistically-bounded verifier (Arthur) that some statement holds via a challenge–response game; see Goldreich (2009) and Arora &amp; Barak (2009) for textbook treatments. The EV protocol borrows the multi-round challenge structure but drops the <em>secrecy</em> guarantee of zero-knowledge proofs — the goal is to certify <em>knowledge</em>, not protect it.</p> <p><strong>The rise of LLM-as-a-judge.</strong> Zheng et al.’s MT-Bench/Chatbot Arena (Zheng et al., NeurIPS 2023) and Liu et al.’s G-Eval (Liu et al., EMNLP 2023) cemented the practice of using GPT-4-class models as automatic evaluators. Li et al. (2024) survey the rapidly-growing literature. Several follow-ups have flagged reliability concerns: Gehmann et al. 2023 on cracks in evaluation practice, Bavaresco et al. 2024 on 20 NLP evaluation tasks where LLM judges fail to substitute for humans, Hada et al. 2024 on dramatic quality drops in multilingual settings.</p> <p><strong>Limits of agreement metrics.</strong> In the absence of labels, the natural fallback is <em>evaluator-to-evaluator agreement</em> — Cohen’s κ, Fleiss &amp; Cohen 1973’s ICC, percentage agreement. But these metrics require calibration: a κ of 0.4 might be acceptable in one domain and damning in another, and calibrating it requires reference data. As the paper points out, in the no-label regime these signals are “either contradictory or insufficient” — which is exactly what Table 9 quantifies.</p> <p><strong>Provable ML.</strong> Jovanović et al. 2023 establish provable fairness in representation learning, Dimitrov et al. 2022 do the same for adversarial robustness. This paper sits in the same provability tradition, extended to evaluator certification.</p> <h2 id="method--architecture">Method / Architecture</h2> <h3 id="notation-and-assumptions">Notation and Assumptions</h3> <p>Datapoints $x \in X$, labels $y \in Y$. The paper focuses on $Y = {0,1}$ (binary classification) and $X \subseteq {0,1}^n$ ($n$-bit binary strings). These assumptions simplify the proofs; §7 argues the construction extends to natural-language $X$.</p> <p>For each $x \in {0,1}^n$ define its <em>relevant subset</em> $S_x \in 2^x$ — for instance, the set of substrings; the exact construction depends on the setup.</p> <p>A <strong>criterion</strong> $c\colon X \to {0,1}$ classifies datapoints. A <strong>rubric</strong> $C\colon \times_{i,\ldots,n} {0,1} \to {0,1}^n$ is an ordered concatenation of criteria $\mathcal{C} = {c_1, \ldots, c_n}$: $C = c_1 c_2 \ldots c_n$. If some criterion is nonlinear (e.g., $c_i = c_a \oplus c_b$), its decomposed form $\bar{C}$ is what the verifier reasons about. A rubric is <strong>total</strong> when every nonlinear criterion is explicitly decomposed.</p> <p>An <strong>aggregator</strong> $\sigma\colon {0,1}^n \to Y$ collapses the $n$-bit rubric output into a final label (e.g., majority vote).</p> <p>Two points $x, x’ \in X$ are <strong>similar</strong> when $x \cong x’$ <em>and</em> $x \equiv x’$ — i.e., they are equal both up to isomorphism (structural equivalence) and up to permutation.</p> <p>A <strong>lying</strong> evaluator is one that makes unsubstantiated claims of correctness, whether <em>uninformed</em> (random guessing) or <em>sycophantic</em> (confident but wrong). The former fails everywhere, the latter only out-of-distribution. Distinguishing them without labels is the algorithm’s main job.</p> <p><strong>Assumption 3.1</strong> (Composition): the true map $f\colon X \to Y$ decomposes as $f = \sigma C$ — every datapoint has a reason (criterion) for its label, and if there are several, some aggregator combines them. The assumption is weak; when $C$ or $\sigma$ are unknown in practice, user-defined surrogates are allowed, with the caveat that trust bounds apply to the <em>surrogate</em>.</p> <h3 id="the-ev-protocol--one-round">The EV Protocol — One Round</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/fig1-ev-protocol-flow-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/fig1-ev-protocol-flow-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/fig1-ev-protocol-flow-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/fig1-ev-protocol-flow.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Figure 1: one round of the EV protocol. The evaluator (blue) generates an x′ similar to x with a partial label ỹ′; the verifier (orange) then chooses one of two challenges uniformly at random and demands an answer. Repeat r times.</figcaption> </figure> <p>Both players see $x$ and the rubric $C$; the aggregator $\sigma$ is hidden from the verifier. The evaluator claims to know $f\colon X \to Y$; since $C$ is public, that is equivalent to claiming to know $\sigma$. The verifier wants to be <em>sufficiently sure</em> the evaluator’s label for $x$ is trustworthy.</p> <p><strong>One round proceeds as follows:</strong></p> <ol> <li>The evaluator generates an $x’ \in X$ similar to $x$, together with a <em>partial label</em> $\tilde{y}’$. “Partial” because the evaluator has no access to $f$ and can only guess from $C(x’)$. Once generated, $\langle x’, \tilde{y}’ \rangle$ is fixed.</li> <li>The verifier picks one of two challenges uniformly at random: <ul> <li><strong>Challenge 1 (Equality up to Permutation)</strong> — “show me why $x’ \equiv x$”. The verifier checks \(\forall s \in S_x, \exists t \in S_{x'}.\ \forall c \in \bar{C},\ c(s) = c(t) \quad (2)\) i.e., some substring of $x’$ matches the (total) evaluation of a criterion from $x$.</li> <li><strong>Challenge 2 (Equality up to Isomorphism)</strong> — “show me why $f(x’) = y$”. Since $y$ is unavailable, the verifier checks the proxy $x’ \cong x$, namely $C(x’) = C(x)$.</li> </ul> </li> <li>If the evaluator fails, the protocol returns failure immediately; if all $r$ rounds pass, it returns success. Either way it returns $\tilde{y}’$.</li> </ol> <h3 id="why-both-challenges-are-necessary">Why Both Challenges Are Necessary</h3> <p>Lemma A.1’s key claim: the two challenges provide <em>mutually exclusive</em> information and are <em>jointly</em> necessary and sufficient to detect lies.</p> <ul> <li><strong>Challenge 1 alone</strong> is insufficient: even if $x’$’s relevant substructure matches $x$’s, the strings can differ enough to receive different rubric encodings (e.g., when a criterion is a disjunction). The challenge probes permutation equivalence but says nothing about isomorphism.</li> <li><strong>Challenge 2 alone</strong> is insufficient: $C(x’) = C(x)$ matches the <em>final</em> encoding but not the <em>internal</em> structure of the bitstrings.</li> </ul> <p>So an evaluator that knows $f$ passes both challenges deterministically. One that does not has at most two cheat paths:</p> <ul> <li><strong>Lie 1: produce a “good” $x’$</strong>: pick $x’$ with $S_{x’} = S_x$, passing Challenge 1; Challenge 2 ($C(x’) = C(x)$) is then a coin flip — 1/2 of failing.</li> <li><strong>Lie 2: aim for $f(x’) \ne y$</strong>: pick $x’$ with $C(x’) = C(x)$, passing Challenge 2; Challenge 1 is then a coin flip.</li> </ul> <p>Since the challenge is itself chosen with probability 1/2, the per-round success rate of a lying evaluator is at most $1/2 \cdot 1/2 = 1/4$. After $r$ rounds, $(1/4)^r$.</p> <h3 id="the-full-no-data-algorithm">The Full No-Data Algorithm</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/alg1-no-data-algorithm-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/alg1-no-data-algorithm-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/alg1-no-data-algorithm-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/alg1-no-data-algorithm.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Algorithm 1: the No-Data Algorithm. For each x in X it runs the EV protocol for r rounds; on failure, it flips the evaluator's label with probability φ. Returns a success count and the (possibly flipped) predictions.</figcaption> </figure> <p>The algorithm sweeps over $X$, calling EV per datapoint. On success it accepts the evaluator’s label $y$ and increments the success count. On failure — a signal that the evaluator doesn’t know the label — it flips $y$ with probability $\phi$ and records a 0 in the success counter. Final outputs: a list of predictions and a success count.</p> <p>The flip step matters because failure suggests the evaluator is <em>consistently wrong</em> (sycophantic), in which case its flipped label is more likely correct. $\phi$ is tuned to the evaluator’s estimated error rate; Theorem 5.2 then guarantees that the algorithm’s output accuracy tracks the “known case” closely.</p> <h2 id="training-objective--loss-function">Training Objective / Loss Function</h2> <p>There is no model being trained here — the No-Data Algorithm is a <em>meta</em> procedure that audits an external evaluator. The core “losses” are two <strong>correctness bounds</strong>.</p> <h3 id="lemma-51--ev-protocol-bound">Lemma 5.1 — EV Protocol Bound</h3> \[\Pr[\text{verifier fails to detect a lie after } r \text{ rounds}] \le \left(\frac{1}{4}\right)^r\] <p>Proof sketch: as analyzed above, each of the two lying strategies passes a single round with probability $\le 1/4$. Union bound over $r$ rounds.</p> <h3 id="theorem-52--full-algorithm-bound">Theorem 5.2 — Full Algorithm Bound</h3> <p>For a balanced dataset $X$ and an evaluator with accuracy $\alpha$ that lies whenever it mislabels,</p> \[\begin{aligned} \mathbb{E}[\text{correct}] \le 1 - (1 - \alpha)\left(1 - \phi + \phi\left(\frac{1}{4}\right)^r\right) \end{aligned} \quad (3)\] <p>Proof sketch (Appendix A.4): with $\epsilon = 1 - \alpha$,</p> \[\begin{aligned} \mathbb{E}[\text{undetected}] &amp;= \epsilon \left(\frac{1}{4}\right)^r, \\ \mathbb{E}[\text{detected, no flip}] &amp;= \epsilon(1 - \phi)\left(1 - \left(\frac{1}{4}\right)^r\right), \\ \mathbb{E}[\text{wrong}] &amp;\le \epsilon\left(1 - \phi + \phi\left(\frac{1}{4}\right)^r\right). \end{aligned}\] <p>What this says: with a high-accuracy evaluator and a well-calibrated $\phi$, the algorithm’s output mirrors the known case. Conversely, if $\alpha = 0$ (the evaluator is always wrong), there is no way to recover labels — and §7.1 stresses this is the point. The algorithm’s purpose is to <em>establish trust</em>, not to label data ex nihilo.</p> <h3 id="lemma-b1--runtime">Lemma B.1 — Runtime</h3> <p>If both players run in polynomial time on any input, the No-Data Algorithm runs in $O(r|D|)$ on a dataset of size $|D|$. Linear in the data, modulo a small constant in the number of rounds.</p> <h2 id="training-data-and-pipeline">Training Data and Pipeline</h2> <p>Two experimental setups; in both, the evaluator is “trained” (or simply observed) externally, and the No-Data Algorithm only audits on top.</p> <h3 id="experiment-1--synthetic-binary-strings">Experiment 1 — Synthetic Binary Strings</h3> <table> <thead> <tr> <th>Item</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Input domain</td> <td>${0,1}^n$</td> </tr> <tr> <td>Datasets</td> <td>In-Phenomenon (IP) / Out-of-Phenomenon (OOP), disjoint</td> </tr> <tr> <td>Test size</td> <td>498 each, balanced</td> </tr> <tr> <td>Aggregator $\sigma$</td> <td>Majority vote</td> </tr> <tr> <td>Evaluator 1</td> <td>Decision Tree (sklearn defaults)</td> </tr> <tr> <td>Evaluator 2</td> <td>o3-mini (OpenAI, 2025-01-31), max tokens 50,000</td> </tr> <tr> <td>Verifier</td> <td>Rule-matching algorithm against the appropriate rubric</td> </tr> <tr> <td>$\phi$</td> <td>0.4 for DT, 0.1 for LLM</td> </tr> <tr> <td>$r$</td> <td>3</td> </tr> </tbody> </table> <p>IP rubric: $c_0$ (even number of ones), $c_1 = c_{1,a} \oplus c_{1,b}$ (starts with 0 XOR contains 10101), $c_2$ (more than five ones). OOP rubric: $c_0$ (contains 111), $c_1$ (ends with 1), $c_2$ (contains 110001). Key design: both DT and LLM have been trained/observed on IP; neither has seen OOP. We expect the success rate to drop in OOP, exposing the lack of knowledge.</p> <h3 id="experiment-2--west-frisian-low-resource-language">Experiment 2 — West Frisian Low-Resource Language</h3> <table> <thead> <tr> <th>Item</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Data source</td> <td>Random prompts from OpenOrca, MMLU, OpenCode, WildChat</td> </tr> <tr> <td>Output generation</td> <td>GPT-4o</td> </tr> <tr> <td>Translation</td> <td>4 native West Frisian speakers, expert translation + annotation</td> </tr> <tr> <td>Annotator pay</td> <td>28.7 USD/hr base, seniority-adjusted</td> </tr> <tr> <td>Dataset size</td> <td>1,015 entries (train 500 / test 515)</td> </tr> <tr> <td>Evaluator + verifier</td> <td>Both GPT-4.1 (LONGCO-2025-04-14), distinct prompts</td> </tr> <tr> <td>Evaluator/verifier max tokens</td> <td>256</td> </tr> <tr> <td>Generator (for similar datapoints) max tokens</td> <td>5,000</td> </tr> <tr> <td>Temperature</td> <td>0</td> </tr> <tr> <td>$\phi$</td> <td>Tested at 0.1, 0.3, 0.7</td> </tr> </tbody> </table> <p>The IP rubric has 6 criteria: response in West Frisian; culturally/argumentatively correct; (if code) syntactically correct; grammatically correct; not cut off; follows user instructions exactly. The OOP rubric has 7 criteria designed to be almost orthogonal — response in Dutch (since GPT-4o tends to default to Dutch when asked for West Frisian), references to Dutch culture, etc. Only the OOP rubric is decomposable; the IP rubric carries some intentional ambiguity, mirroring real-world deployment.</p> <h2 id="experimental-results">Experimental Results</h2> <h3 id="synthetic-data-the-ipoop-success-rate-gap">Synthetic Data: the IP/OOP Success-Rate Gap</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab1-rubrics-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab1-rubrics-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab1-rubrics-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab1-rubrics.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 1: the two rubrics. The IP rubric's c₁ decomposes via XOR into five effective criteria {c₀, c₂, c₁ₐ, c₁ᵦ, c₁}.</figcaption> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab2-synthetic-results-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab2-synthetic-results-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab2-synthetic-results-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab2-synthetic-results.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 2: known (reference) and unknown (No-Data Algorithm) results. Both evaluators track their known-case accuracy in OOP, but their success rates collapse — that's the deception signal.</figcaption> </figure> <table> <thead> <tr> <th>Setup</th> <th>Evaluator</th> <th>Known Acc/F1</th> <th>Unknown Acc/F1</th> <th>Unknown Succ/Flips</th> </tr> </thead> <tbody> <tr> <td>IP</td> <td>DT</td> <td>62.2 / 58.8</td> <td>62.2 / 59.8</td> <td><strong>100.0 / 0.0</strong></td> </tr> <tr> <td>IP</td> <td>LLM (o3-mini)</td> <td>99.8 / 99.8</td> <td>97.6 / 97.6</td> <td><strong>81.3 / 1.8</strong></td> </tr> <tr> <td>OOP</td> <td>DT</td> <td>54.2 / 54.2</td> <td>52.8 / 52.1</td> <td><strong>4.8 / 46.4</strong></td> </tr> <tr> <td>OOP</td> <td>LLM (o3-mini)</td> <td>60.6 / 66.2</td> <td>59.0 / 64.0</td> <td><strong>28.0 / 6.0</strong></td> </tr> </tbody> </table> <p>How to read it: accuracy is essentially the same between known and unknown columns. <em>Accuracy alone tells us nothing about evaluator trust.</em> The decisive signal is the <strong>success rate</strong>.</p> <ul> <li>IP success gap (100% DT vs 81% LLM): both genuinely know IP.</li> <li>OOP success gap (5% DT vs 28% LLM): both are wrong about OOP, with LLM being slightly luckier (better guessing, not knowledge).</li> <li>Operationally: success rate below ~30% is the deception flag.</li> </ul> <h3 id="west-frisian-success-rate-is-independent-of-φ">West Frisian: Success Rate Is Independent of φ</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab3-westfrisian-results-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab3-westfrisian-results-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab3-westfrisian-results-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab3-westfrisian-results.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 3: GPT-4.1 evaluator + verifier on West Frisian. Sweeping φ from 0.1 to 0.7 changes flips dramatically but leaves the success rate near 86–88% in IP and 1–2% in OOP.</figcaption> </figure> <table> <thead> <tr> <th>Setup</th> <th>$\phi$</th> <th>IP Succ/Flips</th> <th>OOP Succ/Flips</th> </tr> </thead> <tbody> <tr> <td>Unknown</td> <td>0.7</td> <td>87.8 / 9.3</td> <td>1.4 / 70.3</td> </tr> <tr> <td>Unknown</td> <td>0.3</td> <td>86.8 / 4.9</td> <td>1.2 / 33.6</td> </tr> <tr> <td>Unknown</td> <td>0.1</td> <td>86.2 / 1.8</td> <td>1.2 / 9.1</td> </tr> <tr> <td>Known (predict $y$)</td> <td>–</td> <td>76.3 / 80.3 (Acc/F1)</td> <td>51.5 / 67.2</td> </tr> <tr> <td>Known (avg per $c \in \bar{C}$)</td> <td>–</td> <td>89.5 / 94.0</td> <td>45.3 / 46.5</td> </tr> </tbody> </table> <p>The big observation: <strong>the success rate is essentially flat in $\phi$</strong>. Flips swing wildly (OOP goes 9 → 70 as $\phi$ rises), but trust remains stable. The practical implication is that <em>you measure trust with raw success rate, then tune $\phi$ post-hoc for accuracy gains</em> — a clean separation of concerns. Note also that OOP accuracies are near-random (~50%), yet the success rate consistently calls out the deception.</p> <h3 id="comparing-llms-appendix-d">Comparing LLMs (Appendix D)</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab5-llm-comparison-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab5-llm-comparison-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab5-llm-comparison-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab5-llm-comparison.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 5: four LLMs on the synthetic task — o3-mini, DeepSeek R1-Distill-Qwen-32B, GPT-4o, Qwen 2.5-VL 7B. Only o3-mini truly learned IP (99.8% known); the others sit at 50–61% but adjust their unknown accuracy to match, illustrating why accuracy alone cannot distinguish 'knows' from 'lucky guess'.</figcaption> </figure> <p>This table sharpens the message: with the same nominal accuracy, the success rate cleaves the models. o3-mini, which genuinely knows IP, maintains a high success rate across $\phi$. DeepSeek/GPT-4o/Qwen post deceptively high OOP accuracy (up to ~78%) but their success rates (6–34%) expose the lack of knowledge.</p> <h3 id="baselines-ensembles-are-useless-without-labels">Baselines: Ensembles Are Useless Without Labels</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab9-baselines-480.webp 480w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab9-baselines-800.webp 800w,/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab9-baselines-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/papers/0014-algorithmically-establishing-trust-in-evaluators/tab9-baselines.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Table 9: averaging and majority-vote ensembles (GPT-4.1, o3-mini, Qwen 2.5) vs the No-Data Algorithm. Ensembles hit above-random accuracy in OOP — impossible by construction — and yet Cohen's κ is contradictory. The No-Data Algorithm's trust separation (86.2 vs 1.2) makes the deception unambiguous.</figcaption> </figure> <table> <thead> <tr> <th> </th> <th>IP trust</th> <th>OOP trust</th> <th>IP Acc/F1</th> <th>OOP Acc/F1</th> </tr> </thead> <tbody> <tr> <td>Known (GPT-4.1)</td> <td>–</td> <td>–</td> <td>76.3 / 80.3</td> <td>51.5 / 67.2</td> </tr> <tr> <td>No-Data Algorithm ($\phi=0.1$)</td> <td><strong>86.2</strong></td> <td><strong>1.2</strong></td> <td>76.9 / 79.8</td> <td>50.7 / 16.4</td> </tr> <tr> <td>Ensemble (Average), κ / PA</td> <td>0.39 / 0.72</td> <td>0.0 / 0.37</td> <td>80.6 / 81.3</td> <td>50.1 / 0.0</td> </tr> <tr> <td>Ensemble (Majority vote), κ / PA</td> <td>0.39 / 0.72</td> <td>0.0 / 0.37</td> <td>75.5 / 79.9</td> <td>61.6 / 48.7</td> </tr> </tbody> </table> <p>Reading it: in IP the ensembles’ accuracy matches (or exceeds) the single model, but κ=0.39 is <em>poor agreement</em> by Fleiss &amp; Cohen 1973. If we had labels we could resolve the contradiction (accuracy wins, ignore κ). Without labels, the contradictory signal is operationally useless. Worse in OOP: majority vote reports 61.6% accuracy — <em>impossible</em> given the setup, since the ensemble has zero information about OOP — but κ=0.0 leaves the contradiction unresolved. The No-Data Algorithm’s trust pair (86.2 vs 1.2) is a single signal that cleanly accepts IP and rejects OOP.</p> <h2 id="analysis--ablations">Analysis / Ablations</h2> <h3 id="ablation-1-lying-evaluators-and-lying-generators-appendix-d2">Ablation 1: Lying Evaluators and Lying Generators (Appendix D.2)</h3> <p>DT evaluator + $\phi = 0.4$, three lying strategies:</p> <ul> <li><strong>Lie 1</strong> ($\sigma$ unknown): can output $C(x)$ but not $\bar{C}(x)$. IP success 17.0 (Flips 35.1); accuracy unchanged at 58.6/58.8.</li> <li><strong>Lie 2</strong> ($f$ unknown): can retrieve $x’$ with $y’ = y$ but nothing else. IP success 0.6 (Flips 37.4) — almost every round catches it.</li> <li><strong>Lie 3</strong> (generator lies with probability $p = 1/10$): noisy lying, harder to detect. IP success 43.6 (Flips 24.9).</li> </ul> <p>Key observation: not knowing $\sigma$ (Lie 1) leaves a partial signal because the evaluator still has half of $f$; not knowing $f$ at all (Lie 2) is catastrophic, as it should be. This matches the algorithm’s intuition that <em>deeper</em> lies leave fewer escape paths.</p> <h3 id="ablation-2-llm-generation-strategies-appendix-d3">Ablation 2: LLM Generation Strategies (Appendix D.3)</h3> <p>When the evaluator must <em>generate</em> $x’$ from scratch vs <em>pick</em> it from data, the success rate drops markedly: o3-mini IP picking 81.3 → generation 55.4 (−26pp); GPT-4o 28.7 → 5.8 (−23pp). This is a known weakness of LLMs on non-natural-language problems (binary strings here). It does not transfer to the natural-language §6.2 setup, where LLMs are in their element.</p> <h3 id="ablation-3-linguistic-competency-appendix-d4">Ablation 3: Linguistic Competency (Appendix D.4)</h3> <p>Qwen 2.5-VL 7B is not proficient in West Frisian. Its IP accuracy hovers around 35% (vs GPT-4.1’s 76%), but the success rate still distinguishes “uninformed” from “deceptive” — Qwen is <em>wrong</em> but doesn’t <em>lie</em>. This empirically supports the paper’s §3.2 distinction between uninformed and sycophantic lying.</p> <h3 id="ablation-4-ternary-labels-appendix-c1">Ablation 4: Ternary Labels (Appendix C.1)</h3> <p>Extending to $Y = {0, 1, 2}$ via three coarse labels (none, one, all-but-one criteria satisfied), the algorithm largely retains its behaviour but with ~6% accuracy degradation. Disabling label flip improves restoration, suggesting a proper $k$-ary version of the algorithm needs a richer flip mechanism. Formal $k$-ary guarantees are deferred to future work (Conjecture 1).</p> <h2 id="limitations-and-critical-assessment">Limitations and Critical Assessment</h2> <h3 id="author-acknowledged-limitations">Author-acknowledged limitations</h3> <ul> <li><strong>Binary labels</strong>: the formal bounds are for $Y = {0, 1}$. Multi-class needs a one-vs-all reduction (linear blow-up) or further theoretical development.</li> <li><strong>Rubric dependency</strong>: the algorithm certifies trust <em>with respect to a given rubric</em>. A poorly-designed rubric yields a meaningless trust score. §7.3 makes clear that rubric design is the user’s responsibility, analogous to choosing $H_0$ in scientific hypothesis testing.</li> <li><strong>The “lies-when-mislabels” assumption</strong>: Theorem 5.2 assumes the evaluator lies whenever it mislabels. An evaluator that <em>guesses correctly</em> (right label, wrong reasoning) is not captured and would require a separate analysis.</li> <li><strong>Surrogate rubrics</strong>: when the true $\sigma$ is unknown, users substitute a surrogate, but the trust bound then applies to the surrogate, not to ground truth.</li> </ul> <h3 id="reviewers-additional-concerns">Reviewer’s additional concerns</h3> <ul> <li><strong>Verifier cost is not reported</strong>: the natural-language setup invokes GPT-4.1 as evaluator, verifier, and generator — at least three API calls per round per datapoint. For 1,015 entries × 3 rounds × 3 calls that’s nearly 10K calls; the paper reports no wall-clock or USD cost numbers.</li> <li><strong>Generator quality is a hidden variable</strong>: the picking-vs-generation gap (~25pp in §6.1.2) suggests generator capability is at least as important as evaluator capability. The paper concludes “picking works better” without measuring generator quality directly.</li> <li><strong>Theory vs practice gap</strong>: the $(1/4)^r$ bound assumes a <em>perfect</em> verifier. In §6.2 the verifier is GPT-4.1, whose own reliability is in question. §7.2 defends this by saying “evaluator and verifier solve different tasks,” but the two GPT-4.1 instances’ sensitivity to each other’s prompts is not quantified.</li> <li><strong>Rubric ambiguity vs the “well-designed rubric” assumption</strong>: the §6.2 IP rubric contains internal contradictions (“response must be correct” + “follows user instructions exactly even when wrong”). The algorithm still works, which is impressive — but it also undermines the §7.3 claim that rubric quality is critical.</li> <li><strong>No replication across tasks</strong>: Table 5’s “DeepSeek IP known 61.0%” is surprisingly low and may reflect this binary-string task specifically rather than DeepSeek’s general reasoning. A cross-task replication would strengthen the claim.</li> </ul> <h2 id="takeaways">Takeaways</h2> <ul> <li><strong>Same accuracy does not mean same trust.</strong> Table 2’s DT vs LLM in IP, and Table 9’s ensembles in OOP, all post similar (or higher!) accuracies as the No-Data Algorithm, yet the algorithmic trust signal cleanly separates them. In the no-label regime, accuracy is an <em>uninterpretable</em> number.</li> <li><strong>The success rate is operationally robust.</strong> Across $\phi \in {0.1, 0.3, 0.7}$ on West Frisian, success stays within ~1pp. That means you can measure trust online while keeping $\phi$ as a separate post-hoc accuracy knob.</li> <li><strong>A real tool for label-scarce regimes.</strong> Low-resource languages, medicine, market research — domains where references are scarce <em>and</em> labels are expensive. This is the first algorithm to make “this evaluator knows what it’s doing” a provable, single-number claim without any references.</li> <li><strong>Rubric design is now front-and-center.</strong> The algorithm measures trust <em>relative to the rubric you specify</em>. Rubric design becomes the act of measurement itself — analogous to defining $H_0$ in scientific hypothesis testing. A good algorithm cannot save an ill-posed rubric.</li> </ul> <h2 id="setup-and-usage">Setup and usage</h2> <p>All code and data are available in the author’s <a href="https://github.com/adewynter/no_data_algorithm">GitHub repository</a>. The synthetic experiment can be reproduced without any external API by using the included decision-tree evaluator; the West Frisian setup requires OpenAI API access and GPT-4.1 quota.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/adewynter/no_data_algorithm
<span class="nb">cd </span>no_data_algorithm
<span class="c"># See repo README for full reproduction instructions</span>
</code></pre></div></div> <h2 id="references">References</h2> <ul> <li>Paper: <a href="https://arxiv.org/abs/2506.03083">https://arxiv.org/abs/2506.03083</a></li> <li>Code &amp; data: <a href="https://github.com/adewynter/no_data_algorithm">https://github.com/adewynter/no_data_algorithm</a></li> </ul> <h2 id="further-reading">Further reading</h2> <ul> <li><strong><a href="https://dl.acm.org/doi/10.1145/22145.22192">Trading Group Theory for Randomness</a></strong> (Babai, STOC 1985) — the original Arthur–Merlin paper. The EV protocol’s challenge–response structure descends directly from this construction.</li> <li><strong><a href="https://arxiv.org/abs/2306.05685">Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena</a></strong> (Zheng et al., NeurIPS 2023) — the canonical reference for the LLM-as-a-judge paradigm and the position/verbosity biases that motivate the trust problem this paper tackles.</li> <li><strong><a href="https://arxiv.org/abs/2303.16634">G-Eval: NLG Evaluation using GPT-4 with Better Human Alignment</a></strong> (Liu et al., EMNLP 2023) — a representative GPT-4-as-evaluator system; useful context for why “trust your judge” is now an open question.</li> <li><strong><a href="https://arxiv.org/abs/2412.05579">LLMs-as-Judges: A Comprehensive Survey on LLM-based Evaluation Methods</a></strong> (Li et al., 2024) — broad survey of the field where the No-Data Algorithm sits.</li> <li><strong><a href="https://arxiv.org/abs/2310.18018">NLP Evaluation in trouble: On the Need to Measure LLM Data Contamination for each Benchmark</a></strong> (Sainz et al., EMNLP 2023) — argues that <em>even with</em> labels, benchmark contamination undermines evaluation; this paper is the natural sequel for the no-label case.</li> <li><strong><a href="https://arxiv.org/abs/2408.15409">Awes, Laws, and Flaws From Today’s LLM Research</a></strong> (de Wynter, ACL 2025) — same author’s meta-critique of 2,000+ LLM papers, quantifying the rise of LLM-as-evaluator. The No-Data Algorithm reads as a proposed remedy to that critique.</li> <li><strong><a href="https://arxiv.org/abs/2503.08327">Adding Chocolate to Mint: Mitigating Metric Interference in Machine Translation</a></strong> (Pombal et al., 2025) — complementary work on metric reliability rather than evaluator reliability; both pull at the same problem from different sides.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="llm-as-a-judge"/><category term="evaluation"/><category term="zero-knowledge-proof"/><category term="trust"/><category term="low-resource-language"/><summary type="html"><![CDATA[A zero-knowledge-style challenge–response protocol that certifies the trustworthiness of an evaluator (e.g. LLM-as-a-judge) without any labelled data, bounded by (1/4)^r.]]></summary></entry></feed>